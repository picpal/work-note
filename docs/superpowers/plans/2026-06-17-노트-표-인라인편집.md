# 노트 표 인라인 편집기 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 마크다운 표를 실제 그리드로 렌더하고 셀 위에서 직접 편집(Confluence식)하되, 문서는 항상 GFM 표 마크다운으로 유지한다.

**Architecture:** 마크다운 텍스트가 단일 진실. 순수 함수(`gfmTable.ts`)가 표 소스↔모델 변환·구조조작을 담당하고, CodeMirror block 위젯(`tableWidget.ts`)이 `contenteditable` 셀로 편집 UI를 제공한다. 셀 편집은 DOM 로컬, blur/구조변경/디바운스 시 문서 소스를 트랜잭션으로 재작성한다. `cm.ts`의 `Table` 분기 한 줄만 새 위젯으로 교체한다.

**Tech Stack:** CodeMirror 6 (`@codemirror/view` `WidgetType`, `@codemirror/language` `syntaxTree`), TypeScript, Vitest(node 환경, 순수 로직만).

**스펙:** `docs/superpowers/specs/2026-06-17-노트-표-인라인편집-design.md`

---

## 파일 구조

| 파일 | 책임 | 상태 |
|---|---|---|
| `frontend/src/editor/gfmTable.ts` | 순수: `TableModel` 타입, `parse`/`serialize`, `escape`/`unescape`, 구조조작 5종. CM·DOM 의존 없음 | 신규 |
| `frontend/src/editor/gfmTable.test.ts` | Vitest 단위 테스트(파싱·직렬화·라운드트립·구조조작) | 신규 |
| `frontend/src/editor/tableWidget.ts` | `TableWidget(WidgetType)`: DOM 빌드·셀편집·커밋·키보드·핸들/메뉴. CM/DOM 의존 | 신규 |
| `frontend/src/editor/cm.ts` | `Table` 분기에서 `RenderWidget("table")` → `TableWidget` 교체, `RenderWidget`의 table 케이스 제거 | 수정 |
| `frontend/src/styles/app.css` | 표 그리드·핸들·메뉴·정렬 스타일 | 수정 |

**테스트 가능 경계:** node 환경이라 DOM 위젯은 단위 테스트 불가(프로젝트 관례: 컴포넌트 렌더 테스트 없음). → **Task 1–4(순수 함수)는 엄격 TDD**, **Task 5–9(위젯/CSS)는 `pnpm build` 타입체크 + 개발서버 수동 검증**.

**실행 명령 참고:**
- 단일 테스트: `pnpm exec vitest run src/editor/gfmTable.test.ts`
- 전체 테스트: `pnpm test`
- 타입체크+빌드: `pnpm build`
- 개발서버(http): `VITE_STORAGE=http pnpm dev` (백엔드 기동 필요) — 표 편집 수동 검증용

모든 명령은 `frontend/` 디렉토리에서 실행. 커밋은 리포 루트(`work-note/`)에서 실행하거나 `git -C` 사용.

---

## Task 1: gfmTable.ts — 타입 + escape/unescape 헬퍼

**Files:**
- Create: `frontend/src/editor/gfmTable.ts`
- Create: `frontend/src/editor/gfmTable.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

`frontend/src/editor/gfmTable.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { escapeCell, unescapeCell } from "./gfmTable";

describe("escapeCell / unescapeCell", () => {
  it("파이프를 이스케이프한다", () => {
    expect(escapeCell("a|b")).toBe("a\\|b");
  });
  it("개행을 <br>로 바꾼다", () => {
    expect(escapeCell("line1\nline2")).toBe("line1<br>line2");
    expect(escapeCell("a\r\nb")).toBe("a<br>b");
  });
  it("unescape는 <br>과 \\| 를 되돌린다", () => {
    expect(unescapeCell("a\\|b")).toBe("a|b");
    expect(unescapeCell("line1<br>line2")).toBe("line1\nline2");
    expect(unescapeCell("a<br/>b")).toBe("a\nb");
  });
  it("escape→unescape 왕복", () => {
    const s = "x|y\nz";
    expect(unescapeCell(escapeCell(s))).toBe(s);
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `pnpm exec vitest run src/editor/gfmTable.test.ts`
Expected: FAIL — `escapeCell`/`unescapeCell` is not exported (모듈/함수 없음).

- [ ] **Step 3: 최소 구현**

`frontend/src/editor/gfmTable.ts`:

```ts
// gfmTable.ts — GFM 표 마크다운 ↔ 모델 순수 변환 + 구조조작. CodeMirror/DOM 의존 없음.
// 모델의 셀 텍스트는 "논리 평문"(파이프=|, 줄바꿈=\n)이며, 직렬화 시에만 GFM 안전 형태로 이스케이프한다.

export type Align = "none" | "left" | "center" | "right";

export interface TableModel {
  align: Align[];   // 열 개수만큼. 구분행에서 파싱
  header: string[]; // 헤더 셀(논리 평문)
  rows: string[][]; // 본문 행들. 각 행은 열 개수에 맞춰 정규화
}

/** 논리 평문 → GFM 셀 안전 형태: 내부 파이프 \| , 줄바꿈 <br>. */
export function escapeCell(text: string): string {
  return text.replace(/\|/g, "\\|").replace(/\r?\n/g, "<br>");
}

/** GFM 셀 → 논리 평문: <br> → \n, \| → |. (순서 주의: <br> 먼저) */
export function unescapeCell(raw: string): string {
  return raw.replace(/<br\s*\/?>/gi, "\n").replace(/\\\|/g, "|");
}
```

- [ ] **Step 4: 통과 확인**

Run: `pnpm exec vitest run src/editor/gfmTable.test.ts`
Expected: PASS (4 tests)

- [ ] **Step 5: 커밋**

```bash
git -C /Users/picpal/Desktop/workspace/work-note add frontend/src/editor/gfmTable.ts frontend/src/editor/gfmTable.test.ts
git -C /Users/picpal/Desktop/workspace/work-note commit -m "feat(table): gfmTable 셀 escape/unescape 헬퍼"
```

---

## Task 2: gfmTable.ts — parseGfmTable

**Files:**
- Modify: `frontend/src/editor/gfmTable.ts`
- Test: `frontend/src/editor/gfmTable.test.ts`

- [ ] **Step 1: 실패하는 테스트 추가**

`gfmTable.test.ts`에 추가 (import 줄에 `parseGfmTable` 추가):

```ts
import { escapeCell, unescapeCell, parseGfmTable } from "./gfmTable";

describe("parseGfmTable", () => {
  it("표준 표를 파싱한다", () => {
    const m = parseGfmTable("| 항목 | 설명 |\n| --- | --- |\n| a | b |\n| c | d |");
    expect(m).toEqual({
      align: ["none", "none"],
      header: ["항목", "설명"],
      rows: [["a", "b"], ["c", "d"]],
    });
  });
  it("정렬 마커 4종을 파싱한다", () => {
    const m = parseGfmTable("| h1 | h2 | h3 | h4 |\n| --- | :--- | :---: | ---: |\n| 1 | 2 | 3 | 4 |");
    expect(m!.align).toEqual(["none", "left", "center", "right"]);
  });
  it("선/후행 파이프가 없어도 파싱한다", () => {
    const m = parseGfmTable("a | b\n--- | ---\n1 | 2");
    expect(m).toEqual({ align: ["none", "none"], header: ["a", "b"], rows: [["1", "2"]] });
  });
  it("이스케이프된 \\| 는 셀을 분리하지 않는다", () => {
    const m = parseGfmTable("| h |\n| --- |\n| a\\|b |");
    expect(m!.rows).toEqual([["a|b"]]);
  });
  it("열 수가 부족한 행은 빈 셀로, 초과 셀은 버려 직사각형을 유지한다", () => {
    const m = parseGfmTable("| h1 | h2 |\n| --- | --- |\n| a |\n| x | y | z |");
    expect(m!.rows).toEqual([["a", ""], ["x", "y"]]);
  });
  it("헤더만 있는 표(본문 0행)도 유효", () => {
    const m = parseGfmTable("| h1 | h2 |\n| --- | --- |");
    expect(m).toEqual({ align: ["none", "none"], header: ["h1", "h2"], rows: [] });
  });
  it("구분행이 없으면 null", () => {
    expect(parseGfmTable("| a | b |\n| c | d |")).toBeNull();
  });
  it("두 줄 미만이면 null", () => {
    expect(parseGfmTable("| a |")).toBeNull();
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `pnpm exec vitest run src/editor/gfmTable.test.ts`
Expected: FAIL — `parseGfmTable` is not exported.

- [ ] **Step 3: 최소 구현**

`gfmTable.ts`에 추가:

```ts
const DELIM_CELL = /^:?-+:?$/;

/** 한 줄에서 이스케이프되지 않은 파이프로 셀 분리. 선/후행 파이프 제거, 각 셀 trim. */
function splitCells(line: string): string[] {
  let s = line.trim();
  if (s.startsWith("|")) s = s.slice(1);
  if (s.endsWith("|") && !s.endsWith("\\|")) s = s.slice(0, -1);
  const cells: string[] = [];
  let buf = "";
  for (let i = 0; i < s.length; i++) {
    if (s[i] === "\\" && s[i + 1] === "|") { buf += "\\|"; i++; continue; }
    if (s[i] === "|") { cells.push(buf.trim()); buf = ""; continue; }
    buf += s[i];
  }
  cells.push(buf.trim());
  return cells;
}

function parseAlign(cell: string): Align {
  const c = cell.trim();
  const left = c.startsWith(":");
  const right = c.endsWith(":");
  if (left && right) return "center";
  if (right) return "right";
  if (left) return "left";
  return "none";
}

/** GFM 표 소스(여러 줄) → 모델. 표가 아니면 null. */
export function parseGfmTable(src: string): TableModel | null {
  const lines = src.split(/\r?\n/).filter((l) => l.trim() !== "");
  if (lines.length < 2) return null;
  const header = splitCells(lines[0]);
  const delim = splitCells(lines[1]);
  if (delim.length === 0 || !delim.every((c) => DELIM_CELL.test(c))) return null;
  const ncol = header.length;
  const align: Align[] = [];
  for (let i = 0; i < ncol; i++) align.push(parseAlign(delim[i] ?? ""));
  const normRow = (cells: string[]): string[] => {
    const r = cells.slice(0, ncol).map(unescapeCell);
    while (r.length < ncol) r.push("");
    return r;
  };
  return {
    align,
    header: header.map(unescapeCell),
    rows: lines.slice(2).map((l) => normRow(splitCells(l))),
  };
}
```

- [ ] **Step 4: 통과 확인**

Run: `pnpm exec vitest run src/editor/gfmTable.test.ts`
Expected: PASS (12 tests)

- [ ] **Step 5: 커밋**

```bash
git -C /Users/picpal/Desktop/workspace/work-note add frontend/src/editor/gfmTable.ts frontend/src/editor/gfmTable.test.ts
git -C /Users/picpal/Desktop/workspace/work-note commit -m "feat(table): parseGfmTable — 표 소스 파싱"
```

---

## Task 3: gfmTable.ts — serializeGfmTable + 라운드트립

**Files:**
- Modify: `frontend/src/editor/gfmTable.ts`
- Test: `frontend/src/editor/gfmTable.test.ts`

- [ ] **Step 1: 실패하는 테스트 추가**

import 줄에 `serializeGfmTable` 추가, 아래 describe 추가:

```ts
describe("serializeGfmTable", () => {
  it("정렬 마커를 출력한다", () => {
    const out = serializeGfmTable({
      align: ["none", "left", "center", "right"],
      header: ["h1", "h2", "h3", "h4"],
      rows: [["1", "2", "3", "4"]],
    });
    expect(out).toBe("| h1 | h2 | h3 | h4 |\n| --- | :--- | :---: | ---: |\n| 1 | 2 | 3 | 4 |");
  });
  it("셀의 파이프·개행을 이스케이프한다", () => {
    const out = serializeGfmTable({ align: ["none"], header: ["h"], rows: [["a|b\nc"]] });
    expect(out).toBe("| h |\n| --- |\n| a\\|b<br>c |");
  });
  it("헤더만 있는 표를 직렬화한다", () => {
    expect(serializeGfmTable({ align: ["none", "none"], header: ["a", "b"], rows: [] }))
      .toBe("| a | b |\n| --- | --- |");
  });
});

describe("parse ∘ serialize 라운드트립", () => {
  const fixtures: TableModel[] = [
    { align: ["none", "none"], header: ["항목", "설명"], rows: [["a", "b"], ["c", "d"]] },
    { align: ["left", "center", "right"], header: ["x", "y", "z"], rows: [["1", "2", "3"]] },
    { align: ["none"], header: ["pipe"], rows: [["a|b"], ["c\nd"]] },
    { align: ["none", "none"], header: ["h1", "h2"], rows: [] },
  ];
  it("parse(serialize(m)) === m", () => {
    for (const m of fixtures) {
      expect(parseGfmTable(serializeGfmTable(m))).toEqual(m);
    }
  });
});
```

`import type { TableModel } from "./gfmTable";` 도 추가(테스트에서 타입 사용). 기존 import 줄을 다음과 같이:

```ts
import { escapeCell, unescapeCell, parseGfmTable, serializeGfmTable } from "./gfmTable";
import type { TableModel } from "./gfmTable";
```

- [ ] **Step 2: 실패 확인**

Run: `pnpm exec vitest run src/editor/gfmTable.test.ts`
Expected: FAIL — `serializeGfmTable` is not exported.

- [ ] **Step 3: 최소 구현**

`gfmTable.ts`에 추가:

```ts
function delimFor(a: Align): string {
  switch (a) {
    case "left": return ":---";
    case "center": return ":---:";
    case "right": return "---:";
    default: return "---";
  }
}

/** 모델 → GFM 표 마크다운(끝 개행 없음). 셀은 양쪽 한 칸 패딩 + 이스케이프. */
export function serializeGfmTable(m: TableModel): string {
  const row = (cells: string[]) => "| " + cells.map(escapeCell).join(" | ") + " |";
  return [
    row(m.header),
    "| " + m.align.map(delimFor).join(" | ") + " |",
    ...m.rows.map(row),
  ].join("\n");
}
```

- [ ] **Step 4: 통과 확인**

Run: `pnpm exec vitest run src/editor/gfmTable.test.ts`
Expected: PASS (16 tests)

- [ ] **Step 5: 커밋**

```bash
git -C /Users/picpal/Desktop/workspace/work-note add frontend/src/editor/gfmTable.ts frontend/src/editor/gfmTable.test.ts
git -C /Users/picpal/Desktop/workspace/work-note commit -m "feat(table): serializeGfmTable + 라운드트립 보장"
```

---

## Task 4: gfmTable.ts — 구조조작 5종

**Files:**
- Modify: `frontend/src/editor/gfmTable.ts`
- Test: `frontend/src/editor/gfmTable.test.ts`

- [ ] **Step 1: 실패하는 테스트 추가**

import 줄에 `insertRow, deleteRow, insertColumn, deleteColumn, setAlign` 추가:

```ts
import {
  escapeCell, unescapeCell, parseGfmTable, serializeGfmTable,
  insertRow, deleteRow, insertColumn, deleteColumn, setAlign,
} from "./gfmTable";

describe("구조조작", () => {
  const base = (): TableModel => ({
    align: ["none", "none"],
    header: ["h1", "h2"],
    rows: [["a", "b"], ["c", "d"]],
  });

  it("insertRow: 인덱스 위치에 빈 행 삽입", () => {
    expect(insertRow(base(), 1).rows).toEqual([["a", "b"], ["", ""], ["c", "d"]]);
  });
  it("insertRow: 범위 초과는 끝에 추가", () => {
    expect(insertRow(base(), 99).rows).toEqual([["a", "b"], ["c", "d"], ["", ""]]);
  });
  it("deleteRow: 본문 행 삭제", () => {
    expect(deleteRow(base(), 0).rows).toEqual([["c", "d"]]);
  });
  it("deleteRow: 마지막 본문 행 삭제 → 헤더만", () => {
    let m = deleteRow(base(), 0);
    m = deleteRow(m, 0);
    expect(m.rows).toEqual([]);
  });
  it("deleteRow: 범위 밖이면 그대로", () => {
    expect(deleteRow(base(), 5)).toEqual(base());
  });
  it("insertColumn: 헤더·정렬·모든 행에 빈 열 삽입", () => {
    const m = insertColumn(base(), 1);
    expect(m.header).toEqual(["h1", "", "h2"]);
    expect(m.align).toEqual(["none", "none", "none"]);
    expect(m.rows).toEqual([["a", "", "b"], ["c", "", "d"]]);
  });
  it("deleteColumn: 열 삭제", () => {
    const m = deleteColumn(base(), 0);
    expect(m.header).toEqual(["h2"]);
    expect(m.align).toEqual(["none"]);
    expect(m.rows).toEqual([["b"], ["d"]]);
  });
  it("deleteColumn: 마지막 1열은 삭제 거부", () => {
    const single: TableModel = { align: ["none"], header: ["h"], rows: [["a"]] };
    expect(deleteColumn(single, 0)).toEqual(single);
  });
  it("setAlign: 지정 열 정렬 변경", () => {
    expect(setAlign(base(), 1, "center").align).toEqual(["none", "center"]);
  });
  it("setAlign: 범위 밖이면 그대로", () => {
    expect(setAlign(base(), 9, "right")).toEqual(base());
  });
  it("순수성: 원본 불변", () => {
    const m = base();
    insertRow(m, 0); deleteColumn(m, 0); setAlign(m, 0, "right");
    expect(m.rows).toEqual([["a", "b"], ["c", "d"]]);
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `pnpm exec vitest run src/editor/gfmTable.test.ts`
Expected: FAIL — `insertRow` 등 미export.

- [ ] **Step 3: 최소 구현**

`gfmTable.ts`에 추가:

```ts
function emptyRow(n: number): string[] {
  return Array.from({ length: n }, () => "");
}

/** atBodyIndex 위치에 빈 본문 행 삽입(범위 초과는 끝에). */
export function insertRow(m: TableModel, atBodyIndex: number): TableModel {
  const rows = m.rows.map((r) => r.slice());
  const i = Math.max(0, Math.min(atBodyIndex, rows.length));
  rows.splice(i, 0, emptyRow(m.header.length));
  return { ...m, rows };
}

/** 본문 행 삭제(범위 밖이면 그대로). 헤더만 남는 것 허용. */
export function deleteRow(m: TableModel, bodyIndex: number): TableModel {
  if (bodyIndex < 0 || bodyIndex >= m.rows.length) return m;
  const rows = m.rows.map((r) => r.slice());
  rows.splice(bodyIndex, 1);
  return { ...m, rows };
}

/** atIndex 위치에 빈 열 삽입(align="none"). */
export function insertColumn(m: TableModel, atIndex: number): TableModel {
  const i = Math.max(0, Math.min(atIndex, m.header.length));
  const header = m.header.slice(); header.splice(i, 0, "");
  const align = m.align.slice(); align.splice(i, 0, "none");
  const rows = m.rows.map((r) => { const c = r.slice(); c.splice(i, 0, ""); return c; });
  return { align, header, rows };
}

/** 열 삭제. 최소 1열 유지(마지막 열 삭제 거부). */
export function deleteColumn(m: TableModel, index: number): TableModel {
  if (m.header.length <= 1) return m;
  if (index < 0 || index >= m.header.length) return m;
  const header = m.header.slice(); header.splice(index, 1);
  const align = m.align.slice(); align.splice(index, 1);
  const rows = m.rows.map((r) => { const c = r.slice(); c.splice(index, 1); return c; });
  return { align, header, rows };
}

/** 지정 열 정렬 변경(범위 밖이면 그대로). */
export function setAlign(m: TableModel, index: number, align: Align): TableModel {
  if (index < 0 || index >= m.align.length) return m;
  const a = m.align.slice(); a[index] = align;
  return { ...m, align: a };
}
```

- [ ] **Step 4: 통과 확인**

Run: `pnpm exec vitest run src/editor/gfmTable.test.ts`
Expected: PASS (27 tests)

- [ ] **Step 5: 전체 테스트 회귀 확인**

Run: `pnpm test`
Expected: 기존 테스트 전부 PASS + gfmTable 27 tests PASS.

- [ ] **Step 6: 커밋**

```bash
git -C /Users/picpal/Desktop/workspace/work-note add frontend/src/editor/gfmTable.ts frontend/src/editor/gfmTable.test.ts
git -C /Users/picpal/Desktop/workspace/work-note commit -m "feat(table): 구조조작(insert/delete row·col, setAlign)"
```

---

## Task 5: tableWidget.ts — 그리드 렌더 + cm.ts 배선 + 기본 CSS

읽기 렌더만 먼저(편집 없음). 현재 읽기전용 동작과 동등하되 새 위젯 경로로 그린다.

**Files:**
- Create: `frontend/src/editor/tableWidget.ts`
- Modify: `frontend/src/editor/cm.ts:278-286` (Table 분기), `cm.ts:149-181` (RenderWidget table 케이스 제거)
- Modify: `frontend/src/styles/app.css` (표 기본 스타일 추가)

- [ ] **Step 1: tableWidget.ts 생성 (렌더 + 헬퍼만)**

`frontend/src/editor/tableWidget.ts`:

```ts
// tableWidget.ts — CodeMirror block 위젯: GFM 표를 편집 가능한 그리드로 렌더.
// 셀=contenteditable(plaintext-only). 편집은 DOM 로컬, 커밋 시 문서 소스 재작성(Task 6+).
import { EditorView, WidgetType } from "@codemirror/view";
import { syntaxTree } from "@codemirror/language";
import { parseGfmTable, serializeGfmTable } from "./gfmTable";
import type { TableModel, Align } from "./gfmTable";

/** DOM의 .cm-cell들을 읽어 모델로 복원 → 직렬화. (핸들 셀은 .cm-cell 아님 → 무시) */
export function serializeFromDom(dom: HTMLElement): string {
  const headerCells = Array.from(dom.querySelectorAll(".cm-header-row .cm-cell")) as HTMLElement[];
  const header = headerCells.map((c) => c.innerText);
  const align = headerCells.map((c) => (c.dataset.align as Align) || "none");
  const rows = (Array.from(dom.querySelectorAll(".cm-table tbody tr")) as HTMLElement[]).map((tr) =>
    (Array.from(tr.querySelectorAll(".cm-cell")) as HTMLElement[]).map((c) => c.innerText),
  );
  return serializeGfmTable({ align, header, rows });
}

export class TableWidget extends WidgetType {
  dom?: HTMLElement;
  protected view?: EditorView;
  constructor(public source: string) {
    super();
  }

  eq(other: TableWidget): boolean {
    if (!this.dom) return this.source === other.source;
    if (other.source === this.source) return true;          // 무변경 트랜잭션 → DOM 유지(포커스 보존)
    return serializeFromDom(this.dom) === other.source;     // DOM이 이미 반영(셀편집 커밋) → 유지, 아니면 재생성
  }

  ignoreEvent(): boolean {
    return true; // 위젯 내부 이벤트는 CM이 문서 편집으로 오인하지 않게 차단(키/메뉴는 위젯이 자체 처리)
  }

  get estimatedHeight(): number {
    return 80;
  }

  toDOM(view: EditorView): HTMLElement {
    this.view = view;
    const model = parseGfmTable(this.source) ?? { align: ["none"], header: [""], rows: [] };
    const wrap = document.createElement("div");
    wrap.className = "cm-table-widget";
    wrap.contentEditable = "false";

    const scroll = document.createElement("div");
    scroll.className = "cm-table-scroll";
    const table = document.createElement("table");
    table.className = "cm-table";

    const thead = document.createElement("thead");
    const headerRow = document.createElement("tr");
    headerRow.className = "cm-header-row";
    model.header.forEach((text, col) => headerRow.appendChild(this.makeCell("th", text, model.align[col] ?? "none")));
    thead.appendChild(headerRow);
    table.appendChild(thead);

    const tbody = document.createElement("tbody");
    model.rows.forEach((row) => {
      const tr = document.createElement("tr");
      row.forEach((text, col) => tr.appendChild(this.makeCell("td", text, model.align[col] ?? "none")));
      tbody.appendChild(tr);
    });
    table.appendChild(tbody);

    scroll.appendChild(table);
    wrap.appendChild(scroll);
    this.dom = wrap;
    return wrap;
  }

  /** 셀 엘리먼트 생성. Task 6에서 편집 리스너를 덧붙인다. */
  protected makeCell(tag: "th" | "td", text: string, align: Align): HTMLElement {
    const cell = document.createElement(tag);
    cell.className = "cm-cell";
    cell.dataset.align = align;
    if (align !== "none") cell.style.textAlign = align;
    cell.textContent = text;
    return cell;
  }

  /** 위젯의 현재 문서 범위를 syntaxTree로 재해석(위치 이동에 강함). */
  protected currentRange(): { from: number; to: number } | null {
    if (!this.view || !this.dom) return null;
    const view = this.view;
    let pos: number;
    try {
      pos = view.posAtDOM(this.dom);
    } catch {
      return null;
    }
    let node: ReturnType<typeof syntaxTree>["topNode"] | null = syntaxTree(view.state).resolveInner(pos, 1);
    while (node && node.name !== "Table") node = node.parent;
    if (!node) return null;
    const doc = view.state.doc;
    const first = doc.lineAt(node.from);
    const last = doc.lineAt(Math.min(node.to, doc.length));
    return { from: first.from, to: last.to };
  }
}
```

- [ ] **Step 2: cm.ts에서 TableWidget 사용**

`cm.ts` 상단 import 영역(21행 `renderMarkdown` import 부근)에 추가:

```ts
import { TableWidget } from "./tableWidget";
```

`cm.ts`의 `Table` 분기(현 278–286행)를 교체:

```ts
        if (name === "Table") {
          if (!selTouches(nf, nt)) {
            const firstLine = state.doc.lineAt(nf), lastLine = state.doc.lineAt(Math.min(nt, state.doc.length));
            const src = state.sliceDoc(firstLine.from, lastLine.to);
            out.push(Decoration.replace({ widget: new TableWidget(src), block: true }).range(firstLine.from, lastLine.to));
            return false;
          }
          return; // 선택이 표에 걸침 → raw
        }
```

`RenderWidget`의 table 경로 제거 — `RenderWidget.toDOM`(현 156–177행)에서 `else { ... renderMarkdown ... }` 블록은 mermaid가 아닌 codecard용으로 남는다. table은 더 이상 `RenderWidget`으로 생성되지 않으므로 `estimatedHeight`의 `"table"` 비교만 정리:

```ts
  get estimatedHeight() { return this.kind === "mermaid" ? 180 : 80; }
```
(이미 table 특수분기가 없으므로 변경 불필요 — 확인만. RenderWidget은 mermaid/codecard 전용으로 계속 사용.)

- [ ] **Step 3: 기본 CSS 추가**

`frontend/src/styles/app.css` 끝에 추가:

```css
/* ---- 표 인라인 편집 위젯 ---- */
.cm-table-widget { position: relative; margin: 14px 0; }
.cm-table-scroll { overflow-x: auto; }
.cm-table {
  border-collapse: collapse;
  width: auto;
  min-width: 60%;
  font-size: 14px;
}
.cm-table .cm-cell {
  border: 1px solid var(--border);
  padding: 7px 11px;
  vertical-align: top;
  white-space: pre-wrap;       /* 셀 내 \n(<br>) 표시 */
  word-break: break-word;
  min-width: 48px;
}
.cm-table thead .cm-cell {
  background: var(--bg-elev);
  font-weight: 600;
}
.cm-table .cm-cell:focus {
  outline: 2px solid var(--ink);
  outline-offset: -2px;
}
```

- [ ] **Step 4: 타입체크 + 빌드**

Run: `pnpm build`
Expected: `✓ built` (타입 에러 없음). 에러 시 import/시그니처 정합 수정.

- [ ] **Step 5: 수동 검증**

`VITE_STORAGE=http pnpm dev`로 개발서버 기동(백엔드 필요), 표가 있는 노트를 연다(없으면 툴바 "표" 버튼으로 삽입).
- 기대: 커서가 표 밖일 때 표가 그리드로 렌더된다(테두리·헤더 배경). 아직 셀 클릭 편집은 안 됨(다음 태스크).
- 코드블록·체크박스·이미지·mermaid 위젯이 그대로 동작하는지 확인(회귀 없음).

- [ ] **Step 6: 커밋**

```bash
git -C /Users/picpal/Desktop/workspace/work-note add frontend/src/editor/tableWidget.ts frontend/src/editor/cm.ts frontend/src/styles/app.css
git -C /Users/picpal/Desktop/workspace/work-note commit -m "feat(table): TableWidget 그리드 렌더 + cm.ts 배선"
```

---

## Task 6: 셀 편집 + 커밋(blur/디바운스/destroy) + 포커스 보존

**Files:**
- Modify: `frontend/src/editor/tableWidget.ts`

- [ ] **Step 1: makeCell에 편집 리스너 추가**

`tableWidget.ts`의 `makeCell`를 다음으로 교체:

```ts
  protected makeCell(tag: "th" | "td", text: string, align: Align): HTMLElement {
    const cell = document.createElement(tag);
    cell.className = "cm-cell";
    cell.dataset.align = align;
    if (align !== "none") cell.style.textAlign = align;
    cell.setAttribute("contenteditable", "plaintext-only"); // 평문 셀 — 리치 붙여넣기 차단
    cell.textContent = text;
    cell.addEventListener("input", () => this.scheduleCommit(1000)); // 타이핑 디바운스
    cell.addEventListener("blur", () => this.commit());               // 포커스 이탈 즉시 커밋
    return cell;
  }
```

- [ ] **Step 2: 커밋 메서드 추가**

`TableWidget` 클래스에 필드와 메서드 추가(클래스 상단 필드 영역에 `commitTimer`, 메서드 영역에 아래):

```ts
  private commitTimer?: ReturnType<typeof setTimeout>;

  private scheduleCommit(delay: number): void {
    if (this.commitTimer) clearTimeout(this.commitTimer);
    this.commitTimer = setTimeout(() => this.commit(), delay);
  }

  /** 현재 DOM을 직렬화해 문서 표 범위를 치환. 변경 없으면 skip. */
  protected commit(): void {
    if (this.commitTimer) { clearTimeout(this.commitTimer); this.commitTimer = undefined; }
    if (!this.view || !this.dom) return;
    const range = this.currentRange();
    if (!range) return;
    const next = serializeFromDom(this.dom);
    if (this.view.state.sliceDoc(range.from, range.to) === next) return; // no-op
    try {
      this.view.dispatch({ changes: { from: range.from, to: range.to, insert: next } });
    } catch {
      /* 뷰가 파괴 중이면 무시 */
    }
  }

  destroy(): void {
    this.commit(); // 언마운트/노트전환 안전망 — 미커밋분 최종 반영(best-effort)
  }
```

`serializeFromDom`은 이미 모듈 상단에서 import/정의됨(Task 5). `commit`이 호출하는 `serializeFromDom(this.dom)` 정상 참조.

- [ ] **Step 3: 타입체크 + 빌드**

Run: `pnpm build`
Expected: `✓ built`.

- [ ] **Step 4: 수동 검증**

개발서버에서:
1. 셀 클릭 → 텍스트 편집 → 다른 곳 클릭(blur) → 표 밖에 커서 두고 원문을 보면(드래그 선택 등) 마크다운이 갱신됐는지 확인. 또는 노트 재진입 후 값 유지 확인.
2. 셀에서 연속 타이핑 중 **캐럿이 날아가지 않는지** 확인(1초 디바운스 커밋이 일어나도 포커스 유지 — eq 보존).
3. 셀에 `|` 입력 후 커밋 → 내보내기(또는 raw)에서 `\|`로 보존되는지.

- [ ] **Step 5: 커밋**

```bash
git -C /Users/picpal/Desktop/workspace/work-note add frontend/src/editor/tableWidget.ts
git -C /Users/picpal/Desktop/workspace/work-note commit -m "feat(table): 셀 편집 + 커밋(blur·디바운스·destroy) + 포커스 보존"
```

---

## Task 7: 키보드 내비게이션 (Tab/Enter/Shift+Enter) + 구조 적용

**Files:**
- Modify: `frontend/src/editor/tableWidget.ts`

- [ ] **Step 1: 좌표/포커스 헬퍼 + applyOp + pendingFocus 추가**

모듈 상단(클래스 밖, import 아래)에 추가:

```ts
// 구조 변경으로 위젯이 재생성될 때, 새 toDOM이 소비해 포커스를 복원할 좌표. r=0은 헤더행, r>=1은 본문행(r-1).
let pendingFocus: { r: number; col: number } | null = null;
```

`TableWidget` 클래스에 메서드 추가:

```ts
  protected colCount(): number {
    return this.dom ? this.dom.querySelectorAll(".cm-header-row .cm-cell").length : 0;
  }
  protected bodyRowCount(): number {
    return this.dom ? this.dom.querySelectorAll(".cm-table tbody tr").length : 0;
  }
  protected allCells(): HTMLElement[] {
    return this.dom ? (Array.from(this.dom.querySelectorAll(".cm-cell")) as HTMLElement[]) : [];
  }
  /** flat .cm-cell 인덱스 → {r, col} (r=0 헤더, r>=1 본문). */
  protected coordOf(flatIdx: number): { r: number; col: number } {
    const ncol = this.colCount() || 1;
    if (flatIdx < ncol) return { r: 0, col: flatIdx };
    const b = flatIdx - ncol;
    return { r: 1 + Math.floor(b / ncol), col: b % ncol };
  }
  protected cellAt(r: number, col: number): HTMLElement | undefined {
    if (!this.dom) return undefined;
    if (r === 0) return this.dom.querySelectorAll(".cm-header-row .cm-cell")[col] as HTMLElement | undefined;
    const tr = this.dom.querySelectorAll(".cm-table tbody tr")[r - 1] as HTMLElement | undefined;
    return tr?.querySelectorAll(".cm-cell")[col] as HTMLElement | undefined;
  }
  protected focusCellAt(r: number, col: number): void {
    this.cellAt(r, col)?.focus();
  }

  /** 모델 변형 후 재직렬화→dispatch. 위젯 재생성 시 새 toDOM이 pendingFocus로 포커스 복원. */
  protected applyOp(fn: (m: TableModel) => TableModel, focus: { r: number; col: number }): void {
    if (!this.view || !this.dom) return;
    const m = parseGfmTable(serializeFromDom(this.dom));
    if (!m) return;
    const next = serializeGfmTable(fn(m));
    const range = this.currentRange();
    if (!range) return;
    pendingFocus = focus;
    try {
      this.view.dispatch({ changes: { from: range.from, to: range.to, insert: next } });
    } catch {
      pendingFocus = null;
    }
  }
```

- [ ] **Step 2: makeCell에 keydown 핸들러 연결**

`makeCell`의 리스너 등록부에 한 줄 추가(input/blur 등록 아래):

```ts
    cell.addEventListener("keydown", (e) => this.onCellKey(e, cell));
```

`onCellKey` 메서드 추가:

```ts
  protected onCellKey(e: KeyboardEvent, cell: HTMLElement): void {
    if (e.key === "Tab") {
      e.preventDefault();
      const cells = this.allCells();
      const idx = cells.indexOf(cell);
      if (e.shiftKey) {
        if (idx > 0) cells[idx - 1].focus();
        return;
      }
      if (idx + 1 < cells.length) { cells[idx + 1].focus(); return; }
      // 마지막 셀에서 Tab → 새 본문 행 추가 후 첫 셀
      this.commit(); // 현재 편집분 먼저 반영
      const newRowVisual = 1 + this.bodyRowCount();
      this.applyOp((m) => insertRow(m, m.rows.length), { r: newRowVisual, col: 0 });
      return;
    }
    if (e.key === "Enter" && !e.shiftKey) {
      e.preventDefault();
      const cells = this.allCells();
      const { r, col } = this.coordOf(cells.indexOf(cell));
      const lastVisualRow = this.bodyRowCount(); // 본문 마지막 행의 visual r
      if (r >= lastVisualRow) {
        this.commit();
        this.applyOp((m) => insertRow(m, m.rows.length), { r: 1 + this.bodyRowCount(), col });
      } else {
        this.focusCellAt(r + 1, col);
      }
      return;
    }
    // Shift+Enter: plaintext-only 기본 줄바꿈 허용(preventDefault 안 함) → innerText에 \n
  }
```

- [ ] **Step 3: toDOM 끝에서 pendingFocus 소비**

`toDOM`의 `this.dom = wrap; return wrap;` 직전에 추가:

```ts
    if (pendingFocus) {
      const f = pendingFocus;
      pendingFocus = null;
      queueMicrotask(() => this.focusCellAt(f.r, f.col));
    }
    this.dom = wrap;
    return wrap;
```

`insertRow`를 사용하므로 `tableWidget.ts` import에 추가:

```ts
import { parseGfmTable, serializeGfmTable, insertRow } from "./gfmTable";
```

- [ ] **Step 4: 타입체크 + 빌드**

Run: `pnpm build`
Expected: `✓ built`.

- [ ] **Step 5: 수동 검증**

1. 셀에서 Tab → 다음 셀, 마지막 셀에서 Tab → **새 행 생성 + 새 행 첫 셀 포커스**.
2. Shift+Tab → 이전 셀.
3. Enter → 아래 셀, 마지막 행 Enter → 새 행.
4. Shift+Enter → 셀 안 줄바꿈, blur 후 raw에서 `<br>` 확인.
5. 행 추가 후에도 포커스가 새 셀에 정확히 들어가는지(pendingFocus 복원).

- [ ] **Step 6: 커밋**

```bash
git -C /Users/picpal/Desktop/workspace/work-note add frontend/src/editor/tableWidget.ts
git -C /Users/picpal/Desktop/workspace/work-note commit -m "feat(table): 키보드 내비게이션(Tab·Enter·Shift+Enter) + 행 자동 추가"
```

---

## Task 8: 행/열 핸들 + 메뉴 (삽입·삭제·정렬)

표에 핸들 행/열을 통합 렌더(절대 위치 계산 회피). 핸들 클릭 → 메뉴 팝오버.

**Files:**
- Modify: `frontend/src/editor/tableWidget.ts`
- Modify: `frontend/src/styles/app.css`

- [ ] **Step 1: toDOM에 핸들 행/열 통합**

`toDOM`에서 thead/tbody 구성을 다음으로 교체(헤더 위에 열 핸들 행, 각 행 앞에 행/코너 핸들 셀 추가):

```ts
    const thead = document.createElement("thead");

    // (1) 열 핸들 행
    const handleRow = document.createElement("tr");
    handleRow.className = "cm-handle-row";
    handleRow.appendChild(this.makeHandleCorner());
    model.header.forEach((_t, col) => handleRow.appendChild(this.makeColHandle(col)));
    thead.appendChild(handleRow);

    // (2) 헤더 셀 행 (앞에 행핸들 자리 코너)
    const headerRow = document.createElement("tr");
    headerRow.className = "cm-header-row";
    headerRow.appendChild(this.makeHandleCorner());
    model.header.forEach((text, col) => headerRow.appendChild(this.makeCell("th", text, model.align[col] ?? "none")));
    thead.appendChild(headerRow);
    table.appendChild(thead);

    const tbody = document.createElement("tbody");
    model.rows.forEach((row, bodyIdx) => {
      const tr = document.createElement("tr");
      tr.appendChild(this.makeRowHandle(bodyIdx));
      row.forEach((text, col) => tr.appendChild(this.makeCell("td", text, model.align[col] ?? "none")));
      tbody.appendChild(tr);
    });
    table.appendChild(tbody);
```

- [ ] **Step 2: 핸들/메뉴 메서드 추가**

`import` 줄에 구조조작 함수 보강:

```ts
import { parseGfmTable, serializeGfmTable, insertRow, deleteRow, insertColumn, deleteColumn, setAlign } from "./gfmTable";
```

`TableWidget` 클래스에 추가:

```ts
  private menuEl?: HTMLElement;
  private onDocDown = (e: MouseEvent) => {
    if (this.menuEl && !this.menuEl.contains(e.target as Node)) this.closeMenu();
  };

  private makeHandleCorner(): HTMLElement {
    const c = document.createElement("th");
    c.className = "cm-handle-corner";
    c.contentEditable = "false";
    return c;
  }
  private makeColHandle(col: number): HTMLElement {
    const th = document.createElement("th");
    th.className = "cm-col-handle";
    th.contentEditable = "false";
    th.textContent = "⋯";
    th.title = "열 메뉴";
    th.addEventListener("mousedown", (e) => e.preventDefault());
    th.addEventListener("click", () => this.openColMenu(col, th));
    return th;
  }
  private makeRowHandle(bodyIdx: number): HTMLElement {
    const td = document.createElement("td");
    td.className = "cm-row-handle";
    td.contentEditable = "false";
    td.textContent = "⋮";
    td.title = "행 메뉴";
    td.addEventListener("mousedown", (e) => e.preventDefault());
    td.addEventListener("click", () => this.openRowMenu(bodyIdx, td));
    return td;
  }

  private openColMenu(col: number, anchor: HTMLElement): void {
    this.openMenu(anchor, [
      { label: "← 왼쪽에 열 삽입", run: () => this.applyOp((m) => insertColumn(m, col), { r: 0, col }) },
      { label: "→ 오른쪽에 열 삽입", run: () => this.applyOp((m) => insertColumn(m, col + 1), { r: 0, col: col + 1 }) },
      { label: "🗑 열 삭제", run: () => this.applyOp((m) => deleteColumn(m, col), { r: 0, col: Math.max(0, col - 1) }) },
      { sep: true },
      { label: "⬅ 왼쪽 정렬", run: () => this.applyOp((m) => setAlign(m, col, "left"), { r: 0, col }) },
      { label: "⬛ 가운데 정렬", run: () => this.applyOp((m) => setAlign(m, col, "center"), { r: 0, col }) },
      { label: "➡ 오른쪽 정렬", run: () => this.applyOp((m) => setAlign(m, col, "right"), { r: 0, col }) },
    ]);
  }
  private openRowMenu(bodyIdx: number, anchor: HTMLElement): void {
    this.openMenu(anchor, [
      { label: "↑ 위에 행 삽입", run: () => this.applyOp((m) => insertRow(m, bodyIdx), { r: bodyIdx + 1, col: 0 }) },
      { label: "↓ 아래에 행 삽입", run: () => this.applyOp((m) => insertRow(m, bodyIdx + 1), { r: bodyIdx + 2, col: 0 }) },
      { label: "🗑 행 삭제", run: () => this.applyOp((m) => deleteRow(m, bodyIdx), { r: Math.max(0, bodyIdx), col: 0 }) },
    ]);
  }

  private openMenu(anchor: HTMLElement, items: ({ label: string; run: () => void } | { sep: true })[]): void {
    this.closeMenu();
    if (!this.dom) return;
    const menu = document.createElement("div");
    menu.className = "cm-table-menu";
    menu.contentEditable = "false";
    menu.style.left = anchor.offsetLeft + "px";
    menu.style.top = anchor.offsetTop + anchor.offsetHeight + "px";
    for (const it of items) {
      if ("sep" in it) {
        const sep = document.createElement("div");
        sep.className = "cm-menu-sep";
        menu.appendChild(sep);
        continue;
      }
      const b = document.createElement("button");
      b.type = "button";
      b.textContent = it.label;
      b.addEventListener("mousedown", (e) => e.preventDefault());
      b.addEventListener("click", (e) => { e.preventDefault(); it.run(); this.closeMenu(); });
      menu.appendChild(b);
    }
    this.dom.appendChild(menu);
    this.menuEl = menu;
    setTimeout(() => document.addEventListener("mousedown", this.onDocDown), 0);
  }
  private closeMenu(): void {
    document.removeEventListener("mousedown", this.onDocDown);
    this.menuEl?.remove();
    this.menuEl = undefined;
  }
```

`destroy`에 메뉴 정리 추가:

```ts
  destroy(): void {
    this.closeMenu();
    this.commit();
  }
```

- [ ] **Step 3: 핸들/메뉴 CSS 추가**

`app.css`의 표 스타일 블록에 이어서 추가:

```css
/* 핸들 행/열 — 평소 흐릿, 위젯 hover 시 또렷 */
.cm-handle-corner { border: none; width: 18px; padding: 0; background: transparent; }
.cm-col-handle, .cm-row-handle {
  border: 1px solid transparent;
  color: var(--text-3);
  text-align: center;
  cursor: pointer;
  user-select: none;
  opacity: 0;
  transition: opacity .12s ease, background .12s ease;
  font-size: 12px;
  line-height: 1;
  padding: 2px;
}
.cm-col-handle { height: 16px; }
.cm-row-handle { width: 16px; }
.cm-table-widget:hover .cm-col-handle,
.cm-table-widget:hover .cm-row-handle { opacity: .55; }
.cm-col-handle:hover, .cm-row-handle:hover { opacity: 1; background: var(--bg-elev); border-color: var(--border); }

/* 메뉴 팝오버 */
.cm-table-menu {
  position: absolute;
  z-index: 20;
  background: var(--bg-elev);
  border: 1px solid var(--border);
  border-radius: 8px;
  box-shadow: var(--shadow-pop);
  padding: 4px;
  min-width: 150px;
  display: flex;
  flex-direction: column;
}
.cm-table-menu button {
  font: inherit;
  font-size: 13px;
  text-align: left;
  background: none;
  border: none;
  border-radius: 5px;
  padding: 6px 9px;
  color: var(--text);
  cursor: pointer;
}
.cm-table-menu button:hover { background: var(--sel); }
.cm-menu-sep { height: 1px; background: var(--border); margin: 4px 2px; }
```

- [ ] **Step 4: 타입체크 + 빌드**

Run: `pnpm build`
Expected: `✓ built`.

- [ ] **Step 5: 수동 검증**

1. 표에 hover → 열 위 `⋯`, 행 왼쪽 `⋮` 핸들이 흐릿하게 표시.
2. 열 핸들 클릭 → 메뉴: 왼쪽/오른쪽 삽입·삭제·좌/중/우 정렬. 각각 적용 후 그리드·정렬 반영.
3. 행 핸들 클릭 → 위/아래 삽입·삭제.
4. 메뉴 바깥 클릭 → 닫힘.
5. 정렬 변경 후 raw/내보내기에서 `:---:` 등 구분행 반영 확인.
6. 마지막 1열에서 "열 삭제" → 변화 없음(거부).

- [ ] **Step 6: 커밋**

```bash
git -C /Users/picpal/Desktop/workspace/work-note add frontend/src/editor/tableWidget.ts frontend/src/styles/app.css
git -C /Users/picpal/Desktop/workspace/work-note commit -m "feat(table): 행/열 핸들 + 메뉴(삽입·삭제·정렬)"
```

---

## Task 9: 정리 + 전체 회귀 + 수동 QA 체크리스트

**Files:**
- Modify: `frontend/src/editor/cm.ts` (사용 안 하는 table 잔재 확인)
- 검증만(코드 변경 없을 수 있음)

- [ ] **Step 1: RenderWidget table 잔재 확인**

`cm.ts`에서 `RenderWidget`이 `"table"` kind로 생성되는 곳이 없는지 확인:

Run: `grep -n 'RenderWidget("table"' frontend/src/editor/cm.ts`
Expected: 결과 없음(0건). 있으면 Task 5에서 누락된 것 — TableWidget로 교체.

`RenderWidget`의 mermaid/codecard 사용처는 유지(검증만).

- [ ] **Step 2: 전체 테스트**

Run: `pnpm test`
Expected: 기존 테스트 전부 PASS + gfmTable 27 tests PASS. 회귀 0.

- [ ] **Step 3: 프로덕션 빌드**

Run: `pnpm build`
Expected: `✓ built`, 타입/번들 에러 0.

- [ ] **Step 4: 수동 QA 체크리스트 (개발서버)**

스펙 §9의 7항목을 순서대로:
1. 셀 편집 후 blur → 문서 마크다운 갱신(노트 재진입·raw로 확인).
2. Tab 끝 셀 → 새 행, ⇧Enter → `<br>`.
3. 행/열 추가·삭제, 좌/중/우 정렬 → Markdown 내보내기가 유효한 GFM.
4. 편집 중 위젯 포커스 유지(중간 재렌더에도 캐럿 보존).
5. undo/redo로 표 변경 정확 반영.
6. 코드블록·체크박스·이미지·mermaid 위젯 회귀 없음.
7. `\|` 포함 셀 왕복, 빈 셀, 헤더만 있는 표.

각 항목 통과 여부 기록. 실패 시 systematic-debugging으로 근본원인 분석 후 수정 커밋.

- [ ] **Step 5: 최종 커밋(정리분이 있을 때만)**

```bash
git -C /Users/picpal/Desktop/workspace/work-note add -A frontend/src/editor frontend/src/styles/app.css
git -C /Users/picpal/Desktop/workspace/work-note commit -m "chore(table): 표 인라인 편집기 정리 + 회귀 검증"
```

---

## Self-Review (작성자 체크)

**1. 스펙 커버리지**

| 스펙 요구 | 태스크 |
|---|---|
| 마크다운 단일진실·내보내기 무변경 | Task 5(소스 재작성 방식), Task 9 QA #3 |
| 평문 셀 | Task 6 (`contenteditable=plaintext-only`) |
| `parse`/`serialize`/구조조작 순수함수 | Task 1–4 |
| `TableModel { align, header, rows }` | Task 1 |
| 포커스 보존 eq | Task 5(eq), Task 6 검증 #2 |
| 커밋 시점(blur/디바운스/구조/destroy) | Task 6 |
| CM 경계(ignoreEvent·currentRange) | Task 5 |
| 키보드(Tab/Enter/⇧Enter) | Task 7 |
| 행/열 핸들 + 정렬 메뉴 | Task 8 |
| raw 안전판(selTouches) | Task 5(분기 유지) |
| 엣지(`\|`·빈셀·헤더만) | Task 2–3 테스트, Task 9 QA #7 |

누락 없음.

**2. 플레이스홀더 스캔:** TBD/임의처리 없음. 코드 블록 전부 실제 코드.

**3. 타입 일관성:** `TableModel`/`Align`/`parseGfmTable`/`serializeGfmTable`/`insertRow`/`deleteRow`/`insertColumn`/`deleteColumn`/`setAlign`/`escapeCell`/`unescapeCell`/`serializeFromDom`/`TableWidget` 명칭이 모든 태스크에서 일치. 위젯 메서드(`currentRange`/`commit`/`applyOp`/`coordOf`/`cellAt`/`focusCellAt`/`colCount`/`bodyRowCount`/`allCells`/`onCellKey`/`makeCell`/`makeColHandle`/`makeRowHandle`/`openMenu`/`closeMenu`) 정의·호출 일치.

**알려진 한계(스펙 명시):** 인라인 코드 내 비이스케이프 파이프, `plaintext-only` 미지원 구형 브라우저(폐쇄망 Chrome/Edge 가정), 셀 포커스 중 키보드 노트전환 시 <1s 편집 유실 가능(blur 미발생 희귀 케이스). 모두 스펙/QA에서 수용.
