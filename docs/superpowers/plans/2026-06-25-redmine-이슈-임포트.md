# Redmine 이슈 임포트(클리퍼) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** server 모드에서 사용자가 본인 Redmine API 키로 사내 Redmine 이슈를 work-note 안에서 검색·열람하고, 분할 작업창에서 메타/본문/댓글을 골라 현재 노트 커서 위치에 마크다운으로 삽입한다.

**Architecture:** 백엔드가 사용자별 키(AES at-rest)로 Redmine REST를 서버-투-서버 프록시(`java.net.http.HttpClient`). 프런트는 `/api/redmine/*` 프록시만 호출하고, 도킹형 분할 패널(좌 Redmine 뷰어 / 우 실제 에디터)에서 `[삽입]` → 기존 `cm.insertAtCursor`로 마크다운 삽입. 동기화/갱신 없음(클리퍼).

**Tech Stack:** Java 21 + Spring Boot 3.5 + MyBatis + Flyway + SQLite (backend) · Vite 6 + TS + React 18(JSX 미사용) + CodeMirror 6 (frontend) · 신규 런타임 의존성 0.

## Global Constraints

- **server 모드 한정.** local(무인증) 모드에서는 전 기능 비활성(엔드포인트 404, 툴바 버튼 미노출).
- **JSX 미사용.** 모든 컴포넌트는 `const h = React.createElement` 관례. `.tsx`지만 JSX 문법 없음.
- **신규 런타임 의존성 0.** Redmine REST = JDK `java.net.http.HttpClient`, JSON = 기존 Jackson `ObjectMapper` 빈, 암호화 = 기존 `com.worknote.auth.totp.SecretCipher`(AES-256-GCM, `WORKNOTE_2FA_KEY` 공용).
- **마이그레이션 번호 = V11.** 커밋된 최신은 V9, 다른 작업 브랜치가 V10(`V10__rename_operator_role.sql`)을 점유 중. V11 사용. 머지 시 충돌 재확인.
- **항상 호출자 본인 키로 Redmine 호출.** 단일 서비스 계정 키 금지(Redmine ACL 우회 방지).
- **평문 키 절대 미응답.** 상태(present/login/lastVerified)만 노출.
- **테스트 관례.** 백엔드 JUnit + 명명 메모리 DB 격리(`jdbc:sqlite:file:memdb-<name>?mode=memory&cache=shared`). 프런트는 React 렌더/e2e 없음 → 결정 로직을 순수 함수로 추출해 vitest, 실화면은 `/qa` browse.
- **동시 편집 주의.** App.tsx·components/ProfileModal.tsx·admin/screens/* 등 공유 파일에 다른 터미널의 미커밋 변경이 있을 수 있음. 해당 파일 편집 전 최신 상태 확인, `git add -A` 금지, 본인 파일만 스테이징.
- **본문 포맷 = Markdown/CommonMark.** Textile 변환 안 함. 본문/댓글은 거의 그대로 삽입.

## File Structure

**Backend (신규)**
- `backend/src/main/resources/db/migration/sqlite/V11__redmine_token.sql` — 테이블 + 설정 seed
- `com/worknote/redmine/RedmineTokenRow.java` — 토큰 행 record
- `com/worknote/redmine/RedmineTokenMapper.java` + `resources/mappers/RedmineTokenMapper.xml` — CRUD
- `com/worknote/redmine/RedmineDtos.java` — IssueSummary/IssueDetail/Comment/Query record
- `com/worknote/redmine/RedmineJson.java` — JSON→DTO 순수 파서(테스트 대상)
- `com/worknote/redmine/RedmineException.java` — Auth/NotFound/Upstream 구분
- `com/worknote/redmine/RedmineClient.java` — HttpClient 트랜스포트
- `com/worknote/redmine/RedmineTokenService.java` — 키 저장/검증/복호화
- `com/worknote/redmine/RedmineService.java` — 프록시(검색/상세) + 예외 매핑 + 감사
- `com/worknote/redmine/MeRedmineController.java` — `/api/me/redmine/*`
- `com/worknote/redmine/RedmineController.java` — `/api/redmine/*`

**Backend (수정)**
- `com/worknote/setting/SettingService.java` — redmine 설정 accessor
- `com/worknote/admin/AdminSettingController.java` — `/api/admin/settings/redmine` GET/PUT
- `com/worknote/auth/dto/MeResponse.java` + `com/worknote/auth/AuthController.java` — `redmine` 필드

**Frontend (신규)**
- `frontend/src/api/redmine.ts` — RedmineApi + 타입
- `frontend/src/editor/redmineMarkdown.ts` — 삽입 마크다운 생성 순수 함수(테스트 대상) + `redmineMarkdown.test.ts`
- `frontend/src/components/redmineSplit.ts` — `splitDirection(width)` 순수 함수 + `redmineSplit.test.ts`
- `frontend/src/components/RedmineImportPanel.tsx` — 도킹 분할 패널
- `frontend/src/account/RedmineTab.tsx` — 프로필 키 등록 섹션
- `frontend/src/admin/screens/Redmine.tsx` — 관리자 설정 화면

**Frontend (수정)**
- `frontend/src/api/auth.ts` — `Me.redmine` 필드
- `frontend/src/admin/api.ts` — `getRedmineConfig`/`setRedmineConfig`
- `frontend/src/admin/mappers.ts` — `redmineStatusLabel`
- `frontend/src/components/Editor.tsx` — `ToolbarHandlers.redmine` + registerToolbar
- `frontend/src/App.tsx` — 툴바 버튼(게이트) + 패널 토글/레이아웃 + 에디터 view 연결
- `frontend/src/components/ProfileModal.tsx` — RedmineTab 섹션 포함
- `frontend/src/styles/app.css` — 분할 패널/이슈 목록 스타일

---

## Task 1: Flyway V11 — user_redmine_token 테이블 + 매퍼

**Files:**
- Create: `backend/src/main/resources/db/migration/sqlite/V11__redmine_token.sql`
- Create: `backend/src/main/java/com/worknote/redmine/RedmineTokenRow.java`
- Create: `backend/src/main/java/com/worknote/redmine/RedmineTokenMapper.java`
- Create: `backend/src/main/resources/mappers/RedmineTokenMapper.xml`
- Test: `backend/src/test/java/com/worknote/redmine/RedmineTokenMapperTest.java`

**Interfaces:**
- Produces: `RedmineTokenRow(String userId, String tokenEnc, String redmineLogin, String lastVerifiedAt, String createdAt)`; `RedmineTokenMapper.upsert(RedmineTokenRow)`, `.find(String userId): RedmineTokenRow`, `.delete(String userId)`.

- [ ] **Step 1: Write the migration**

`V11__redmine_token.sql`:
```sql
-- 사용자별 Redmine API 키 (user_totp처럼 분리 테이블, 시드와 동일 AES 보관)
CREATE TABLE user_redmine_token (
  user_id          TEXT PRIMARY KEY REFERENCES app_user(id) ON DELETE CASCADE,
  token_enc        TEXT NOT NULL,            -- AES-256-GCM(base64(nonce||ct||tag)) 암호화된 Redmine API 키
  redmine_login    TEXT,                     -- GET /users/current 가 반환한 login (표시/진단용)
  last_verified_at TEXT,                     -- ISO_LOCAL_DATE_TIME, 마지막 검증 성공 시각
  created_at       TEXT NOT NULL
);

-- 관리자 설정(미설정이면 기능 비활성)
INSERT INTO app_setting (key, value) VALUES ('redmine.enabled', '0');
INSERT INTO app_setting (key, value) VALUES ('redmine.base_url', '');
```

- [ ] **Step 2: Write RedmineTokenRow record**

```java
package com.worknote.redmine;

public record RedmineTokenRow(
    String userId,
    String tokenEnc,
    String redmineLogin,
    String lastVerifiedAt,
    String createdAt
) {}
```

- [ ] **Step 3: Write the failing mapper test**

`RedmineTokenMapperTest.java` (명명 DB 격리 관례 — `Me2faApiTest` 참조):
```java
package com.worknote.redmine;

import static org.assertj.core.api.Assertions.assertThat;
import com.worknote.auth.UserRow;
import com.worknote.user.UserMapper;
import org.junit.jupiter.api.*;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.jdbc.core.JdbcTemplate;

@SpringBootTest(properties = {
    "spring.datasource.url=jdbc:sqlite:file:memdb-redminetoken?mode=memory&cache=shared",
    "worknote.mode=server",
    "worknote.admin-password=x",
    "worknote.totp.key=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA="
})
class RedmineTokenMapperTest {
    @Autowired RedmineTokenMapper mapper;
    @Autowired UserMapper users;
    @Autowired JdbcTemplate jdbc;

    @BeforeEach void clean() {
        jdbc.update("DELETE FROM user_redmine_token");
        jdbc.update("DELETE FROM app_user");
        users.insert(new UserRow("u1", "10001", "a@corp.local", "홍", "operator", "active", null));
    }

    @Test void upsert_find_delete_roundtrip() {
        mapper.upsert(new RedmineTokenRow("u1", "ENC", "jdoe", "2026-06-25T10:00:00", "2026-06-25T10:00:00"));
        RedmineTokenRow r = mapper.find("u1");
        assertThat(r).isNotNull();
        assertThat(r.tokenEnc()).isEqualTo("ENC");
        assertThat(r.redmineLogin()).isEqualTo("jdoe");

        mapper.upsert(new RedmineTokenRow("u1", "ENC2", "jdoe2", "2026-06-25T11:00:00", "2026-06-25T10:00:00"));
        assertThat(mapper.find("u1").tokenEnc()).isEqualTo("ENC2");

        mapper.delete("u1");
        assertThat(mapper.find("u1")).isNull();
    }
}
```
> `UserRow` 생성자 인자 순서는 실제 `com.worknote.auth.UserRow` 정의를 확인해 맞춘다(조사 시 `(id, emp, email, name, roleId, status, ?)` 형태). 불일치 시 실제 시그니처로 교정.

- [ ] **Step 4: Run test to verify it fails**

Run: `cd backend && ./gradlew test --tests "com.worknote.redmine.RedmineTokenMapperTest"`
Expected: FAIL (RedmineTokenMapper 빈 없음 / 컴파일 에러)

- [ ] **Step 5: Write mapper interface + XML**

`RedmineTokenMapper.java`:
```java
package com.worknote.redmine;

import org.apache.ibatis.annotations.Mapper;
import org.apache.ibatis.annotations.Param;

@Mapper
public interface RedmineTokenMapper {
    void upsert(RedmineTokenRow row);
    RedmineTokenRow find(@Param("userId") String userId);
    void delete(@Param("userId") String userId);
}
```

`RedmineTokenMapper.xml` (TotpMapper.xml 패턴, SQLite upsert):
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.worknote.redmine.RedmineTokenMapper">
  <insert id="upsert">
    INSERT INTO user_redmine_token (user_id, token_enc, redmine_login, last_verified_at, created_at)
    VALUES (#{userId}, #{tokenEnc}, #{redmineLogin}, #{lastVerifiedAt}, #{createdAt})
    ON CONFLICT(user_id) DO UPDATE SET
      token_enc = excluded.token_enc,
      redmine_login = excluded.redmine_login,
      last_verified_at = excluded.last_verified_at
  </insert>
  <select id="find" resultType="com.worknote.redmine.RedmineTokenRow">
    SELECT user_id AS userId, token_enc AS tokenEnc, redmine_login AS redmineLogin,
           last_verified_at AS lastVerifiedAt, created_at AS createdAt
    FROM user_redmine_token WHERE user_id = #{userId}
  </select>
  <delete id="delete">DELETE FROM user_redmine_token WHERE user_id = #{userId}</delete>
</mapper>
```

- [ ] **Step 6: Run test to verify it passes**

Run: `cd backend && ./gradlew test --tests "com.worknote.redmine.RedmineTokenMapperTest"`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add backend/src/main/resources/db/migration/sqlite/V11__redmine_token.sql \
        backend/src/main/java/com/worknote/redmine/RedmineTokenRow.java \
        backend/src/main/java/com/worknote/redmine/RedmineTokenMapper.java \
        backend/src/main/resources/mappers/RedmineTokenMapper.xml \
        backend/src/test/java/com/worknote/redmine/RedmineTokenMapperTest.java
git commit -m "feat(redmine): V11 user_redmine_token 테이블 + 매퍼"
```

---

## Task 2: SettingService Redmine 설정 + 관리자 엔드포인트

**Files:**
- Modify: `backend/src/main/java/com/worknote/setting/SettingService.java`
- Modify: `backend/src/main/java/com/worknote/admin/AdminSettingController.java`
- Test: `backend/src/test/java/com/worknote/redmine/RedmineSettingTest.java`

**Interfaces:**
- Produces: `SettingService.redmineEnabled(): boolean`, `.redmineBaseUrl(): String`, `.setRedmine(boolean enabled, String baseUrl)`. `GET/PUT /api/admin/settings/redmine`.

- [ ] **Step 1: Write the failing test**

```java
package com.worknote.redmine;

import static org.assertj.core.api.Assertions.assertThat;
import com.worknote.setting.SettingService;
import org.junit.jupiter.api.*;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest(properties = {
    "spring.datasource.url=jdbc:sqlite:file:memdb-redminesetting?mode=memory&cache=shared",
    "worknote.mode=server", "worknote.admin-password=x",
    "worknote.totp.key=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA="
})
class RedmineSettingTest {
    @Autowired SettingService settings;

    @Test void defaults_then_roundtrip() {
        assertThat(settings.redmineEnabled()).isFalse();
        assertThat(settings.redmineBaseUrl()).isEmpty();
        settings.setRedmine(true, "http://redmine.intra");
        assertThat(settings.redmineEnabled()).isTrue();
        assertThat(settings.redmineBaseUrl()).isEqualTo("http://redmine.intra");
    }
}
```

- [ ] **Step 2: Run to verify it fails**

Run: `cd backend && ./gradlew test --tests "com.worknote.redmine.RedmineSettingTest"`
Expected: FAIL (메서드 없음)

- [ ] **Step 3: Add SettingService accessors**

`SettingService.java` (graceDays/setUploadPolicy 패턴):
```java
public boolean redmineEnabled() {
    return "1".equals(mapper.get("redmine.enabled"));
}

public String redmineBaseUrl() {
    String v = mapper.get("redmine.base_url");
    return v == null ? "" : v.trim();
}

@Transactional
public void setRedmine(boolean enabled, String baseUrl) {
    mapper.put("redmine.enabled", enabled ? "1" : "0");
    mapper.put("redmine.base_url", baseUrl == null ? "" : baseUrl.trim());
}
```

- [ ] **Step 4: Run to verify it passes**

Run: `cd backend && ./gradlew test --tests "com.worknote.redmine.RedmineSettingTest"`
Expected: PASS

- [ ] **Step 5: Add admin endpoint**

`AdminSettingController.java` (upload 엔드포인트 패턴 — `guard.requireAdmin`, `audit.log` 정확한 호출은 기존 메서드 시그니처에 맞춤):
```java
public record RedmineConfig(boolean enabled, String baseUrl) {}

@GetMapping("/redmine")
public RedmineConfig getRedmine(HttpServletRequest http) {
    guard.requireAdmin(http);
    return new RedmineConfig(settings.redmineEnabled(), settings.redmineBaseUrl());
}

@PutMapping("/redmine")
public RedmineConfig setRedmine(@RequestBody RedmineConfig cfg, HttpServletRequest http) {
    UserRow u = guard.requireAdmin(http);
    settings.setRedmine(cfg.enabled(), cfg.baseUrl());
    audit.log(u, "settings.redmine", "enabled=" + cfg.enabled() + " url=" + cfg.baseUrl(), http.getRemoteAddr());
    return new RedmineConfig(settings.redmineEnabled(), settings.redmineBaseUrl());
}
```
> `guard.requireAdmin`의 실제 시그니처(리턴 타입·존재 여부)를 기존 컨트롤러에서 확인해 맞춘다. 다른 패턴이면 기존 admin 가드 관례 그대로 사용.

- [ ] **Step 6: Add admin endpoint test (MockMvc)**

`RedmineSettingTest`에 admin PUT/GET 흐름 추가(`AdminSettingControllerTest` 또는 기존 admin 테스트 셋업 패턴 재사용 — 관리자 세션 로그인 후 PUT → GET 반영 확인).

- [ ] **Step 7: Run + Commit**

Run: `cd backend && ./gradlew test --tests "com.worknote.redmine.RedmineSettingTest"`
```bash
git add backend/src/main/java/com/worknote/setting/SettingService.java \
        backend/src/main/java/com/worknote/admin/AdminSettingController.java \
        backend/src/test/java/com/worknote/redmine/RedmineSettingTest.java
git commit -m "feat(redmine): 관리자 redmine.enabled/base_url 설정 API"
```

---

## Task 3: RedmineClient — JSON 파서(순수) + HttpClient 트랜스포트

**Files:**
- Create: `backend/src/main/java/com/worknote/redmine/RedmineDtos.java`
- Create: `backend/src/main/java/com/worknote/redmine/RedmineException.java`
- Create: `backend/src/main/java/com/worknote/redmine/RedmineJson.java`
- Create: `backend/src/main/java/com/worknote/redmine/RedmineClient.java`
- Test: `backend/src/test/java/com/worknote/redmine/RedmineJsonTest.java`

**Interfaces:**
- Produces: DTOs `RedmineIssueSummary`, `RedmineComment`, `RedmineIssueDetail`, `RedmineQuery`; `RedmineJson.parseIssueList(JsonNode): List<RedmineIssueSummary>`, `.parseIssueDetail(JsonNode): RedmineIssueDetail`, `.parseCurrentLogin(JsonNode): String`; `RedmineClient.fetchCurrentLogin(base, token)`, `.listIssues(base, token, RedmineQuery)`, `.getIssue(base, token, long id)`. Exceptions `RedmineException.Auth/NotFound/Upstream`.

- [ ] **Step 1: Write DTOs**

`RedmineDtos.java`:
```java
package com.worknote.redmine;

import java.util.List;

public final class RedmineDtos {
    public record RedmineIssueSummary(
        long id, String subject, String statusName, String assignedToName,
        String projectName, String updatedOn) {}

    public record RedmineComment(String userName, String createdOn, String notes) {}

    public record RedmineIssueDetail(
        long id, String subject, String description, String statusName,
        String assignedToName, String projectName, String priorityName,
        String dueDate, String updatedOn, List<RedmineComment> comments) {}

    public record RedmineQuery(
        String query, boolean assignedToMe, String statusId,
        String projectId, int offset, int limit) {}

    private RedmineDtos() {}
}
```

- [ ] **Step 2: Write exceptions**

`RedmineException.java`:
```java
package com.worknote.redmine;

public sealed class RedmineException extends RuntimeException
    permits RedmineException.Auth, RedmineException.NotFound, RedmineException.Upstream {
    RedmineException(String m) { super(m); }
    public static final class Auth extends RedmineException { public Auth(String m){super(m);} }       // 401/403 키 무효
    public static final class NotFound extends RedmineException { public NotFound(String m){super(m);} } // 404 없음/권한없음
    public static final class Upstream extends RedmineException { public Upstream(String m){super(m);} } // 5xx/timeout/파싱
}
```

- [ ] **Step 3: Write the failing parser test (fixtures)**

`RedmineJsonTest.java` — 순수 파서를 Redmine REST 표준 JSON 픽스처로 검증:
```java
package com.worknote.redmine;

import static org.assertj.core.api.Assertions.assertThat;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.worknote.redmine.RedmineDtos.*;
import java.util.List;
import org.junit.jupiter.api.Test;

class RedmineJsonTest {
    final ObjectMapper om = new ObjectMapper();

    @Test void parseIssueList_maps_fields() throws Exception {
        String json = """
        {"issues":[
          {"id":1234,"subject":"결제 타임아웃","status":{"name":"In Progress"},
           "assigned_to":{"name":"김OO"},"project":{"name":"결제"},"updated_on":"2026-06-20T09:00:00Z"}
        ],"total_count":1}""";
        List<RedmineIssueSummary> r = RedmineJson.parseIssueList(om.readTree(json));
        assertThat(r).hasSize(1);
        assertThat(r.get(0).id()).isEqualTo(1234);
        assertThat(r.get(0).statusName()).isEqualTo("In Progress");
        assertThat(r.get(0).assignedToName()).isEqualTo("김OO");
    }

    @Test void parseIssueList_handles_missing_assignee() throws Exception {
        String json = """
        {"issues":[{"id":1,"subject":"s","status":{"name":"New"},"project":{"name":"P"},"updated_on":"x"}]}""";
        List<RedmineIssueSummary> r = RedmineJson.parseIssueList(om.readTree(json));
        assertThat(r.get(0).assignedToName()).isNull();
    }

    @Test void parseIssueDetail_filters_empty_journals() throws Exception {
        String json = """
        {"issue":{"id":7,"subject":"버그","description":"본문","status":{"name":"New"},
          "priority":{"name":"높음"},"due_date":"2026-07-01","updated_on":"x",
          "project":{"name":"P"},"assigned_to":{"name":"홍"},
          "journals":[
            {"user":{"name":"홍"},"created_on":"2026-06-20T10:00:00Z","notes":"댓글1"},
            {"user":{"name":"이"},"created_on":"2026-06-20T11:00:00Z","notes":""}
          ]}}""";
        RedmineIssueDetail d = RedmineJson.parseIssueDetail(om.readTree(json));
        assertThat(d.description()).isEqualTo("본문");
        assertThat(d.priorityName()).isEqualTo("높음");
        assertThat(d.comments()).hasSize(1);                 // notes 빈 journal 제외
        assertThat(d.comments().get(0).notes()).isEqualTo("댓글1");
    }

    @Test void parseCurrentLogin_reads_user_login() throws Exception {
        String json = """{"user":{"id":3,"login":"jdoe","firstname":"J"}}""";
        assertThat(RedmineJson.parseCurrentLogin(om.readTree(json))).isEqualTo("jdoe");
    }
}
```

- [ ] **Step 4: Run to verify it fails**

Run: `cd backend && ./gradlew test --tests "com.worknote.redmine.RedmineJsonTest"`
Expected: FAIL (RedmineJson 없음)

- [ ] **Step 5: Write RedmineJson (pure)**

```java
package com.worknote.redmine;

import com.fasterxml.jackson.databind.JsonNode;
import com.worknote.redmine.RedmineDtos.*;
import java.util.ArrayList;
import java.util.List;

final class RedmineJson {
    private RedmineJson() {}

    static String parseCurrentLogin(JsonNode root) {
        return text(root.path("user"), "login");
    }

    static List<RedmineIssueSummary> parseIssueList(JsonNode root) {
        List<RedmineIssueSummary> out = new ArrayList<>();
        for (JsonNode n : root.path("issues")) {
            out.add(new RedmineIssueSummary(
                n.path("id").asLong(),
                text(n, "subject"),
                text(n.path("status"), "name"),
                nodeName(n, "assigned_to"),
                text(n.path("project"), "name"),
                text(n, "updated_on")));
        }
        return out;
    }

    static RedmineIssueDetail parseIssueDetail(JsonNode root) {
        JsonNode n = root.path("issue");
        List<RedmineComment> comments = new ArrayList<>();
        for (JsonNode j : n.path("journals")) {
            String notes = text(j, "notes");
            if (notes == null || notes.isBlank()) continue;   // 상태변경만 있는 journal 제외
            comments.add(new RedmineComment(nodeName(j, "user"), text(j, "created_on"), notes));
        }
        return new RedmineIssueDetail(
            n.path("id").asLong(), text(n, "subject"), text(n, "description"),
            text(n.path("status"), "name"), nodeName(n, "assigned_to"),
            text(n.path("project"), "name"), text(n.path("priority"), "name"),
            text(n, "due_date"), text(n, "updated_on"), comments);
    }

    private static String text(JsonNode n, String f) {
        JsonNode v = n.path(f);
        return v.isMissingNode() || v.isNull() ? null : v.asText();
    }
    private static String nodeName(JsonNode parent, String f) {
        JsonNode v = parent.path(f);
        return v.isMissingNode() || v.isNull() ? null : text(v, "name");
    }
}
```

- [ ] **Step 6: Run to verify it passes**

Run: `cd backend && ./gradlew test --tests "com.worknote.redmine.RedmineJsonTest"`
Expected: PASS

- [ ] **Step 7: Write RedmineClient (transport)**

```java
package com.worknote.redmine;

import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.worknote.redmine.RedmineDtos.*;
import java.net.URI;
import java.net.URLEncoder;
import java.net.http.*;
import java.nio.charset.StandardCharsets;
import java.time.Duration;
import java.util.List;
import org.springframework.stereotype.Component;

@Component
public class RedmineClient {
    private final ObjectMapper json;
    private final HttpClient http = HttpClient.newBuilder()
        .connectTimeout(Duration.ofSeconds(5)).build();

    public RedmineClient(ObjectMapper json) { this.json = json; }

    public String fetchCurrentLogin(String base, String token) {
        return RedmineJson.parseCurrentLogin(get(base, token, "/users/current.json"));
    }

    public List<RedmineIssueSummary> listIssues(String base, String token, RedmineQuery q) {
        StringBuilder p = new StringBuilder("/issues.json?status_id=")
            .append(q.statusId() == null || q.statusId().isBlank() ? "open" : enc(q.statusId()))
            .append("&offset=").append(Math.max(0, q.offset()))
            .append("&limit=").append(q.limit() <= 0 ? 25 : Math.min(100, q.limit()));
        if (q.assignedToMe()) p.append("&assigned_to_id=me");
        if (q.projectId() != null && !q.projectId().isBlank()) p.append("&project_id=").append(enc(q.projectId()));
        if (q.query() != null && !q.query().isBlank()) p.append("&subject=~").append(enc(q.query()));
        return RedmineJson.parseIssueList(get(base, token, p.toString()));
    }

    public RedmineIssueDetail getIssue(String base, String token, long id) {
        return RedmineJson.parseIssueDetail(get(base, token, "/issues/" + id + ".json?include=journals"));
    }

    private JsonNode get(String base, String token, String path) {
        if (base == null || base.isBlank()) throw new RedmineException.Upstream("base_url 미설정");
        String url = base.replaceAll("/+$", "") + path;
        try {
            HttpRequest req = HttpRequest.newBuilder(URI.create(url))
                .timeout(Duration.ofSeconds(10))
                .header("X-Redmine-API-Key", token)
                .header("Accept", "application/json")
                .GET().build();
            HttpResponse<String> res = http.send(req, HttpResponse.BodyHandlers.ofString(StandardCharsets.UTF_8));
            int sc = res.statusCode();
            if (sc == 401 || sc == 403) throw new RedmineException.Auth("redmine_token_invalid");
            if (sc == 404) throw new RedmineException.NotFound("redmine_not_found");
            if (sc >= 400) throw new RedmineException.Upstream("redmine_upstream_" + sc);
            return json.readTree(res.body());
        } catch (RedmineException e) {
            throw e;
        } catch (Exception e) {
            throw new RedmineException.Upstream("redmine_io: " + e.getClass().getSimpleName());
        }
    }

    private static String enc(String s) { return URLEncoder.encode(s, StandardCharsets.UTF_8); }
}
```
> 트랜스포트(`get`)는 실서버 의존이라 유닛 테스트 비대상. 로직 검증은 RedmineJson(순수)이 담당. 통합은 Task 5에서 service를 mock-client로 검증.

- [ ] **Step 8: Commit**

Run: `cd backend && ./gradlew test --tests "com.worknote.redmine.*"`
```bash
git add backend/src/main/java/com/worknote/redmine/RedmineDtos.java \
        backend/src/main/java/com/worknote/redmine/RedmineException.java \
        backend/src/main/java/com/worknote/redmine/RedmineJson.java \
        backend/src/main/java/com/worknote/redmine/RedmineClient.java \
        backend/src/test/java/com/worknote/redmine/RedmineJsonTest.java
git commit -m "feat(redmine): REST 클라이언트(순수 파서 + HttpClient 트랜스포트)"
```

---

## Task 4: RedmineTokenService + MeRedmineController (`/api/me/redmine`)

**Files:**
- Create: `backend/src/main/java/com/worknote/redmine/RedmineTokenService.java`
- Create: `backend/src/main/java/com/worknote/redmine/MeRedmineController.java`
- Test: `backend/src/test/java/com/worknote/redmine/RedmineTokenServiceTest.java`

**Interfaces:**
- Consumes: `RedmineTokenMapper`(T1), `SecretCipher`, `SettingService`(T2), `RedmineClient`(T3), `Clock`.
- Produces: `RedmineTokenService.setToken(UserRow, String token)`, `.tokenFor(String userId): String`(없으면 `RedmineException.Auth("redmine_token_missing")` 아님 — 아래 매핑 주의), `.hasToken(String): boolean`, `.delete(String)`, `.status(String): RedmineTokenRow`. `GET/PUT/DELETE /api/me/redmine[/token]`.

- [ ] **Step 1: Write the failing service test**

토큰 검증은 RedmineClient를 거치므로 테스트에서 `@MockBean RedmineClient`로 대체:
```java
package com.worknote.redmine;

import static org.assertj.core.api.Assertions.*;
import static org.mockito.Mockito.*;
import com.worknote.auth.UserRow;
import com.worknote.setting.SettingService;
import com.worknote.user.UserMapper;
import org.junit.jupiter.api.*;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.mock.mockito.MockBean;
import org.springframework.jdbc.core.JdbcTemplate;

@SpringBootTest(properties = {
    "spring.datasource.url=jdbc:sqlite:file:memdb-redminetoksvc?mode=memory&cache=shared",
    "worknote.mode=server", "worknote.admin-password=x",
    "worknote.totp.key=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA="
})
class RedmineTokenServiceTest {
    @Autowired RedmineTokenService svc;
    @Autowired SettingService settings;
    @Autowired UserMapper users;
    @Autowired JdbcTemplate jdbc;
    @MockBean RedmineClient client;

    UserRow u1;
    @BeforeEach void clean() {
        jdbc.update("DELETE FROM user_redmine_token");
        jdbc.update("DELETE FROM app_user");
        u1 = new UserRow("u1","10001","a@corp.local","홍","operator","active",null);
        users.insert(u1);
        settings.setRedmine(true, "http://redmine.intra");
    }

    @Test void setToken_validates_encrypts_and_stores_login() {
        when(client.fetchCurrentLogin(eq("http://redmine.intra"), eq("KEY"))).thenReturn("jdoe");
        svc.setToken(u1, "KEY");
        assertThat(svc.hasToken("u1")).isTrue();
        assertThat(svc.status("u1").redmineLogin()).isEqualTo("jdoe");
        assertThat(svc.tokenFor("u1")).isEqualTo("KEY");           // 복호화 라운드트립
        assertThat(svc.status("u1").tokenEnc()).isNotEqualTo("KEY"); // 평문 아님
    }

    @Test void setToken_invalid_key_throws_auth() {
        when(client.fetchCurrentLogin(anyString(), anyString()))
            .thenThrow(new RedmineException.Auth("redmine_token_invalid"));
        assertThatThrownBy(() -> svc.setToken(u1, "BAD"))
            .isInstanceOf(RedmineException.Auth.class);
        assertThat(svc.hasToken("u1")).isFalse();
    }

    @Test void setToken_disabled_throws() {
        settings.setRedmine(false, "");
        assertThatThrownBy(() -> svc.setToken(u1, "KEY")).isInstanceOf(RedmineException.NotFound.class);
    }
}
```

- [ ] **Step 2: Run to verify it fails**

Run: `cd backend && ./gradlew test --tests "com.worknote.redmine.RedmineTokenServiceTest"`
Expected: FAIL

- [ ] **Step 3: Write RedmineTokenService**

```java
package com.worknote.redmine;

import com.worknote.auth.UserRow;
import com.worknote.auth.totp.SecretCipher;
import com.worknote.setting.SettingService;
import com.worknote.vault.VaultException;
import java.time.Clock;
import java.time.LocalDateTime;
import java.time.format.DateTimeFormatter;
import org.springframework.stereotype.Service;

@Service
public class RedmineTokenService {
    private final RedmineTokenMapper mapper;
    private final SecretCipher cipher;
    private final SettingService settings;
    private final RedmineClient client;
    private final Clock clock;

    public RedmineTokenService(RedmineTokenMapper mapper, SecretCipher cipher,
                               SettingService settings, RedmineClient client, Clock clock) {
        this.mapper = mapper; this.cipher = cipher; this.settings = settings;
        this.client = client; this.clock = clock;
    }

    public void setToken(UserRow user, String token) {
        if (!settings.redmineEnabled()) throw new RedmineException.NotFound("redmine_disabled");
        if (token == null || token.isBlank()) throw VaultException.invalid("토큰을 입력하세요");
        if (!cipher.available()) throw VaultException.invalid("암호화 키(WORKNOTE_2FA_KEY) 미구성");
        String base = settings.redmineBaseUrl();
        String login = client.fetchCurrentLogin(base, token);   // 무효 시 RedmineException.Auth
        String now = LocalDateTime.now(clock).format(DateTimeFormatter.ISO_LOCAL_DATE_TIME);
        mapper.upsert(new RedmineTokenRow(user.id(), cipher.encrypt(token), login, now, now));
    }

    public boolean hasToken(String userId) { return mapper.find(userId) != null; }
    public RedmineTokenRow status(String userId) { return mapper.find(userId); }
    public void delete(String userId) { mapper.delete(userId); }

    public String tokenFor(String userId) {
        RedmineTokenRow r = mapper.find(userId);
        if (r == null) throw VaultException.conflict("redmine_token_missing");
        return cipher.decrypt(r.tokenEnc());
    }
}
```
> `VaultException` 정적 팩토리(`invalid`/`conflict`)의 실제 시그니처를 확인해 맞춘다.

- [ ] **Step 4: Run to verify it passes**

Run: `cd backend && ./gradlew test --tests "com.worknote.redmine.RedmineTokenServiceTest"`
Expected: PASS

- [ ] **Step 5: Write MeRedmineController**

```java
package com.worknote.redmine;

import com.worknote.audit.AuditService;
import com.worknote.auth.AuthFilter;
import com.worknote.auth.AuthException;
import com.worknote.auth.UserRow;
import jakarta.servlet.http.HttpServletRequest;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.*;

@RestController
@RequestMapping("/api/me/redmine")
public class MeRedmineController {
    private final RedmineTokenService tokens;
    private final com.worknote.setting.SettingService settings;
    private final AuditService audit;

    public MeRedmineController(RedmineTokenService tokens,
                              com.worknote.setting.SettingService settings, AuditService audit) {
        this.tokens = tokens; this.settings = settings; this.audit = audit;
    }

    public record RedmineMeStatus(boolean enabled, boolean tokenPresent,
                                  String redmineLogin, String lastVerifiedAt) {}
    public record TokenRequest(String token) {}

    private UserRow me(HttpServletRequest http) {
        UserRow u = (UserRow) http.getAttribute(AuthFilter.CURRENT_USER);
        if (u == null) throw AuthException.forbidden("로그인 상태에서만 가능합니다");
        return u;
    }

    @GetMapping
    public RedmineMeStatus status(HttpServletRequest http) {
        UserRow u = me(http);
        RedmineTokenRow r = tokens.status(u.id());
        return new RedmineMeStatus(settings.redmineEnabled(), r != null,
            r == null ? null : r.redmineLogin(), r == null ? null : r.lastVerifiedAt());
    }

    @PutMapping("/token")
    public RedmineMeStatus setToken(@RequestBody TokenRequest req, HttpServletRequest http) {
        UserRow u = me(http);
        tokens.setToken(u, req.token());
        audit.log(u, "redmine.token.set", null, http.getRemoteAddr());
        return status(http);
    }

    @DeleteMapping("/token")
    @ResponseStatus(HttpStatus.NO_CONTENT)
    public void deleteToken(HttpServletRequest http) {
        UserRow u = me(http);
        tokens.delete(u.id());
        audit.log(u, "redmine.token.delete", null, http.getRemoteAddr());
    }
}
```

- [ ] **Step 6: Map RedmineException in ApiExceptionHandler**

`com/worknote/...ApiExceptionHandler`(기존 위치)에 추가 — Auth→409(`redmine_token_invalid`), NotFound→404, Upstream→502. 본문은 기존 `{"error": msg}` 관례:
```java
@ExceptionHandler(RedmineException.Auth.class)
ResponseEntity<?> redmineAuth(RedmineException.Auth e) {
    return ResponseEntity.status(HttpStatus.CONFLICT).body(java.util.Map.of("error", e.getMessage()));
}
@ExceptionHandler(RedmineException.NotFound.class)
ResponseEntity<?> redmineNotFound(RedmineException.NotFound e) {
    return ResponseEntity.status(HttpStatus.NOT_FOUND).body(java.util.Map.of("error", e.getMessage()));
}
@ExceptionHandler(RedmineException.Upstream.class)
ResponseEntity<?> redmineUpstream(RedmineException.Upstream e) {
    return ResponseEntity.status(HttpStatus.BAD_GATEWAY).body(java.util.Map.of("error", e.getMessage()));
}
```

- [ ] **Step 7: Add controller MockMvc test + Commit**

`RedmineTokenServiceTest`(또는 신규 `MeRedmineApiTest`)에 PUT(검증성공·실패 409)·GET(tokenPresent)·DELETE(204) 흐름을 MockMvc로 추가(관리자/일반 세션 로그인 셋업은 기존 `Me2faApiTest` 패턴 재사용, `@MockBean RedmineClient`).

Run: `cd backend && ./gradlew test --tests "com.worknote.redmine.*"`
```bash
git add backend/src/main/java/com/worknote/redmine/RedmineTokenService.java \
        backend/src/main/java/com/worknote/redmine/MeRedmineController.java \
        backend/src/test/java/com/worknote/redmine/ \
        <ApiExceptionHandler 파일 경로>
git commit -m "feat(redmine): 본인 키 등록/검증 API + 예외 매핑"
```

---

## Task 5: RedmineService + RedmineController (`/api/redmine/issues`)

**Files:**
- Create: `backend/src/main/java/com/worknote/redmine/RedmineService.java`
- Create: `backend/src/main/java/com/worknote/redmine/RedmineController.java`
- Test: `backend/src/test/java/com/worknote/redmine/RedmineApiTest.java`

**Interfaces:**
- Consumes: `RedmineTokenService`(T4), `RedmineClient`(T3), `SettingService`(T2), `AuditService`.
- Produces: `GET /api/redmine/issues?query&assignedToMe&statusId&projectId&offset&limit` → `IssueSummary[]`; `GET /api/redmine/issues/{id}` → `IssueDetail`.

- [ ] **Step 1: Write the failing API test**

```java
package com.worknote.redmine;

import static org.mockito.Mockito.*;
import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.*;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.*;
// ... (셋업은 Me2faApiTest 패턴: server 모드, 로그인 세션, @MockBean RedmineClient)

@Test void issues_requires_token_409_when_missing() throws Exception {
    // 로그인 세션, enabled=true, 토큰 미등록
    mvc.perform(get("/api/redmine/issues").session(session))
       .andExpect(status().isConflict());
}

@Test void issues_returns_list_using_session_user_token() throws Exception {
    // 토큰 등록(client.fetchCurrentLogin mock) → search
    when(client.listIssues(anyString(), eq("KEY"), any()))
        .thenReturn(List.of(new RedmineDtos.RedmineIssueSummary(1, "s", "New", null, "P", "x")));
    mvc.perform(get("/api/redmine/issues?assignedToMe=true").session(session))
       .andExpect(status().isOk())
       .andExpect(jsonPath("$.issues[0].id").value(1));
}

@Test void issues_404_when_disabled() throws Exception {
    settings.setRedmine(false, "");
    mvc.perform(get("/api/redmine/issues").session(session)).andExpect(status().isNotFound());
}
```

- [ ] **Step 2: Run to verify it fails**

Run: `cd backend && ./gradlew test --tests "com.worknote.redmine.RedmineApiTest"`
Expected: FAIL

- [ ] **Step 3: Write RedmineService**

```java
package com.worknote.redmine;

import com.worknote.audit.AuditService;
import com.worknote.auth.UserRow;
import com.worknote.redmine.RedmineDtos.*;
import com.worknote.setting.SettingService;
import java.util.List;
import org.springframework.stereotype.Service;

@Service
public class RedmineService {
    private final RedmineTokenService tokens;
    private final RedmineClient client;
    private final SettingService settings;
    private final AuditService audit;

    public RedmineService(RedmineTokenService tokens, RedmineClient client,
                          SettingService settings, AuditService audit) {
        this.tokens = tokens; this.client = client; this.settings = settings; this.audit = audit;
    }

    private String base() {
        if (!settings.redmineEnabled()) throw new RedmineException.NotFound("redmine_disabled");
        String b = settings.redmineBaseUrl();
        if (b == null || b.isBlank()) throw new RedmineException.NotFound("redmine_disabled");
        return b;
    }

    public List<RedmineIssueSummary> search(UserRow user, RedmineQuery q) {
        return client.listIssues(base(), tokens.tokenFor(user.id()), q);
    }

    public RedmineIssueDetail detail(UserRow user, long id, String ip) {
        RedmineIssueDetail d = client.getIssue(base(), tokens.tokenFor(user.id()), id);
        audit.log(user, "redmine.import", "issue#" + id, ip);
        return d;
    }
}
```

- [ ] **Step 4: Write RedmineController**

```java
package com.worknote.redmine;

import com.worknote.auth.AuthFilter;
import com.worknote.auth.AuthException;
import com.worknote.auth.UserRow;
import com.worknote.redmine.RedmineDtos.*;
import jakarta.servlet.http.HttpServletRequest;
import java.util.List;
import org.springframework.web.bind.annotation.*;

@RestController
@RequestMapping("/api/redmine")
public class RedmineController {
    private final RedmineService service;
    public RedmineController(RedmineService service) { this.service = service; }

    public record IssueListResponse(List<RedmineIssueSummary> issues) {}

    private UserRow me(HttpServletRequest http) {
        UserRow u = (UserRow) http.getAttribute(AuthFilter.CURRENT_USER);
        if (u == null) throw AuthException.forbidden("로그인 상태에서만 가능합니다");
        return u;
    }

    @GetMapping("/issues")
    public IssueListResponse issues(
        @RequestParam(required = false) String query,
        @RequestParam(defaultValue = "false") boolean assignedToMe,
        @RequestParam(required = false) String statusId,
        @RequestParam(required = false) String projectId,
        @RequestParam(defaultValue = "0") int offset,
        @RequestParam(defaultValue = "25") int limit,
        HttpServletRequest http) {
        return new IssueListResponse(service.search(me(http),
            new RedmineQuery(query, assignedToMe, statusId, projectId, offset, limit)));
    }

    @GetMapping("/issues/{id}")
    public RedmineIssueDetail issue(@PathVariable long id, HttpServletRequest http) {
        return service.detail(me(http), id, http.getRemoteAddr());
    }
}
```

- [ ] **Step 5: Run to verify it passes**

Run: `cd backend && ./gradlew test --tests "com.worknote.redmine.RedmineApiTest"`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add backend/src/main/java/com/worknote/redmine/RedmineService.java \
        backend/src/main/java/com/worknote/redmine/RedmineController.java \
        backend/src/test/java/com/worknote/redmine/RedmineApiTest.java
git commit -m "feat(redmine): 이슈 검색/상세 프록시 API(본인 키·감사)"
```

---

## Task 6: MeResponse에 redmine 상태 추가

**Files:**
- Modify: `backend/src/main/java/com/worknote/auth/dto/MeResponse.java`
- Modify: `backend/src/main/java/com/worknote/auth/AuthController.java`
- Test: `backend/src/test/java/com/worknote/redmine/MeRedmineFieldTest.java`

**Interfaces:**
- Consumes: `RedmineTokenService.hasToken`(T4), `SettingService.redmineEnabled`(T2).
- Produces: `MeResponse.redmine: RedmineInfo(enabled, tokenPresent)` — 프런트 툴바 게이트.

- [ ] **Step 1: Write the failing test**

`GET /api/auth/me`(또는 login 응답)에 `redmine.enabled`/`redmine.tokenPresent`가 포함되는지 MockMvc로 검증(server 모드, 토큰 등록 전 false / 등록 후 true).

- [ ] **Step 2: Run to verify it fails**

Run: `cd backend && ./gradlew test --tests "com.worknote.redmine.MeRedmineFieldTest"`
Expected: FAIL

- [ ] **Step 3: Add field to MeResponse**

```java
public record MeResponse(
    String id, String emp, String name, String email,
    String roleId, Set<String> caps, TotpInfo totp, RedmineInfo redmine
) {
    public record TotpInfo(boolean enabled, boolean enforced, boolean graceExpired, boolean emailPresent) {}
    public record RedmineInfo(boolean enabled, boolean tokenPresent) {}
}
```

- [ ] **Step 4: Update AuthController.toMe + local fallback**

`toMe(...)` 끝의 `new MeResponse(...)`에 `new MeResponse.RedmineInfo(settings.redmineEnabled(), redmineTokenService.hasToken(user.id()))` 인자 추가. `RedmineTokenService`를 AuthController 생성자에 주입. local 모드 fallback(라인 ~230)의 직접 생성에도 `new MeResponse.RedmineInfo(false, false)` 추가.

- [ ] **Step 5: Run to verify it passes**

Run: `cd backend && ./gradlew test --tests "com.worknote.redmine.MeRedmineFieldTest"`
Expected: PASS

- [ ] **Step 6: Run full backend suite (회귀)**

Run: `cd backend && ./gradlew test`
Expected: PASS (MeResponse 인자 추가가 다른 생성처를 깨지 않는지 — toMe 단일 경로 + local fallback만 수정됐는지 확인)

- [ ] **Step 7: Commit**

```bash
git add backend/src/main/java/com/worknote/auth/dto/MeResponse.java \
        backend/src/main/java/com/worknote/auth/AuthController.java \
        backend/src/test/java/com/worknote/redmine/MeRedmineFieldTest.java
git commit -m "feat(redmine): me 응답에 redmine 상태(enabled/tokenPresent) 추가"
```

---

## Task 7: 프런트 API 모듈 + 타입

**Files:**
- Create: `frontend/src/api/redmine.ts`
- Modify: `frontend/src/api/auth.ts` (Me 인터페이스)
- Modify: `frontend/src/admin/api.ts` (관리자 설정)

**Interfaces:**
- Produces: `RedmineApi.status/setToken/deleteToken/search/get`, 타입 `RedmineStatus/RedmineIssueSummary/RedmineComment/RedmineIssueDetail`; `Me.redmine?`; `AdminApi.getRedmineConfig/setRedmineConfig`.

- [ ] **Step 1: Write api/redmine.ts**

```ts
import { req } from "./http";

export interface RedmineStatus { enabled: boolean; tokenPresent: boolean; redmineLogin: string | null; lastVerifiedAt: string | null; }
export interface RedmineIssueSummary { id: number; subject: string; statusName: string; assignedToName: string | null; projectName: string; updatedOn: string; }
export interface RedmineComment { userName: string; createdOn: string; notes: string; }
export interface RedmineIssueDetail extends RedmineIssueSummary { description: string; priorityName: string | null; dueDate: string | null; comments: RedmineComment[]; }

export interface RedmineSearchParams { query?: string; assignedToMe?: boolean; statusId?: string; projectId?: string; offset?: number; limit?: number; }

function qs(p: RedmineSearchParams): string {
  const s = new URLSearchParams();
  if (p.query) s.set("query", p.query);
  if (p.assignedToMe) s.set("assignedToMe", "true");
  if (p.statusId) s.set("statusId", p.statusId);
  if (p.projectId) s.set("projectId", p.projectId);
  if (p.offset) s.set("offset", String(p.offset));
  if (p.limit) s.set("limit", String(p.limit));
  return s.toString();
}

export const RedmineApi = {
  status: () => req<RedmineStatus>("/me/redmine"),
  setToken: (token: string) => req<RedmineStatus>("/me/redmine/token", { method: "PUT", body: JSON.stringify({ token }) }),
  deleteToken: () => req<void>("/me/redmine/token", { method: "DELETE" }),
  search: (p: RedmineSearchParams) => req<{ issues: RedmineIssueSummary[] }>(`/redmine/issues?${qs(p)}`),
  get: (id: number) => req<RedmineIssueDetail>(`/redmine/issues/${id}`),
};
```

- [ ] **Step 2: Extend Me interface (api/auth.ts)**

`Me`에 추가:
```ts
  redmine?: { enabled: boolean; tokenPresent: boolean };
```

- [ ] **Step 3: Add admin config (admin/api.ts)**

기존 `AdminApi` 객체에 추가(`getUploadPolicy` 패턴):
```ts
  getRedmineConfig: () => req<{ enabled: boolean; baseUrl: string }>("/admin/settings/redmine"),
  setRedmineConfig: (cfg: { enabled: boolean; baseUrl: string }) =>
    req<{ enabled: boolean; baseUrl: string }>("/admin/settings/redmine", { method: "PUT", body: JSON.stringify(cfg) }),
```
> `admin/api.ts`의 실제 `req` import 경로/객체명 확인해 맞춤.

- [ ] **Step 4: Type check + Commit**

Run: `cd frontend && pnpm tsc --noEmit`
Expected: 통과 (사용처 없어도 타입만 추가)
```bash
git add frontend/src/api/redmine.ts frontend/src/api/auth.ts frontend/src/admin/api.ts
git commit -m "feat(redmine): 프런트 API 모듈 + Me.redmine/관리자 설정 타입"
```

---

## Task 8: 삽입 마크다운 생성(순수 함수) + 테스트

**Files:**
- Create: `frontend/src/editor/redmineMarkdown.ts`
- Modify: `frontend/src/admin/mappers.ts` (`redmineStatusLabel`)
- Test: `frontend/src/editor/redmineMarkdown.test.ts`

**Interfaces:**
- Produces: `metaTableMd(d)`, `bodyMd(d)`, `commentMd(c)` — 삽입용 마크다운 문자열.

- [ ] **Step 1: Write the failing test**

```ts
import { describe, it, expect } from "vitest";
import { metaTableMd, bodyMd, commentMd } from "./redmineMarkdown";

const detail = {
  id: 1234, subject: "결제 타임아웃", description: "본문\n둘째줄",
  statusName: "In Progress", assignedToName: "김OO", projectName: "결제",
  priorityName: "높음", dueDate: "2026-07-01", updatedOn: "x", comments: [],
};

describe("metaTableMd", () => {
  it("GFM 표 + 출처 인용을 만든다", () => {
    const md = metaTableMd(detail);
    expect(md).toContain("| 상태 | 담당 | 우선순위 | 마감 |");
    expect(md).toContain("| In Progress | 김OO | 높음 | 2026-07-01 |");
    expect(md).toContain("> 🔗 redmine #1234");
  });
  it("담당/마감 없으면 - 로 채운다", () => {
    const md = metaTableMd({ ...detail, assignedToName: null, dueDate: null });
    expect(md).toContain("| In Progress | - | 높음 | - |");
  });
});

describe("bodyMd", () => {
  it("본문을 그대로 트림해 반환한다", () => {
    expect(bodyMd(detail)).toBe("본문\n둘째줄\n");
  });
});

describe("commentMd", () => {
  it("작성자/날짜 헤더 + 인용 본문", () => {
    const md = commentMd({ userName: "홍", createdOn: "2026-06-20T10:00:00Z", notes: "리뷰\n해주세요" });
    expect(md).toContain("> **홍**");
    expect(md).toContain("2026-06-20");
    expect(md).toContain("> 리뷰");
    expect(md).toContain("> 해주세요");
  });
});
```

- [ ] **Step 2: Run to verify it fails**

Run: `cd frontend && pnpm test redmineMarkdown`
Expected: FAIL

- [ ] **Step 3: Write redmineMarkdown.ts**

```ts
import type { RedmineIssueDetail, RedmineComment } from "../api/redmine";

const dash = (s: string | null | undefined) => (s && s.trim() ? s : "-");
const day = (iso: string) => (iso ? iso.slice(0, 10) : "");

export function metaTableMd(d: RedmineIssueDetail): string {
  return (
    `| 상태 | 담당 | 우선순위 | 마감 |\n` +
    `|---|---|---|---|\n` +
    `| ${dash(d.statusName)} | ${dash(d.assignedToName)} | ${dash(d.priorityName)} | ${dash(d.dueDate)} |\n` +
    `\n> 🔗 redmine #${d.id} · ${d.subject}\n`
  );
}

export function bodyMd(d: RedmineIssueDetail): string {
  return `${(d.description ?? "").trim()}\n`;
}

export function commentMd(c: RedmineComment): string {
  const quoted = (c.notes ?? "").split("\n").map((l) => `> ${l}`).join("\n");
  return `> **${c.userName ?? "?"}** ${day(c.createdOn)}\n>\n${quoted}\n`;
}
```

- [ ] **Step 4: Add redmineStatusLabel to mappers.ts**

```ts
const REDMINE_STATUS: Record<string, string> = {
  "New": "신규", "In Progress": "진행 중", "Resolved": "해결", "Closed": "완료", "Rejected": "거부",
};
export function redmineStatusLabel(s: string): string { return REDMINE_STATUS[s] ?? s; }
```

- [ ] **Step 5: Run to verify it passes**

Run: `cd frontend && pnpm test redmineMarkdown`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add frontend/src/editor/redmineMarkdown.ts frontend/src/editor/redmineMarkdown.test.ts frontend/src/admin/mappers.ts
git commit -m "feat(redmine): 삽입 마크다운 생성 순수 함수 + 상태 라벨"
```

---

## Task 9: 분할 방향 결정(순수 함수) + 패널 CSS

**Files:**
- Create: `frontend/src/components/redmineSplit.ts`
- Modify: `frontend/src/styles/app.css`
- Test: `frontend/src/components/redmineSplit.test.ts`

**Interfaces:**
- Produces: `splitDirection(width: number): "row" | "column"`.

- [ ] **Step 1: Write the failing test**

```ts
import { describe, it, expect } from "vitest";
import { splitDirection, SPLIT_THRESHOLD } from "./redmineSplit";

describe("splitDirection", () => {
  it("임계폭 이상이면 좌우(row)", () => {
    expect(splitDirection(SPLIT_THRESHOLD)).toBe("row");
    expect(splitDirection(SPLIT_THRESHOLD + 1)).toBe("row");
  });
  it("임계폭 미만이면 상하(column)", () => {
    expect(splitDirection(SPLIT_THRESHOLD - 1)).toBe("column");
    expect(splitDirection(320)).toBe("column");
  });
});
```

- [ ] **Step 2: Run to verify it fails**

Run: `cd frontend && pnpm test redmineSplit`
Expected: FAIL

- [ ] **Step 3: Write redmineSplit.ts**

```ts
export const SPLIT_THRESHOLD = 900;
export function splitDirection(width: number): "row" | "column" {
  return width >= SPLIT_THRESHOLD ? "row" : "column";
}
```

- [ ] **Step 4: Add CSS (styles/app.css)**

기존 CSS 변수 사용. 도킹 분할 컨테이너:
```css
.rm-split { display: flex; min-height: 0; flex: 1; }
.rm-split.row { flex-direction: row; }
.rm-split.column { flex-direction: column; }
.rm-panel { display: flex; flex-direction: column; min-width: 0; min-height: 0;
  border-right: 1px solid var(--border); background: var(--bg); }
.rm-split.column .rm-panel { border-right: none; border-bottom: 1px solid var(--border); }
.rm-panel-head { padding: 10px 14px; border-bottom: 1px solid var(--border); display: flex; gap: 8px; align-items: center; }
.rm-issue-list { overflow: auto; flex: 1; }
.rm-issue { padding: 8px 14px; border-bottom: 1px solid var(--border); cursor: pointer; }
.rm-issue:hover { background: var(--bg-active); }
.rm-issue .rm-id { color: var(--text-3); font-family: var(--font-mono); font-size: 12px; }
.rm-detail { overflow: auto; flex: 1; padding: 14px; }
.rm-block { border: 1px solid var(--border); border-radius: var(--radius); margin-bottom: 12px; }
.rm-block-head { display: flex; justify-content: space-between; align-items: center; padding: 6px 10px; border-bottom: 1px solid var(--border); }
.rm-insert-btn { font-size: 12px; }
```
> 실제 CSS 변수명(`--bg-active`, `--text-3`, `--border`, `--radius`, `--font-mono`)은 app.css에 존재 확인됨. 톤은 기존 패널과 일치시킴.

- [ ] **Step 5: Run + Commit**

Run: `cd frontend && pnpm test redmineSplit`
```bash
git add frontend/src/components/redmineSplit.ts frontend/src/components/redmineSplit.test.ts frontend/src/styles/app.css
git commit -m "feat(redmine): 분할 방향 순수 함수 + 패널 스타일"
```
> ⚠️ app.css는 다른 워커도 수정 중일 수 있음 — 편집 전 `git diff frontend/src/styles/app.css` 확인, 본인 추가분만 스테이징.

---

## Task 10: RedmineImportPanel 컴포넌트(도킹 분할)

**Files:**
- Create: `frontend/src/components/RedmineImportPanel.tsx`

**Interfaces:**
- Consumes: `RedmineApi`(T7), `metaTableMd/bodyMd/commentMd`(T8), `splitDirection`(T9), `redmineStatusLabel`(T8).
- Produces: `RedmineImportPanel({ onInsert, onClose })` — `onInsert(md: string)`를 호출(부모가 `cm.insertAtCursor(view, md)`로 연결).

- [ ] **Step 1: Implement (createElement 관례)**

핵심 동작:
- 좌측(또는 상단) head: 검색 input + "내 이슈" 토글 → `RedmineApi.search({ query, assignedToMe })` → 목록.
- 목록 항목 클릭 → `RedmineApi.get(id)` → 우측 상세.
- 상세: 메타/본문/댓글 블록마다 `[삽입]` → `onInsert(metaTableMd(d) | bodyMd(d) | commentMd(c))`.
- 분할 방향: `window.innerWidth` + resize 리스너 → `splitDirection`로 `row`/`column` 클래스.
- 에러 처리: `ApiError` 메시지 분기 — `redmine_token_missing`/`redmine_token_invalid` → "프로필에서 키 등록/갱신" 안내 토스트.

```ts
import React, { useState, useEffect, useCallback } from "react";
import { RedmineApi, type RedmineIssueSummary, type RedmineIssueDetail } from "../api/redmine";
import { ApiError } from "../api/http";
import { metaTableMd, bodyMd, commentMd } from "../editor/redmineMarkdown";
import { splitDirection } from "./redmineSplit";
import { redmineStatusLabel } from "../admin/mappers";
import { Icon } from "./Icon";

const h = React.createElement;

interface Props { onInsert: (md: string) => void; onClose: () => void; toast?: (m: string, i?: string) => void; }

export function RedmineImportPanel({ onInsert, onClose, toast }: Props) {
  const [dir, setDir] = useState(splitDirection(window.innerWidth));
  const [q, setQ] = useState("");
  const [mine, setMine] = useState(true);
  const [list, setList] = useState<RedmineIssueSummary[]>([]);
  const [detail, setDetail] = useState<RedmineIssueDetail | null>(null);
  const [busy, setBusy] = useState(false);

  useEffect(() => {
    const onResize = () => setDir(splitDirection(window.innerWidth));
    window.addEventListener("resize", onResize);
    return () => window.removeEventListener("resize", onResize);
  }, []);

  const fail = useCallback((e: unknown) => {
    const msg = e instanceof ApiError ? e.message : "오류";
    if (msg === "redmine_token_missing") toast?.("프로필에서 Redmine 키를 먼저 등록하세요");
    else if (msg === "redmine_token_invalid") toast?.("Redmine 키가 유효하지 않습니다. 프로필에서 갱신하세요");
    else toast?.(msg);
  }, [toast]);

  const search = useCallback(async () => {
    setBusy(true);
    try { setList((await RedmineApi.search({ query: q, assignedToMe: mine })).issues); }
    catch (e) { fail(e); } finally { setBusy(false); }
  }, [q, mine, fail]);

  useEffect(() => { void search(); /* 최초 1회: 내 이슈 */ }, []);  // eslint-disable-line

  const open = async (id: number) => {
    setBusy(true);
    try { setDetail(await RedmineApi.get(id)); } catch (e) { fail(e); } finally { setBusy(false); }
  };

  // ... h(...) 트리: rm-split{dir} > [좌 rm-panel(검색+목록), 우 rm-detail(블록+삽입 버튼)]
  // 블록 [삽입] onClick: () => onInsert(metaTableMd(detail)) 등
}
```

- [ ] **Step 2: Logic extraction check**

거동 로직(마크다운 생성·분할 방향·에러 메시지 분기)은 T8/T9 순수 함수 + 본 컴포넌트 내 단순 분기. 추가 추출이 필요한 순수 로직이 있으면 별 파일로 빼고 vitest 추가([[worknote-frontend-test-convention]]).

- [ ] **Step 3: Type check + Commit**

Run: `cd frontend && pnpm tsc --noEmit`
```bash
git add frontend/src/components/RedmineImportPanel.tsx
git commit -m "feat(redmine): 이슈 임포트 분할 패널 컴포넌트"
```

---

## Task 11: 툴바 버튼 + 패널 토글 + 에디터 view 연결

**Files:**
- Modify: `frontend/src/components/Editor.tsx` (`ToolbarHandlers.redmine`, registerToolbar, view 노출)
- Modify: `frontend/src/App.tsx` (TB_GROUPS 버튼 게이트, 패널 상태/레이아웃, onInsert→insertAtCursor)

**Interfaces:**
- Consumes: `RedmineImportPanel`(T10), `cm.insertAtCursor`(기존), `me.redmine`(T6/T7).

- [ ] **Step 1: Add toolbar handler**

`Editor.tsx`의 `ToolbarHandlers`에 `redmine: () => void` 추가, `registerToolbar({... redmine: () => onOpenRedmine?.() })`. 에디터 `EditorView`(viewRef)를 부모에 노출하는 콜백(`onView?(view)`) 또는 기존 노출 경로 활용 — 패널의 `onInsert`가 `cm.insertAtCursor(view, md)`를 호출할 수 있게.

- [ ] **Step 2: Wire in App.tsx**

- `TB_GROUPS` 마지막 그룹에 `{ k: "redmine", icon: "import", title: "Redmine 가져오기", fn: (h) => h.redmine() }` 추가.
- 버튼 노출 게이트: `storageMode === "http" && me?.redmine?.enabled` (키 미등록이면 클릭 시 "프로필에서 키 등록" 안내).
- 패널 open 상태(`useState`) — 열리면 에디터 영역을 `RedmineImportPanel`(좌) + Editor(우)로 분할 렌더, 닫으면 Editor 풀폭.
- `onInsert = (md) => { const v = editorViewRef.current; if (v) cm.insertAtCursor(v, md); }`.

> ⚠️ App.tsx는 다른 워커가 수정 중(라인 505 인근). 편집 전 `git diff frontend/src/App.tsx`로 충돌 영역 확인, **본인 추가 hunk만** `git add -p`로 스테이징.

- [ ] **Step 3: Icon 확인**

`import` 아이콘이 `Icon.tsx`에 있는지 확인. 없으면 적절한 기존 아이콘(예: `download`/`link`) 사용 또는 추가.

- [ ] **Step 4: Type check + 실화면 QA**

Run: `cd frontend && pnpm tsc --noEmit`
실화면: `/qa` 또는 dev(5173, `VITE_STORAGE=http`)에서 — 관리자가 redmine.enabled=on + base_url 설정 → 사용자가 프로필에서 키 등록 → 툴바 버튼 노출 → 패널 열기 → 검색/상세/삽입 → 분할 반응형(창 좁히면 상하) 확인.

- [ ] **Step 5: Commit**

```bash
git add -p frontend/src/App.tsx       # 본인 hunk만
git add frontend/src/components/Editor.tsx
git commit -m "feat(redmine): 툴바 진입 + 분할 패널 토글 + 커서 삽입 연결"
```

---

## Task 12: 프로필 Redmine 키 등록 섹션

**Files:**
- Create: `frontend/src/account/RedmineTab.tsx`
- Modify: `frontend/src/components/ProfileModal.tsx` (RedmineTab 포함)

**Interfaces:**
- Consumes: `RedmineApi.status/setToken/deleteToken`(T7).

- [ ] **Step 1: Implement RedmineTab (SecurityTab 패턴)**

상태머신 없이 단순: 마운트 시 `RedmineApi.status()` → enabled면 섹션 표시. 키 input + "저장"(`setToken` → 성공 시 `redmineLogin` 표시·토스트, 실패 시 ApiError 메시지) + 등록돼 있으면 "해제"(`deleteToken`). `enabled=false`면 "관리자가 Redmine 연동을 켜지 않았습니다" 안내.

```ts
import React, { useState, useEffect } from "react";
import { RedmineApi, type RedmineStatus } from "../api/redmine";
import { ApiError } from "../api/http";
const h = React.createElement;

export function RedmineTab({ toast }: { toast?: (m: string, i?: string) => void }) {
  const [st, setSt] = useState<RedmineStatus | null>(null);
  const [token, setToken] = useState("");
  const [busy, setBusy] = useState(false);
  useEffect(() => { void RedmineApi.status().then(setSt).catch(() => {}); }, []);
  // save/remove 핸들러 + h(...) 트리 (pf-sec 패턴)
}
```

- [ ] **Step 2: Include in ProfileModal**

`ProfileModal.tsx`에서 보안 탭 인접 위치에 `storageMode === "http"`일 때 `h(RedmineTab, { toast })` 포함.
> ⚠️ ProfileModal.tsx는 다른 워커 수정 중 — `git diff` 확인 후 본인 hunk만 스테이징.

- [ ] **Step 3: Type check + 실화면 QA + Commit**

Run: `cd frontend && pnpm tsc --noEmit`
실화면: 프로필 모달에서 키 등록/검증 실패(잘못된 키 → 409 메시지)/해제.
```bash
git add frontend/src/account/RedmineTab.tsx
git add -p frontend/src/components/ProfileModal.tsx
git commit -m "feat(redmine): 프로필 Redmine 키 등록/해제 섹션"
```

---

## Task 13: 관리자 Redmine 설정 화면

**Files:**
- Create: `frontend/src/admin/screens/Redmine.tsx`
- Modify: admin 네비/라우트 등록 위치(기존 `Uploads` 화면 등록 지점)

**Interfaces:**
- Consumes: `AdminApi.getRedmineConfig/setRedmineConfig`(T7).

- [ ] **Step 1: Implement Redmine.tsx (Uploads 패턴)**

`enabled` 체크박스 + `baseUrl` input + 저장(`setRedmineConfig` → 토스트). `SecHead` 재사용.

- [ ] **Step 2: Register in admin nav/routes**

기존 admin 화면 등록 지점(메뉴 목록 + 라우팅 switch)에 "Redmine 연동" 추가. `admin.security`(또는 적절한 admin cap) 가드 일관.
> ⚠️ admin/common.tsx·admin/screens/Users.tsx 등 다른 워커 수정 파일과 겹치면 본인 hunk만 스테이징.

- [ ] **Step 3: Type check + 실화면 QA + Commit**

Run: `cd frontend && pnpm tsc --noEmit`
실화면: 관리자 화면에서 enabled 토글 + base_url 저장 → 새로고침 반영, 일반 사용자 프로필에 섹션 노출 확인.
```bash
git add frontend/src/admin/screens/Redmine.tsx
git add -p <admin 네비/라우트 파일>
git commit -m "feat(redmine): 관리자 Redmine 연동 설정 화면"
```

---

## Task 14: 통합 회귀 + 문서

- [ ] **Step 1: Full backend suite**

Run: `cd backend && ./gradlew test`
Expected: PASS (전 테스트)

- [ ] **Step 2: Full frontend suite + build**

Run: `cd frontend && pnpm test && pnpm tsc --noEmit && pnpm build`
Expected: PASS

- [ ] **Step 3: e2e 실화면 QA (server 모드)**

`/qa` 또는 수동: 관리자 설정 on → 사용자 키 등록 → 이슈 검색(내 이슈/검색어) → 상세 → 메타/본문/댓글 삽입 → 노트 저장 → 분할 반응형(좌우↔상하) → 키 무효/미등록/disabled 에러 경로.

- [ ] **Step 4: CLAUDE.md 업데이트(선택)**

backend/frontend 구조 설명에 Redmine 임포트 한 줄 추가.

```bash
git add CLAUDE.md
git commit -m "docs(redmine): 이슈 임포트 기능 반영"
```

---

## 구현 순서 / 의존성 요약

```
BE: T1(테이블) → T2(설정) → T3(클라이언트) → T4(키서비스+API, T1·T3 필요)
                                            → T5(프록시 API, T2·T3·T4) → T6(me 필드, T2·T4)
FE: T7(API/타입) → T8(MD 순수함수) · T9(분할 순수함수) → T10(패널, T7·T8·T9)
                 → T11(툴바/연결, T10·T6) → T12(프로필 키) → T13(관리자 설정) → T14(회귀)
```

각 태스크는 독립 테스트 가능 단위. BE는 명명 DB 격리 + `@MockBean RedmineClient`로 외부 의존 차단. FE는 순수 함수 vitest + UI는 `/qa`. **공유 파일(App.tsx·ProfileModal.tsx·admin/*)은 다른 워커 변경과 충돌 주의 — 본인 hunk만 스테이징.**
