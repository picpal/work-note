# 위키링크·지식 그래프 (② 코어 연결) 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `[[id:X|label]]` 위키링크 입력·자동완성, 에디터/공유 렌더, 백링크 패널, 접근불가 링크 처리를 추가해 노트 간 연결(지식 그래프)을 만든다.

**Architecture:** 클라이언트 파생(백엔드 변경 0). 링크는 안정 id 기반, 해석은 렌더 시점 동적. 백링크는 메모리 역인덱스(`buildBacklinks(tree)`). 에디터는 CodeMirror 데코레이션 + autocomplete, 공유/미리보기는 marked extension. 권한 모델: edge 절대·가시성 viewer 종속(못 읽는 노트는 트리에 없어 자동 권한-correct).

**Tech Stack:** React 18(`createElement` 스타일), TypeScript, CodeMirror 6(`@codemirror/view`·`state`·`autocomplete`), marked 12, Vitest(node env). 스펙: `docs/superpowers/specs/2026-06-18-위키링크-지식그래프-design.md`.

---

## 파일 구조

| 파일 | 책임 | 검증 |
|---|---|---|
| `frontend/src/lib/wikilink.ts` (신규) | `parseLinks(content)` · `makeLink(id,label)` — 순수 | Vitest |
| `frontend/src/lib/wikilink.test.ts` (신규) | 위 단위 테스트 | Vitest |
| `frontend/src/lib/linkIndex.ts` (신규) | `buildBacklinks(tree)` — 순수 역인덱스 | Vitest |
| `frontend/src/lib/linkIndex.test.ts` (신규) | 위 단위 테스트 | Vitest |
| `frontend/src/lib/markdown.ts` (수정) | marked extension: `[[id:X\|label]]` → 안전한 `<a class=wikilink data-note-id>` | Vitest(문자열) |
| `frontend/src/lib/wikilinkMarked.test.ts` (신규) | marked extension 출력 검증 | Vitest |
| `frontend/src/editor/wikilinkComplete.ts` (신규) | CodeMirror autocomplete source(`[[` 트리거) | build |
| `frontend/src/editor/wikilinkWidget.ts` (신규) | CM 데코레이션 + `wikiConfigFacet` | build |
| `frontend/src/editor/cm.ts` (수정) | `CreateOpts`에 wiki 설정·autocomplete·decoration·`[[` 트리거 배선 | build |
| `frontend/src/components/Editor.tsx` (수정) | wiki props → `cm.create`(ref 게터 패턴) | build |
| `frontend/src/components/Backlinks.tsx` (신규) | 활성 노트 하단 접이식 백링크 패널 | build |
| `frontend/src/App.tsx` (수정) | candidates·resolve·navigate·backlinks·삭제 경고 배선 | build |
| `frontend/src/styles/app.css` (수정) | `.cm-wikilink`·`.wikilink`·`.backlinks` 스타일 | build |

**백엔드: 변경 없음.**

**링크 문법:** 저장형 `[[id:<nodeId>|<label>]]`, 라벨 생략형 `[[id:<nodeId>]]`. `nodeId`는 `]`·`|`·공백을 포함하지 않는다(파서 정규식 전제). 매칭 실패 시 원문 텍스트 보존.

---

## Task 1: 위키링크 파싱·직렬화 (`lib/wikilink.ts`)

**Files:**
- Create: `frontend/src/lib/wikilink.ts`
- Test: `frontend/src/lib/wikilink.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

Create `frontend/src/lib/wikilink.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { parseLinks, makeLink, WIKILINK_RE } from "./wikilink";

describe("parseLinks", () => {
  it("라벨 있는 단일 링크를 파싱한다", () => {
    const r = parseLinks("앞 [[id:abc|배포 런북]] 뒤");
    expect(r).toEqual([{ id: "abc", label: "배포 런북", from: 2, to: 21 }]);
  });

  it("라벨 없는 링크는 label이 빈 문자열", () => {
    const r = parseLinks("[[id:n1]]");
    expect(r).toEqual([{ id: "n1", label: "", from: 0, to: 9 }]);
  });

  it("여러 링크를 순서대로 파싱한다", () => {
    const r = parseLinks("[[id:a|A]] 그리고 [[id:b|B]]");
    expect(r.map((x) => x.id)).toEqual(["a", "b"]);
  });

  it("깨진/비링크 대괄호는 무시한다", () => {
    expect(parseLinks("[[잘못]] [단일](x) [[id: ]]")).toEqual([]);
  });

  it("id 안에 공백·파이프·닫는 대괄호는 허용하지 않는다", () => {
    expect(parseLinks("[[id:a b|X]]")).toEqual([]);
  });
});

describe("makeLink", () => {
  it("라벨이 있으면 id:라벨 형식", () => {
    expect(makeLink("abc", "배포 런북")).toBe("[[id:abc|배포 런북]]");
  });
  it("라벨이 비면 id만", () => {
    expect(makeLink("abc", "")).toBe("[[id:abc]]");
  });
});

describe("WIKILINK_RE", () => {
  it("전역 플래그를 가진다(반복 exec)", () => {
    expect(WIKILINK_RE.global).toBe(true);
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `pnpm test src/lib/wikilink.test.ts`
Expected: FAIL — "Cannot find module './wikilink'"

- [ ] **Step 3: 최소 구현 작성**

Create `frontend/src/lib/wikilink.ts`:

```ts
// 위키링크 파싱·직렬화. 저장형: [[id:<nodeId>|<label>]], 라벨 생략형: [[id:<nodeId>]].
// nodeId는 ']' '|' 공백을 포함하지 않는다. 매칭 실패는 일반 텍스트로 둔다(파괴 금지).

export interface LinkRef {
  id: string;
  label: string; // 파이프 뒤 표시 라벨(없으면 "")
  from: number;
  to: number;
}

// 전역 정규식 — 재사용 시 lastIndex를 0으로 리셋하고 exec 루프를 돈다.
export const WIKILINK_RE = /\[\[id:([^\]|\s]+)(?:\|([^\]]*))?\]\]/g;

export function parseLinks(content: string): LinkRef[] {
  const out: LinkRef[] = [];
  WIKILINK_RE.lastIndex = 0;
  let m: RegExpExecArray | null;
  while ((m = WIKILINK_RE.exec(content || ""))) {
    out.push({ id: m[1], label: m[2] || "", from: m.index, to: m.index + m[0].length });
  }
  return out;
}

export function makeLink(id: string, label: string): string {
  return label ? `[[id:${id}|${label}]]` : `[[id:${id}]]`;
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `pnpm test src/lib/wikilink.test.ts`
Expected: PASS (8 tests)

- [ ] **Step 5: 커밋**

```bash
git add frontend/src/lib/wikilink.ts frontend/src/lib/wikilink.test.ts
git commit -m "feat(wikilink): [[id:X|label]] 파싱·직렬화 유틸"
```

---

## Task 2: 백링크 역인덱스 (`lib/linkIndex.ts`)

**Files:**
- Create: `frontend/src/lib/linkIndex.ts`
- Test: `frontend/src/lib/linkIndex.test.ts`

`buildBacklinks(tree)`는 전체 노트를 순회(`walkTree`)하며 각 노트 content의 링크(`parseLinks`)를 모아, **대상 id → 그 대상을 가리키는 출발 노트들** 맵을 만든다. self-link 제외, 같은 (source→target) 중복은 1회만.

- [ ] **Step 1: 실패하는 테스트 작성**

Create `frontend/src/lib/linkIndex.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { buildBacklinks } from "./linkIndex";
import type { VaultTree } from "../types";

function note(id: string, title: string, content = ""): any {
  return { id, type: "note", title, tags: [], updated: "", content };
}
function folder(id: string, name: string, children: any[]): any {
  return { id, type: "folder", name, children };
}

describe("buildBacklinks", () => {
  it("대상 id별로 출발 노트를 모은다", () => {
    const tree: VaultTree = [
      note("a", "A", "[[id:c|C 참조]]"),
      note("b", "B", "본문 [[id:c]]"),
      note("c", "C", "끝"),
    ];
    const idx = buildBacklinks(tree);
    expect(idx.get("c")).toEqual([
      { sourceId: "a", sourceTitle: "A" },
      { sourceId: "b", sourceTitle: "B" },
    ]);
    expect(idx.get("a")).toBeUndefined();
  });

  it("폴더 안 노트도 순회한다", () => {
    const tree: VaultTree = [folder("f", "F", [note("a", "A", "[[id:b]]"), note("b", "B")])];
    expect(buildBacklinks(tree).get("b")).toEqual([{ sourceId: "a", sourceTitle: "A" }]);
  });

  it("self-link는 제외한다", () => {
    const tree: VaultTree = [note("a", "A", "[[id:a|자기]]")];
    expect(buildBacklinks(tree).get("a")).toBeUndefined();
  });

  it("같은 노트가 같은 대상을 여러 번 가리켜도 1회만 집계한다", () => {
    const tree: VaultTree = [note("a", "A", "[[id:b]] 또 [[id:b|다시]]"), note("b", "B")];
    expect(buildBacklinks(tree).get("b")).toEqual([{ sourceId: "a", sourceTitle: "A" }]);
  });

  it("제목이 비면 '제목 없음'으로 표기한다", () => {
    const tree: VaultTree = [note("a", "", "[[id:b]]"), note("b", "B")];
    expect(buildBacklinks(tree).get("b")).toEqual([{ sourceId: "a", sourceTitle: "제목 없음" }]);
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `pnpm test src/lib/linkIndex.test.ts`
Expected: FAIL — "Cannot find module './linkIndex'"

- [ ] **Step 3: 최소 구현 작성**

Create `frontend/src/lib/linkIndex.ts`:

```ts
import { walkTree } from "./tree";
import { parseLinks } from "./wikilink";
import type { VaultTree } from "../types";

export interface Backlink {
  sourceId: string;
  sourceTitle: string;
}
export type BacklinkIndex = Map<string, Backlink[]>;

// 대상 id → 그 대상을 가리키는 출발 노트 목록. self 제외, (source,target) 중복 제거.
export function buildBacklinks(tree: VaultTree): BacklinkIndex {
  const idx: BacklinkIndex = new Map();
  walkTree(tree, (n) => {
    if (n.type !== "note") return;
    const seen = new Set<string>(); // 이 노트가 이미 집계한 대상(중복 링크 1회만)
    for (const link of parseLinks(n.content)) {
      if (link.id === n.id || seen.has(link.id)) continue;
      seen.add(link.id);
      const list = idx.get(link.id) || [];
      list.push({ sourceId: n.id, sourceTitle: n.title || "제목 없음" });
      idx.set(link.id, list);
    }
  });
  return idx;
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `pnpm test src/lib/linkIndex.test.ts`
Expected: PASS (5 tests)

- [ ] **Step 5: 커밋**

```bash
git add frontend/src/lib/linkIndex.ts frontend/src/lib/linkIndex.test.ts
git commit -m "feat(wikilink): 백링크 역인덱스 buildBacklinks"
```

---

## Task 3: marked extension — 공유/미리보기 렌더 (`lib/markdown.ts`)

**Files:**
- Modify: `frontend/src/lib/markdown.ts` (상단 marked 설정부)
- Test: `frontend/src/lib/wikilinkMarked.test.ts` (신규)

`[[id:X|label]]`을 인라인 marked extension으로 잡아 **href 없는** `<a class="wikilink" data-note-id="X">label</a>`로 렌더한다(라벨 escape, XSS 안전). 라벨이 없으면 `🔗 노트`로 표기(공유 뷰는 resolver가 없어 현재 title을 모름). 해석(접근불가 판정)·클릭 네비게이션은 v1 코어 범위 밖(에디터는 Task 5 위젯이 담당) — 공유 뷰는 비활성 링크로 라벨만 보인다.

- [ ] **Step 1: 실패하는 테스트 작성**

Create `frontend/src/lib/wikilinkMarked.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { renderWikiInline } from "./markdown";

describe("renderWikiInline (marked extension)", () => {
  it("라벨 있는 링크 → data-note-id a, href 없음", () => {
    const html = renderWikiInline("[[id:abc|배포 런북]]");
    expect(html).toContain('class="wikilink"');
    expect(html).toContain('data-note-id="abc"');
    expect(html).toContain("배포 런북");
    expect(html).not.toContain("href");
  });

  it("라벨 없는 링크는 기본 표기", () => {
    expect(renderWikiInline("[[id:abc]]")).toContain("🔗");
  });

  it("라벨의 HTML은 escape된다(XSS 차단)", () => {
    const html = renderWikiInline("[[id:x|<img src=x onerror=alert(1)>]]");
    expect(html).not.toContain("<img");
    expect(html).toContain("&lt;img");
  });

  it("일반 마크다운 링크는 건드리지 않는다", () => {
    expect(renderWikiInline("[보통](https://e.com)")).toContain("href");
  });
});
```

> 참고: `renderWikiInline(src)`는 테스트 전용으로 `marked.parse(src)` 결과 문자열을 반환하는 얇은 헬퍼다(아래 export). 실제 렌더는 `renderMarkdown`이 동일 marked 싱글턴을 통해 수행한다.

- [ ] **Step 2: 테스트 실패 확인**

Run: `pnpm test src/lib/wikilinkMarked.test.ts`
Expected: FAIL — "renderWikiInline is not a function"

- [ ] **Step 3: marked extension 등록 + 헬퍼 export**

`frontend/src/lib/markdown.ts`의 `marked.setOptions({ gfm: true, breaks: true });` (7행) **바로 아래**에 추가:

```ts
// ---- 위키링크 인라인 확장 ([[id:X|label]]) ----
// href 없는 a로 렌더(클릭 네비게이션·접근불가 판정은 소비측 책임). 라벨은 escape.
function escWiki(s: string): string {
  return s.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;");
}
marked.use({
  extensions: [{
    name: "wikilink",
    level: "inline",
    start(src: string) { return src.indexOf("[[id:"); },
    tokenizer(src: string) {
      const m = /^\[\[id:([^\]|\s]+)(?:\|([^\]]*))?\]\]/.exec(src);
      if (!m) return undefined;
      return { type: "wikilink", raw: m[0], id: m[1], label: m[2] || "" };
    },
    renderer(token: any) {
      const text = token.label ? escWiki(token.label) : "🔗 노트";
      return `<a class="wikilink" data-note-id="${escWiki(token.id)}">${text}</a>`;
    },
  }],
});

// 테스트 전용 — marked 싱글턴으로 인라인만 렌더한 HTML 문자열.
export function renderWikiInline(src: string): string {
  return marked.parse(src) as string;
}
```

> `data-note-id` 보존: `renderMarkdown`의 `DOMPurify.sanitize`는 기본 정책이 `data-*`·`class`를 보존하고 핸들러·`javascript:`만 제거한다(파일 상단 주석 참조). 별도 allow 설정 불필요.

- [ ] **Step 4: 테스트 통과 확인**

Run: `pnpm test src/lib/wikilinkMarked.test.ts`
Expected: PASS (4 tests)

- [ ] **Step 5: 전체 테스트 + 빌드로 회귀 확인**

Run: `pnpm test && pnpm build`
Expected: 전체 PASS, `✓ built`

- [ ] **Step 6: 커밋**

```bash
git add frontend/src/lib/markdown.ts frontend/src/lib/wikilinkMarked.test.ts
git commit -m "feat(wikilink): marked 인라인 확장(공유·미리보기 렌더)"
```

---

## Task 4: 에디터 자동완성 source (`editor/wikilinkComplete.ts`)

**Files:**
- Create: `frontend/src/editor/wikilinkComplete.ts`
- (Task 5에서 cm.ts에 배선)

`[[` 뒤 입력을 후보 노트(title·path)로 필터링해 선택 시 `[[id:ID|TITLE]]`을 삽입한다. 후보는 게터(`() => WikiCandidate[]`)로 받아 라이브 트리를 본다.

- [ ] **Step 1: 구현 작성**

Create `frontend/src/editor/wikilinkComplete.ts`:

```ts
import type { CompletionContext, CompletionSource, Completion } from "@codemirror/autocomplete";
import { makeLink } from "../lib/wikilink";

export interface WikiCandidate {
  id: string;
  title: string;
  path: string; // "A / B" 폴더 경로 — 동명 노트 구분용
}

// [[ 트리거 자동완성. 이미 [[id: 가 들어간(완성된) 링크 편집 중이면 비활성.
export function wikilinkCompletion(getCandidates: () => WikiCandidate[]): CompletionSource {
  return (ctx: CompletionContext) => {
    const before = ctx.matchBefore(/\[\[([^\]]*)$/);
    if (!before) return null;
    const typed = before.text.slice(2); // "[[" 제거
    if (typed.startsWith("id:")) return null; // 완성된 링크 편집 중
    const q = typed.toLowerCase();
    const options: Completion[] = getCandidates()
      .filter((c) => !q || c.title.toLowerCase().includes(q) || c.path.toLowerCase().includes(q))
      .slice(0, 50)
      .map((c) => {
        const title = c.title || "제목 없음";
        return { label: title, detail: c.path, type: "wikilink", apply: makeLink(c.id, title) };
      });
    if (!options.length) return null;
    return { from: before.from, options, validFor: /^\[\[[^\]]*$/ };
  };
}
```

- [ ] **Step 2: 타입체크**

Run: `pnpm build`
Expected: `✓ built` (아직 cm.ts에서 미사용 — export만 존재하므로 통과)

- [ ] **Step 3: 커밋**

```bash
git add frontend/src/editor/wikilinkComplete.ts
git commit -m "feat(wikilink): 에디터 [[ 자동완성 source"
```

---

## Task 5: 에디터 위젯·facet + cm.ts 배선 (`editor/wikilinkWidget.ts`, `editor/cm.ts`)

**Files:**
- Create: `frontend/src/editor/wikilinkWidget.ts`
- Modify: `frontend/src/editor/cm.ts`

에디터에서 활성(편집) 줄이 아닌 `[[id:X|label]]`을 클릭 가능한 토큰으로 치환한다. 해석 결과 `null`이면 "🔒 연결할 수 없음"으로 비활성. 설정(resolve·navigate·candidates)은 `wikiConfigFacet`로 인스턴스별 주입.

- [ ] **Step 1: 위젯·facet·decoration 헬퍼 작성**

Create `frontend/src/editor/wikilinkWidget.ts`:

```ts
import { WidgetType, Decoration } from "@codemirror/view";
import { Facet } from "@codemirror/state";
import type { EditorState, Range } from "@codemirror/state";
import { WIKILINK_RE } from "../lib/wikilink";

export interface WikiConfig {
  resolve: (id: string) => string | null; // 대상 현재 title, 못 보거나 없으면 null
  navigate: (id: string) => void;
}

export const wikiConfigFacet = Facet.define<WikiConfig, WikiConfig | null>({
  combine: (vals) => (vals.length ? vals[0] : null),
});

class WikilinkWidget extends WidgetType {
  constructor(readonly id: string, readonly label: string, readonly cfg: WikiConfig) { super(); }
  eq(o: WikilinkWidget) { return o.id === this.id && o.label === this.label; }
  toDOM() {
    const title = this.cfg.resolve(this.id);
    const el = document.createElement("span");
    if (title == null) {
      el.className = "cm-wikilink broken";
      el.textContent = "🔒 연결할 수 없음";
      return el;
    }
    el.className = "cm-wikilink";
    el.textContent = this.label || title;
    el.addEventListener("mousedown", (e) => { e.preventDefault(); this.cfg.navigate(this.id); });
    return el;
  }
  ignoreEvent() { return false; }
}

// 활성 줄이 아닌 [[id:..]]를 토큰으로 치환하는 데코레이션 목록. 설정 없으면 빈 배열.
export function wikilinkDecorations(state: EditorState, isLineActive: (pos: number) => boolean): Range<Decoration>[] {
  const cfg = state.facet(wikiConfigFacet);
  if (!cfg) return [];
  const out: Range<Decoration>[] = [];
  const text = state.doc.toString();
  WIKILINK_RE.lastIndex = 0;
  let m: RegExpExecArray | null;
  while ((m = WIKILINK_RE.exec(text))) {
    const from = m.index, to = from + m[0].length;
    if (isLineActive(from)) continue; // 커서가 그 줄이면 원문 노출(편집)
    out.push(Decoration.replace({ widget: new WikilinkWidget(m[1], m[2] || "", cfg) }).range(from, to));
  }
  return out;
}
```

- [ ] **Step 2: cm.ts — import 추가**

`frontend/src/editor/cm.ts` 22행 `import { TableWidget } from "./tableWidget";` 아래에 추가:

```ts
import { wikiConfigFacet, wikilinkDecorations } from "./wikilinkWidget";
import { wikilinkCompletion, type WikiCandidate } from "./wikilinkComplete";
import { startCompletion } from "@codemirror/autocomplete";
import type { WikiConfig } from "./wikilinkWidget";
```

- [ ] **Step 3: cm.ts — buildDecorations에 위키링크 합치기**

`buildDecorations`의 `tree.iterate({ ... });` 블록이 끝난 직후, `return Decoration.set(out, true);` (366행) **바로 위**에 추가:

```ts
  for (const r of wikilinkDecorations(state, lineActive)) out.push(r);
```

- [ ] **Step 4: cm.ts — CreateOpts 확장**

`CreateOpts` 인터페이스(416–421행)에 필드 추가:

```ts
export interface CreateOpts {
  doc?: string;
  placeholder?: string;
  onChange?: (value: string) => void;
  onFocusEditable?: () => void;
  wiki?: WikiConfig;                       // 링크 해석·네비게이션
  wikiCandidates?: () => WikiCandidate[];  // [[ 자동완성 후보
}
```

- [ ] **Step 5: cm.ts — create()에서 autocomplete·facet·트리거 배선**

`create()`의 구조 분해(424행)를 수정:

```ts
  const { doc = "", onChange, onFocusEditable, wiki, wikiCandidates } = opts || {};
```

`extensions` 배열에서 `autocompletion(...)` 줄(432행)을 아래로 교체(위키 source를 fence와 함께 등록):

```ts
      autocompletion({
        override: wikiCandidates ? [fenceCompletion, wikilinkCompletion(wikiCandidates)] : [fenceCompletion],
        activateOnTyping: true, icons: false,
      }),
```

같은 `extensions` 배열의 `atomicRanges,` 줄 아래에 facet 주입 추가:

```ts
      ...(wiki ? [wikiConfigFacet.of(wiki)] : []),
```

`updateListener`(440–443행)에 `[[` 자동완성 트리거를 추가 — 기존 콜백 본문 끝에:

```ts
      EditorView.updateListener.of((u) => {
        if (u.focusChanged) u.view.dispatch({ effects: setFocus.of(u.view.hasFocus) });
        if (u.docChanged && onChange) onChange(u.state.doc.toString());
        if (u.docChanged && wikiCandidates) {
          const pos = u.state.selection.main.head;
          if (u.state.sliceDoc(Math.max(0, pos - 2), pos) === "[[") startCompletion(u.view);
        }
      }),
```

- [ ] **Step 6: 타입체크·빌드**

Run: `pnpm build`
Expected: `✓ built`

- [ ] **Step 7: 커밋**

```bash
git add frontend/src/editor/wikilinkWidget.ts frontend/src/editor/cm.ts
git commit -m "feat(wikilink): 에디터 토큰 위젯·[[ 자동완성 배선"
```

---

## Task 6: Backlinks 패널 + App 배선 + CSS

**Files:**
- Create: `frontend/src/components/Backlinks.tsx`
- Modify: `frontend/src/components/Editor.tsx`, `frontend/src/App.tsx`, `frontend/src/styles/app.css`

- [ ] **Step 1: Backlinks 컴포넌트 작성**

Create `frontend/src/components/Backlinks.tsx`:

```tsx
import { createElement, useState } from "react";
import { Icon } from "./Icon";
import type { Backlink } from "../lib/linkIndex";

interface BacklinksProps {
  items: Backlink[];
  onOpen: (note: { id: string }) => void;
}

// 활성 노트 하단 접이식 "이 노트를 참조하는 문서" 패널. 항목이 없으면 렌더 안 함.
export function Backlinks(props: BacklinksProps) {
  const { items, onOpen } = props;
  const [open, setOpen] = useState(true);
  if (!items.length) return null;
  return createElement(
    "div", { className: "backlinks" },
    createElement("button", { className: "bl-head", onClick: () => setOpen((v) => !v) },
      createElement("span", { className: "bl-twirl" + (open ? " open" : "") }, createElement(Icon, { name: "chevron" })),
      createElement("span", { className: "bl-title" }, "이 노트를 참조하는 문서"),
      createElement("span", { className: "bl-count" }, items.length)),
    open && createElement("div", { className: "bl-list" },
      items.map((b) =>
        createElement("button", { className: "bl-item", key: b.sourceId, onClick: () => onOpen({ id: b.sourceId }) },
          createElement(Icon, { name: "fileLines" }),
          createElement("span", null, b.sourceTitle))))
  );
}
```

- [ ] **Step 2: Editor.tsx — wiki props를 cm.create에 전달(ref 게터)**

`EditorProps`(34–43행)에 추가:

```ts
  wikiCandidates?: () => import("../editor/wikilinkComplete").WikiCandidate[];
  resolveLink?: (id: string) => string | null;
  onNavigate?: (id: string) => void;
```

`onChangeRef.current = onChange;` (66행) 아래에 wiki props ref 추가:

```ts
  const wikiRef = useRef({ wikiCandidates: props.wikiCandidates, resolveLink: props.resolveLink, onNavigate: props.onNavigate });
  wikiRef.current = { wikiCandidates: props.wikiCandidates, resolveLink: props.resolveLink, onNavigate: props.onNavigate };
```

`cm.create` 호출(113–116행)에 옵션 추가 — 게터가 항상 최신 ref를 읽게 한다:

```ts
    viewRef.current = cm.create(hostRef.current, {
      doc: note.content || "",
      onChange: (text) => onChangeRef.current({ content: text }),
      wikiCandidates: () => wikiRef.current.wikiCandidates?.() ?? [],
      wiki: {
        resolve: (id) => wikiRef.current.resolveLink?.(id) ?? null,
        navigate: (id) => wikiRef.current.onNavigate?.(id),
      },
    });
```

- [ ] **Step 3: App.tsx — import + 백링크 인덱스 + 콜백**

`App.tsx`에서 `import { buildBacklinks } from "./lib/linkIndex";`를 다른 lib import 옆(33행 근처)에 추가. `Backlinks` 컴포넌트 import도 추가:

```ts
import { Backlinks } from "./components/Backlinks";
import { buildBacklinks } from "./lib/linkIndex";
```

`crumbs` useMemo(96행) 아래에 추가:

```ts
  // 백링크 역인덱스(클라 파생). 링크 후보·해석 콜백.
  const backlinks = useMemo(() => buildBacklinks(tree), [tree]);
  const wikiCandidates = useCallback(
    () => flattenNotes(tree).map(({ note, path }) => ({ id: note.id, title: note.title || "제목 없음", path: path.join(" / ") })),
    [tree],
  );
  const resolveLink = useCallback((id: string) => {
    const { node } = findNode(tree, id);
    return node && node.type === "note" ? (node.title || "제목 없음") : null;
  }, [tree]);
```

- [ ] **Step 4: App.tsx — Editor에 props 전달**

Editor `createElement` 호출(387–394행)의 props에 추가:

```ts
              wikiCandidates, resolveLink, onNavigate: (id: string) => setActiveId(id),
```

- [ ] **Step 5: App.tsx — `.doc-scroll`에 Backlinks 렌더**

`.doc-scroll` div(384–401행)에서 `activeNote ? Editor : empty-state` 삼항 **뒤**(닫는 `)` 앞)에 형제로 추가:

```ts
        , activeNote && createElement(Backlinks, {
            key: "bl-" + activeNote.id,
            items: backlinks.get(activeNote.id) || [],
            onOpen: openNote,
          })
```

- [ ] **Step 6: CSS 추가**

`frontend/src/styles/app.css` 끝에 추가:

```css
/* 위키링크 — 에디터 토큰 + 공유/미리보기 a */
.cm-wikilink, .md a.wikilink {
  color: var(--ink); cursor: pointer;
  border-bottom: 1px solid var(--border-strong);
  transition: color .12s ease, border-color .12s ease;
}
.cm-wikilink:hover, .md a.wikilink:hover { border-bottom-color: var(--ink); }
.cm-wikilink.broken, .md a.wikilink.broken {
  color: var(--text-faint); cursor: not-allowed; border-bottom-style: dotted;
}

/* 백링크 패널 */
.backlinks { margin: 28px 0 64px; border-top: 1px solid var(--border); padding-top: 14px; }
.backlinks .bl-head {
  display: flex; align-items: center; gap: 6px; background: none; border: none;
  color: var(--text-2); font-size: 13px; cursor: pointer; padding: 4px 0;
}
.backlinks .bl-twirl { display: inline-flex; transition: transform .12s ease; }
.backlinks .bl-twirl.open { transform: rotate(90deg); }
.backlinks .bl-count { color: var(--text-faint); }
.backlinks .bl-list { display: flex; flex-direction: column; gap: 2px; margin-top: 6px; }
.backlinks .bl-item {
  display: flex; align-items: center; gap: 8px; text-align: left;
  background: none; border: none; cursor: pointer; color: var(--text);
  padding: 6px 8px; border-radius: 6px; font-size: 13.5px;
}
.backlinks .bl-item:hover { background: var(--bg-active); }
```

> `--ink`·`--border-strong`·`--text-2`·`--text-faint`·`--bg-active`·`--border`는 기존 모노톤 CSS 변수다(브레드크럼·활성 행에서 사용 중).

- [ ] **Step 7: 빌드**

Run: `pnpm build`
Expected: `✓ built`

- [ ] **Step 8: 수동 확인 (local 모드)**

Run: `pnpm dev` → 노트에서 `[[`를 입력해 자동완성으로 다른 노트를 삽입 → 커서를 다른 줄로 옮기면 토큰으로 렌더되고 클릭 시 대상 노트로 이동(트리 자동 펼침·스크롤 동작). 대상 노트 하단에 "이 노트를 참조하는 문서"가 보인다.

- [ ] **Step 9: 커밋**

```bash
git add frontend/src/components/Backlinks.tsx frontend/src/components/Editor.tsx frontend/src/App.tsx frontend/src/styles/app.css
git commit -m "feat(wikilink): 백링크 패널·App 배선(candidates·resolve·navigate)"
```

---

## Task 7: 백링크 있는 노트 삭제 경고

**Files:**
- Create: `frontend/src/components/LinkWarnDialog.tsx`
- Modify: `frontend/src/App.tsx`

노트를 삭제할 때 그 노트를 가리키는 백링크가 있으면, 곧바로 휴지통으로 보내지 않고 "N개 문서가 이 노트를 참조 중"을 알리는 확인 대화상자를 띄운다(정보성 — 휴지통이라 30일 복구 가능). 백링크가 없으면 기존 동작 그대로.

- [ ] **Step 1: 경고 대화상자 컴포넌트**

Create `frontend/src/components/LinkWarnDialog.tsx` — `MoveWarnDialog.tsx`와 동일한 모달 클래스(`pf-overlay`·`pf-card`·`pf-head`·`pf-body`·`pf-foot`·`pf-btn`)를 사용한다:

```tsx
import React from "react";
import { Icon } from "./Icon";

const h = React.createElement;

interface LinkWarnDialogProps {
  name: string;
  count: number;
  onConfirm: () => void;
  onCancel: () => void;
}

// 백링크가 있는 노트 삭제 전 정보성 확인 — MoveWarnDialog와 동일 오버레이/카드 마크업.
export function LinkWarnDialog({ name, count, onConfirm, onCancel }: LinkWarnDialogProps) {
  return h("div", { className: "pf-overlay", onMouseDown: onCancel },
    h("div", { className: "pf-card", onMouseDown: (e: React.MouseEvent) => e.stopPropagation() },
      h("div", { className: "pf-head" },
        h("span", { className: "pf-av" }, h(Icon, { name: "trash" })),
        h("div", { className: "pf-id" },
          h("div", { className: "pf-emp" }, name),
          h("div", { className: "pf-role" }, "노트 삭제")),
        h("button", { className: "icon-btn pf-x", onClick: onCancel, title: "닫기" }, h(Icon, { name: "x" }))),
      h("div", { className: "pf-body" },
        h("div", { className: "pf-sec" },
          h("div", { className: "pf-msg err" }, `${count}개 문서가 이 노트를 참조하고 있습니다. 삭제하면 그 링크들은 '연결할 수 없음'이 됩니다. 휴지통에서 30일 내 복구할 수 있습니다.`),
          h("div", { className: "pf-foot" },
            h("button", { className: "pf-btn", onClick: onCancel }, "취소"),
            h("button", { className: "pf-btn danger", onClick: onConfirm }, "삭제"))))));
}
```

- [ ] **Step 2: App.tsx — 삭제 경로 분기**

`App.tsx`에 상태 추가(다른 모달 상태 옆, 72행 근처):

```ts
  const [linkWarn, setLinkWarn] = useState<{ id: string; name: string; count: number } | null>(null);
```

import 추가:

```ts
import { LinkWarnDialog } from "./components/LinkWarnDialog";
```

`removeNode`(204–209행)를 수정 — 노트이고 백링크가 있으면 경고 먼저:

```ts
  const doRemove = (id: string) => {
    actions.remove(id);
    if (id === activeId) setActiveId(null);
    const { node } = findNode(tree, id);
    toast((node && node.type === "folder" ? "폴더" : "노트") + "를 삭제했습니다", "trash");
  };
  const removeNode = (id: string) => {
    const { node } = findNode(tree, id);
    const refs = node && node.type === "note" ? (backlinks.get(id) || []) : [];
    if (refs.length) { setLinkWarn({ id, name: node!.type === "note" ? (node!.title || "제목 없음") : "", count: refs.length }); return; }
    doRemove(id);
  };
```

- [ ] **Step 3: App.tsx — 대화상자 렌더**

다른 오버레이 옆(`pendingWarn && ...` 아래, 436행 근처)에 추가:

```ts
    linkWarn && createElement(LinkWarnDialog, {
      name: linkWarn.name, count: linkWarn.count,
      onConfirm: () => { doRemove(linkWarn.id); setLinkWarn(null); },
      onCancel: () => setLinkWarn(null),
    }),
```

- [ ] **Step 4: 빌드**

Run: `pnpm build`
Expected: `✓ built`

- [ ] **Step 5: 수동 확인**

Run: `pnpm dev` → A 노트에서 B로 링크 → B를 우클릭 삭제 → "1개 문서가 참조 중" 경고 → 삭제 시 A의 링크가 "🔒 연결할 수 없음"으로 바뀐다.

- [ ] **Step 6: 커밋**

```bash
git add frontend/src/components/LinkWarnDialog.tsx frontend/src/App.tsx
git commit -m "feat(wikilink): 백링크 있는 노트 삭제 정보성 경고"
```

---

## 완료 기준 (전체)

- [ ] `pnpm test` 전체 PASS (wikilink·linkIndex·wikilinkMarked 신규 + 기존 회귀 없음)
- [ ] `pnpm build` `✓ built`
- [ ] `[[` 자동완성 → 링크 삽입 → 토큰 렌더 → 클릭 이동(트리 펼침·스크롤)
- [ ] 백링크 패널이 참조 문서를 보여주고 클릭 이동
- [ ] 접근불가/삭제 대상 링크가 "🔒 연결할 수 없음"으로 비활성
- [ ] 백링크 있는 노트 삭제 시 경고

## 범위 밖(후속)

- 공유(share.html) 읽기전용 뷰의 위키링크 클릭 네비게이션·스코프 해석 — Task 3가 안전 마크업까지만 보장(라벨 표시·비활성).
- 백엔드 `note_link` 테이블, 문서 템플릿, 관련 문서 추천, 그래프 뷰 — ③/후속 증분.
