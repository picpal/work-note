# 개인정보 노트 열람 + PII 라인 포커스 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 관리자 PII 점검 화면에서 플래그된 노트의 본문을 모달로 열람하고, 탐지된 PII 라인으로 포커스·네비게이션한다.

**Architecture:** 백엔드가 본문 + 매치 라인 위치를 단일 엔드포인트로 반환(정규식은 `PiiDetector` 단일 출처). 프런트는 고정 행 높이 + no-wrap 가상 윈도잉 뷰어로 길이 무관 일정 성능을 내고, 매치 라인을 인덱스로 점프·하이라이트한다. 열람은 감사 로깅(`pii.view`).

**Tech Stack:** Java 21 · Spring Boot 3.5 · MyBatis · JUnit5/MockMvc (backend) / Vite · TypeScript · React 18(`React.createElement`) · vitest (frontend)

**Spec:** `docs/superpowers/specs/2026-06-22-pii-노트-열람-라인포커스-design.md`

## Global Constraints

- **JSX 미사용** — 모든 프런트 컴포넌트는 `const h = React.createElement`로 작성(`.tsx`지만 JSX 문법 없음).
- **프런트 테스트** — React 렌더/e2e 없음. 결정 로직을 순수 함수로 추출해 vitest 유닛 테스트만. UI 배선은 `tsc --noEmit`(`pnpm build`) + 수동 QA로 검증.
- **마스킹 없음** — 본문·PII는 평문 표시.
- **위치 정규식 단일 출처** — 매치 위치 산출은 백엔드 `PiiDetector`에서만. 프런트는 서버가 준 위치를 소비.
- **열람 감사 필수** — admin 본문 read는 deny 우회 예외 → 모든 호출에 `audit.log(u, "pii.view", id, ip)`.
- **백엔드 테스트 격리** — 커밋 테스트는 전용 네임드 인메모리 DB(`jdbc:sqlite:file:<name>?mode=memory&cache=shared`) 또는 `@BeforeEach` DELETE 정리(공유 `::memory:`)로 PK 충돌 회피.
- **동시작업 분리 커밋** — 같은 작업 트리에 다른 터미널의 미커밋 변경이 있다. 각 커밋 step은 **해당 task가 만든/고친 파일만** `git add`한다(`git add -A` 금지).
- `NodeRow` 생성자(9 인자): `new NodeRow(id, parentId, type, name, position, content, updatedAt, deletedAt, deletedBy)`.

---

### Task 1: PiiDetector — 매치 위치 산출(`scanMatches`)

**Files:**
- Modify: `backend/src/main/java/com/worknote/pii/PiiDetector.java`
- Test: `backend/src/test/java/com/worknote/pii/PiiDetectorTest.java`

**Interfaces:**
- Consumes: 기존 `PiiDetector`의 패턴 상수(`RRN`/`PHONE`/`EMAIL`/`CARD`/`BIZ`/`PASSPORT`/`DRIVER`), `luhn(String)`, `PiiType`.
- Produces:
  - `record Match(PiiType type, int start, int end, String value)` (중첩 public record)
  - `static List<Match> scanMatches(String text)` — 모든 매치를 `start` 오름차순으로 반환. `null`/빈 → 빈 리스트.

- [ ] **Step 1: 실패하는 테스트 작성**

`PiiDetectorTest.java` 맨 끝 `}` 앞에 추가:

```java
    @Test void scanMatches_위치_포함() {
        var ms = PiiDetector.scanMatches("연락처 010-1234-5678");
        assertEquals(1, ms.size());
        assertEquals(PiiType.PHONE, ms.get(0).type());
        assertEquals("010-1234-5678", ms.get(0).value());
        assertEquals(4, ms.get(0).start());   // "연락처 "=4자(공백 포함)
    }

    @Test void scanMatches_다중_등장순서() {
        var ms = PiiDetector.scanMatches("a@b.com 그리고 010-1234-5678");
        assertEquals(2, ms.size());
        assertEquals(PiiType.EMAIL, ms.get(0).type());   // start 오름차순
        assertEquals(PiiType.PHONE, ms.get(1).type());
        assertTrue(ms.get(0).start() < ms.get(1).start());
    }

    @Test void scanMatches_빈텍스트() {
        assertTrue(PiiDetector.scanMatches("").isEmpty());
        assertTrue(PiiDetector.scanMatches(null).isEmpty());
    }
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd backend && ./gradlew test --tests "com.worknote.pii.PiiDetectorTest"`
Expected: FAIL — `cannot find symbol: method scanMatches`

- [ ] **Step 3: 구현**

`PiiDetector.java` import 블록에 추가(없으면): `import java.util.ArrayList;` `import java.util.Comparator;` (`java.util.List`는 이미 있음).

`public record Scan(...)` 선언 바로 아래에 추가:

```java
    /** 매치 위치 포함 — 라인/오프셋 산출용. start/end는 text 내 문자 인덱스. */
    public record Match(PiiType type, int start, int end, String value) {}

    /** 모든 패턴의 매치를 위치와 함께 수집(start 오름차순). CARD만 Luhn 통과. */
    public static List<Match> scanMatches(String text) {
        List<Match> out = new ArrayList<>();
        if (text == null || text.isEmpty()) return out;
        collectMatches(RRN, PiiType.RRN, text, out);
        collectMatches(BIZ, PiiType.BIZ, text, out);
        collectCardMatches(text, out);
        collectMatches(PHONE, PiiType.PHONE, text, out);
        collectMatches(EMAIL, PiiType.EMAIL, text, out);
        collectMatches(PASSPORT, PiiType.PASSPORT, text, out);
        collectMatches(DRIVER, PiiType.DRIVER, text, out);
        out.sort(Comparator.comparingInt(Match::start));
        return out;
    }

    private static void collectMatches(Pattern p, PiiType t, String text, List<Match> out) {
        Matcher m = p.matcher(text);
        while (m.find()) out.add(new Match(t, m.start(), m.end(), m.group()));
    }

    private static void collectCardMatches(String text, List<Match> out) {
        Matcher m = CARD.matcher(text);
        while (m.find()) {
            if (luhn(m.group().replaceAll("[ -]", ""))) {
                out.add(new Match(PiiType.CARD, m.start(), m.end(), m.group()));
            }
        }
    }
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd backend && ./gradlew test --tests "com.worknote.pii.PiiDetectorTest"`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add backend/src/main/java/com/worknote/pii/PiiDetector.java backend/src/test/java/com/worknote/pii/PiiDetectorTest.java
git commit -m "feat(pii): PiiDetector.scanMatches — 매치 위치(start/end) 산출"
```

---

### Task 2: PiiService — 본문 + 매치 라인 조회(`noteContent`)

**Files:**
- Create: `backend/src/main/java/com/worknote/pii/PiiContentResponse.java`
- Modify: `backend/src/main/java/com/worknote/pii/PiiService.java`
- Test: `backend/src/test/java/com/worknote/pii/PiiServiceTest.java`

**Interfaces:**
- Consumes: `PiiDetector.scanMatches` (Task 1), `nodeMapper.findById(String) → NodeRow`(필드: `name()`, `content()`, `deletedAt()`), `VaultException.notFound(String)`.
- Produces:
  - `record PiiContentResponse(String nodeId, String title, String content, List<MatchLine> matches)` with nested `record MatchLine(String type, int line, int col, String value)` — `line` 1-based, `col` 0-based.
  - `PiiContentResponse PiiService.noteContent(String nodeId)` — 삭제/부재 시 `VaultException.notFound`.
  - `static List<PiiContentResponse.MatchLine> PiiService.toMatchLines(String content, List<PiiDetector.Match> matches)`.

- [ ] **Step 1: DTO 생성**

Create `backend/src/main/java/com/worknote/pii/PiiContentResponse.java`:

```java
package com.worknote.pii;

import java.util.List;

/** 관리자 PII 노트 본문 열람 응답 — 본문 + 매치 라인 위치. */
public record PiiContentResponse(String nodeId, String title, String content, List<MatchLine> matches) {
    /** line=1-based, col=라인 내 시작 오프셋(0-based). */
    public record MatchLine(String type, int line, int col, String value) {}
}
```

- [ ] **Step 2: 실패하는 테스트 작성**

`PiiServiceTest.java` 맨 끝 `}` 앞에 추가:

```java
    @Test void noteContent_본문과_매치라인() {
        note("c1");
        svc.evaluate("c1", "이름\n전화 010-1234-5678\n끝");   // 2번째 줄에 PII
        var res = svc.noteContent("c1");
        assertEquals("이름\n전화 010-1234-5678\n끝", res.content());
        assertEquals("n", res.title());                        // note() 헬퍼는 name="n"
        assertEquals(1, res.matches().size());
        assertEquals(2, res.matches().get(0).line());          // 1-based
        assertEquals(3, res.matches().get(0).col());           // "전화 "=3자
        assertEquals("phone", res.matches().get(0).type());
        assertEquals("010-1234-5678", res.matches().get(0).value());
    }

    @Test void noteContent_매치없음_빈리스트() {
        note("c3");
        svc.evaluate("c3", "평범한 내용");
        assertTrue(svc.noteContent("c3").matches().isEmpty());
    }

    @Test void noteContent_삭제노트_404() {
        note("c2");
        nodes.softDeleteSubtree("c2", "2026-06-22T00:00:00", "admin");
        assertThrows(com.worknote.vault.VaultException.class, () -> svc.noteContent("c2"));
    }
```

- [ ] **Step 3: 테스트 실패 확인**

Run: `cd backend && ./gradlew test --tests "com.worknote.pii.PiiServiceTest"`
Expected: FAIL — `cannot find symbol: method noteContent`

- [ ] **Step 4: 구현**

`PiiService.java` import에 추가(없으면): `import com.worknote.vault.NodeRow;` `import java.util.ArrayList;` (`NodeMapper`/`VaultException`/`List`는 이미 있음).

클래스 끝 `}` 앞에 추가:

```java
    /** 관리자 본문 열람 — 본문 + 매치 라인. 삭제/부재 시 404. */
    @Transactional(readOnly = true)
    public PiiContentResponse noteContent(String nodeId) {
        NodeRow node = nodeMapper.findById(nodeId);
        if (node == null || node.deletedAt() != null) {
            throw VaultException.notFound("노트를 찾을 수 없습니다: " + nodeId);
        }
        String content = node.content() == null ? "" : node.content();
        return new PiiContentResponse(nodeId, node.name(), content,
            toMatchLines(content, PiiDetector.scanMatches(content)));
    }

    /** 매치 start(문자 인덱스)를 (line, col)로 변환. 본문 1회 순회로 개행 누적 — O(n+m).
     *  matches는 start 오름차순(scanMatches 보장). */
    static List<PiiContentResponse.MatchLine> toMatchLines(String content, List<PiiDetector.Match> matches) {
        List<PiiContentResponse.MatchLine> out = new ArrayList<>();
        if (matches.isEmpty()) return out;
        int line = 1, lineStart = 0, idx = 0;
        for (int i = 0; i <= content.length() && idx < matches.size(); i++) {
            while (idx < matches.size() && matches.get(idx).start() == i) {
                out.add(new PiiContentResponse.MatchLine(
                    matches.get(idx).type().name().toLowerCase(),
                    line, i - lineStart, matches.get(idx).value()));
                idx++;
            }
            if (i < content.length() && content.charAt(i) == '\n') { line++; lineStart = i + 1; }
        }
        return out;
    }
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `cd backend && ./gradlew test --tests "com.worknote.pii.PiiServiceTest"`
Expected: PASS

- [ ] **Step 6: 커밋**

```bash
git add backend/src/main/java/com/worknote/pii/PiiContentResponse.java backend/src/main/java/com/worknote/pii/PiiService.java backend/src/test/java/com/worknote/pii/PiiServiceTest.java
git commit -m "feat(pii): PiiService.noteContent — 본문+매치 라인(line/col) 조회"
```

---

### Task 3: PiiController — 본문 열람 엔드포인트 + 감사

**Files:**
- Modify: `backend/src/main/java/com/worknote/pii/PiiController.java`
- Test: `backend/src/test/java/com/worknote/pii/PiiApiTest.java`

**Interfaces:**
- Consumes: `pii.noteContent(String)` (Task 2), `adminGuard.requireAdmin(UserRow)`, `audit.log(UserRow, String, String, String)`, `user(req)`.
- Produces: `GET /api/admin/pii/notes/{id}/content` → `PiiContentResponse` JSON. 200(정상)·404(삭제/부재). 감사 `pii.view`.

- [ ] **Step 1: 실패하는 테스트 작성**

`PiiApiTest.java`의 `@BeforeEach seed()` DELETE 목록에 `pc1`/`pc2`가 이미 포함됨(확인만). 맨 끝 `}` 앞에 추가:

```java
    @Test void admin_note_content_returns_body_and_matches() throws Exception {
        nodes.insert(new NodeRow("pc1", null, "note", "고객정보", 1, "전화 010-1234-5678", "2026-06-14T00:00:00", null, null));
        mvc.perform(get("/api/admin/pii/notes/pc1/content"))
            .andExpect(status().isOk())
            .andExpect(jsonPath("$.title").value("고객정보"))
            .andExpect(jsonPath("$.content").value("전화 010-1234-5678"))
            .andExpect(jsonPath("$.matches[0].type").value("phone"))
            .andExpect(jsonPath("$.matches[0].line").value(1))
            .andExpect(jsonPath("$.matches[0].value").value("010-1234-5678"));
    }

    @Test void admin_note_content_404_for_missing() throws Exception {
        mvc.perform(get("/api/admin/pii/notes/nope-xyz/content"))
            .andExpect(status().isNotFound());
    }
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd backend && ./gradlew test --tests "com.worknote.pii.PiiApiTest"`
Expected: FAIL — 404가 아닌 다른 상태(엔드포인트 미존재) 또는 매핑 없음

- [ ] **Step 3: 구현**

`PiiController.java`의 `adminNotes` 메서드 바로 아래에 추가:

```java
    @GetMapping("/admin/pii/notes/{id}/content")
    public PiiContentResponse adminNoteContent(@PathVariable String id, HttpServletRequest req) {
        UserRow u = user(req);
        adminGuard.requireAdmin(u);
        PiiContentResponse res = pii.noteContent(id);
        audit.log(u, "pii.view", id, req.getRemoteAddr());
        return res;
    }
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd backend && ./gradlew test --tests "com.worknote.pii.PiiApiTest"`
Expected: PASS

- [ ] **Step 5: 백엔드 전체 회귀**

Run: `cd backend && ./gradlew test`
Expected: PASS (기존 + 신규 전부)

- [ ] **Step 6: 커밋**

```bash
git add backend/src/main/java/com/worknote/pii/PiiController.java backend/src/test/java/com/worknote/pii/PiiApiTest.java
git commit -m "feat(pii): GET /admin/pii/notes/{id}/content — 본문 열람+pii.view 감사"
```

---

### Task 4: 프런트 순수 함수(뷰어 로직) + 테스트

**Files:**
- Modify: `frontend/src/lib/pii.ts`
- Test: `frontend/src/lib/pii.test.ts` (신설)

**Interfaces:**
- Produces (from `lib/pii.ts`):
  - `interface PiiMatch { type: string; line: number; col: number; value: string }`
  - `interface Seg { text: string; mark: boolean }`
  - `matchesByLine(matches: PiiMatch[]) → Map<number, PiiMatch[]>` (key = 0-based 라인 인덱스)
  - `splitLineSegments(lineText: string, lineMatches: PiiMatch[]) → Seg[]`
  - `nextMatchIndex(cur: number, total: number, dir: 1 | -1) → number` (wrap; total≤0 → -1)
  - `visibleRange(scrollTop, viewportH, row, total, overscan=4) → { start: number; end: number }`

- [ ] **Step 1: 실패하는 테스트 작성**

Create `frontend/src/lib/pii.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { matchesByLine, splitLineSegments, nextMatchIndex, visibleRange, type PiiMatch } from "./pii";

const mt = (over: Partial<PiiMatch>): PiiMatch =>
  ({ type: "phone", line: 1, col: 0, value: "010-1234-5678", ...over });

describe("matchesByLine", () => {
  it("1-based line을 0-based 인덱스로 그룹화", () => {
    const m = matchesByLine([mt({ line: 2 }), mt({ line: 2, col: 20 }), mt({ line: 5 })]);
    expect(m.get(1)!.length).toBe(2);
    expect(m.get(4)!.length).toBe(1);
  });
});

describe("splitLineSegments", () => {
  it("매치 없으면 전체 평문", () => {
    expect(splitLineSegments("hello", [])).toEqual([{ text: "hello", mark: false }]);
  });
  it("단일 매치 분할", () => {
    const segs = splitLineSegments("전화 010-1234-5678 끝", [mt({ col: 3 })]);
    expect(segs).toEqual([
      { text: "전화 ", mark: false },
      { text: "010-1234-5678", mark: true },
      { text: " 끝", mark: false },
    ]);
  });
  it("같은 줄 다중 매치", () => {
    const segs = splitLineSegments("a@b.com 010-1234-5678", [
      mt({ type: "email", col: 0, value: "a@b.com" }), mt({ col: 8 }),
    ]);
    expect(segs.filter((s) => s.mark).map((s) => s.text)).toEqual(["a@b.com", "010-1234-5678"]);
  });
});

describe("nextMatchIndex", () => {
  it("wrap-around", () => {
    expect(nextMatchIndex(2, 3, 1)).toBe(0);
    expect(nextMatchIndex(0, 3, -1)).toBe(2);
    expect(nextMatchIndex(0, 0, 1)).toBe(-1);
  });
});

describe("visibleRange", () => {
  it("스크롤 위치 기준 가시 범위 + overscan", () => {
    expect(visibleRange(200, 100, 20, 1000, 2)).toEqual({ start: 8, end: 17 });
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd frontend && pnpm test -- pii.test`
Expected: FAIL — `matchesByLine is not a function` 등

- [ ] **Step 3: 구현**

`frontend/src/lib/pii.ts` 맨 끝에 추가:

```ts
/** 서버 ApiPiiMatch와 동형 — 뷰어 순수 로직용. */
export interface PiiMatch { type: string; line: number; col: number; value: string }
export interface Seg { text: string; mark: boolean }

/** 매치를 0-based 라인 인덱스별로 그룹화(line은 1-based → -1). */
export function matchesByLine(matches: PiiMatch[]): Map<number, PiiMatch[]> {
  const m = new Map<number, PiiMatch[]>();
  for (const x of matches) {
    const idx = x.line - 1;
    const arr = m.get(idx);
    if (arr) arr.push(x); else m.set(idx, [x]);
  }
  return m;
}

/** 한 라인을 매치 기준으로 [평문, 강조, 평문…] 세그먼트로 분할. */
export function splitLineSegments(lineText: string, lineMatches: PiiMatch[]): Seg[] {
  if (lineMatches.length === 0) return [{ text: lineText, mark: false }];
  const sorted = [...lineMatches].sort((a, b) => a.col - b.col);
  const segs: Seg[] = [];
  let pos = 0;
  for (const m of sorted) {
    const start = Math.max(pos, m.col);
    if (start > pos) segs.push({ text: lineText.slice(pos, start), mark: false });
    const end = start + m.value.length;
    segs.push({ text: lineText.slice(start, end), mark: true });
    pos = end;
  }
  if (pos < lineText.length) segs.push({ text: lineText.slice(pos), mark: false });
  return segs;
}

/** 다음/이전 매치 인덱스(wrap-around). total≤0 → -1. */
export function nextMatchIndex(cur: number, total: number, dir: 1 | -1): number {
  if (total <= 0) return -1;
  return (cur + dir + total) % total;
}

/** 가상 윈도잉 가시 범위 [start, end). overscan 라인 여유. */
export function visibleRange(
  scrollTop: number, viewportH: number, row: number, total: number, overscan = 4,
): { start: number; end: number } {
  const start = Math.max(0, Math.floor(scrollTop / row) - overscan);
  const end = Math.min(total, Math.ceil((scrollTop + viewportH) / row) + overscan);
  return { start, end };
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd frontend && pnpm test -- pii.test`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add frontend/src/lib/pii.ts frontend/src/lib/pii.test.ts
git commit -m "feat(pii): 뷰어 순수 로직(세그먼트 분할·라인 그룹·네비·가상범위)"
```

---

### Task 5: 프런트 API 타입 + 클라이언트 + 감사 라벨

**Files:**
- Modify: `frontend/src/admin/api.ts`
- Modify: `frontend/src/admin/mappers.ts`

**Interfaces:**
- Produces:
  - `interface ApiPiiMatch { type: string; line: number; col: number; value: string }`
  - `interface ApiPiiContent { nodeId: string; title: string; content: string; matches: ApiPiiMatch[] }`
  - `AdminApi.piiNoteContent(nodeId: string) → Promise<ApiPiiContent>`
  - `ACTS["pii.view"] = "개인정보 열람"` (감사 로그 라벨)

- [ ] **Step 1: API 타입 + 메서드 추가**

`frontend/src/admin/api.ts` — `ApiPiiRequest` 인터페이스 선언 바로 아래에 추가:

```ts
export interface ApiPiiMatch { type: string; line: number; col: number; value: string }
export interface ApiPiiContent { nodeId: string; title: string; content: string; matches: ApiPiiMatch[] }
```

같은 파일 `AdminApi` 객체의 `piiNotes:` 줄 바로 아래에 추가:

```ts
  piiNoteContent: (nodeId: string) => req<ApiPiiContent>(`/admin/pii/notes/${encodeURIComponent(nodeId)}/content`),
```

- [ ] **Step 2: 감사 라벨 추가**

`frontend/src/admin/mappers.ts` — `ACTS` 객체의 마지막 항목(`"share.revoke": "공유 링크 취소",`) 다음 줄에 추가:

```ts
  "pii.view": "개인정보 열람",
```

> 주: `KNOWN_ACTS`(감사 필터 드롭다운)는 변경하지 않는다 — 기존 `pii.*` act들도 드롭다운 미포함이라 관례를 따른다. `ACTS`는 라벨 맵이라 키가 더 많아도 무해(`actLabel` 폴백은 `?? act`).

- [ ] **Step 3: 타입체크 + 기존 테스트 회귀**

Run: `cd frontend && npx tsc --noEmit && pnpm test -- mappers.test`
Expected: 타입 오류 없음, mappers 테스트 PASS

- [ ] **Step 4: 커밋**

```bash
git add frontend/src/admin/api.ts frontend/src/admin/mappers.ts
git commit -m "feat(pii): AdminApi.piiNoteContent + pii.view 감사 라벨"
```

---

### Task 6: PiiNoteViewer 컴포넌트(가상 윈도잉 뷰어 + 모달) + 스타일

**Files:**
- Create: `frontend/src/components/PiiNoteViewer.tsx`
- Create: `frontend/src/styles/pii.css`  (admin.css 분리 — 다른 작업자의 admin.css 미커밋 변경과 충돌 회피)

**Interfaces:**
- Consumes: `ApiPiiContent`/`ApiPiiMatch` (Task 5), `matchesByLine`/`splitLineSegments`/`nextMatchIndex`/`visibleRange` (Task 4), `Icon`.
- Produces: `PiiNoteViewer` (default-less named export) — props:
  ```ts
  { data: ApiPiiContent; source: "request" | "note" | "exempted"; busy: boolean;
    onApprove: () => void; onReject: () => void; onNotice: () => void; onClose: () => void }
  ```
  - source별 footer 액션: `request`→허용/반려, `note`→알림 보내기, `exempted`→액션 없음.

> 이 task는 React 렌더라 유닛 테스트 대상이 아니다(로직은 Task 4에서 테스트). 검증 = `tsc --noEmit` + `pnpm build`. 시각 회귀는 Task 8 QA.

- [ ] **Step 1: 컴포넌트 작성**

Create `frontend/src/components/PiiNoteViewer.tsx`:

```tsx
/* 관리자 PII 노트 본문 열람 모달 — 원문 raw 가상 윈도잉 + 매치 라인 포커스/네비. */
import React from "react";
import type { ApiPiiContent } from "../admin/api";
import { matchesByLine, splitLineSegments, nextMatchIndex, visibleRange } from "../lib/pii";
import { piiTypeLabel } from "../lib/pii";
import { Icon } from "./Icon";
import "../styles/pii.css";

const { useState, useRef, useMemo, useEffect, useCallback } = React;
const h = React.createElement;

const ROW = 22;          // 라인 고정 높이(px) — admin.css .pii-line과 일치
const VIEWPORT = 380;    // 코드 영역 높이(px) — admin.css .pii-code와 일치

interface Props {
  data: ApiPiiContent;
  source: "request" | "note" | "exempted";
  busy: boolean;
  onApprove: () => void;
  onReject: () => void;
  onNotice: () => void;
  onClose: () => void;
}

export function PiiNoteViewer({ data, source, busy, onApprove, onReject, onNotice, onClose }: Props) {
  const lines = useMemo(() => data.content.split("\n"), [data.content]);
  const byLine = useMemo(() => matchesByLine(data.matches), [data.matches]);
  const total = data.matches.length;

  const codeRef = useRef<HTMLDivElement | null>(null);
  const [scrollTop, setScrollTop] = useState(0);
  const [active, setActive] = useState(0);

  // active 매치가 바뀌면 그 라인을 가운데로 스크롤.
  useEffect(() => {
    const el = codeRef.current;
    if (!el || total === 0) return;
    const line = data.matches[active].line - 1;
    el.scrollTop = Math.max(0, line * ROW - VIEWPORT / 2 + ROW / 2);
  }, [active, total, data.matches]);

  const go = useCallback((dir: 1 | -1) => setActive((c) => nextMatchIndex(c, total, dir)), [total]);

  const { start, end } = visibleRange(scrollTop, VIEWPORT, ROW, lines.length);
  const activeLine = total > 0 ? data.matches[active].line - 1 : -1;

  const rows: React.ReactNode[] = [];
  for (let i = start; i < end; i++) {
    const segs = splitLineSegments(lines[i], byLine.get(i) ?? []);
    rows.push(h("div", { key: i, className: "pii-line" + (i === activeLine ? " active" : "") },
      segs.map((s, k) => s.mark
        ? h("mark", { key: k }, s.text)
        : h("span", { key: k }, s.text))));
  }

  const footer: React.ReactNode[] = [
    h("button", { key: "close", className: "btn", disabled: busy, onClick: onClose }, "닫기"),
  ];
  if (source === "request") {
    footer.push(
      h("button", { key: "reject", className: "btn danger", disabled: busy, onClick: onReject }, "반려"),
      h("button", { key: "approve", className: "btn primary", disabled: busy, onClick: onApprove }, "허용"));
  } else if (source === "note") {
    footer.push(h("button", { key: "notice", className: "btn primary", disabled: busy, onClick: onNotice },
      h(Icon, { name: "check" }), "알림 보내기"));
  }

  return h("div", { className: "modal-ov", onMouseDown: () => { if (!busy) onClose(); } },
    h("div", { className: "modal pii-viewer", onMouseDown: (e: React.MouseEvent) => e.stopPropagation() },
      h("div", { className: "modal-head" },
        h("div", { className: "micon" }, h(Icon, { name: "shield" })),
        h("h3", null, data.title || "노트")),
      h("div", { className: "modal-body" },
        h("div", { className: "pii-nav" },
          total === 0
            ? h("span", { className: "muted" }, "탐지된 항목 없음")
            : h(React.Fragment, null,
                h("span", { className: "muted" },
                  piiTypeLabel(data.matches[active].type) + " · " + (active + 1) + " / " + total),
                h("span", { style: { flex: 1 } }),
                h("button", { className: "btn sm", onClick: () => go(-1) }, "이전"),
                h("button", { className: "btn sm", onClick: () => go(1) }, "다음"))),
        h("div", {
          ref: codeRef, className: "pii-code",
          onScroll: (e: React.UIEvent<HTMLDivElement>) => setScrollTop(e.currentTarget.scrollTop),
        },
          h("div", { style: { height: lines.length * ROW + "px", position: "relative" } },
            h("div", { style: { position: "absolute", top: start * ROW + "px", left: 0, right: 0 } }, rows)))),
      h("div", { className: "modal-foot" }, footer)));
}
```

- [ ] **Step 2: 스타일 파일 생성**

Create `frontend/src/styles/pii.css` (admin.css와 분리 — 다른 작업자의 admin.css 미커밋 변경과 충돌 회피. `.click-row`도 여기 포함):

```css
/* ---- PII 노트 열람 뷰어 ---- */
.modal.pii-viewer { width: min(720px, 94vw); }
.pii-nav { display: flex; align-items: center; gap: 8px; margin-bottom: 8px; font-size: 12.5px; }
.pii-code {
  height: 380px; overflow: auto; background: var(--bg-sunken); border: 1px solid var(--border);
  border-radius: 8px; font-family: var(--mono, monospace); font-size: 12.5px;
}
.pii-line {
  height: 22px; line-height: 22px; white-space: pre; padding: 0 10px;
  color: var(--text-2);
}
.pii-line.active { background: rgba(179, 38, 30, .10); }
.pii-line mark { background: #ffe08a; color: var(--ink); border-radius: 2px; padding: 0 1px; }
[data-theme="dark"] .pii-line mark { background: #7a5b00; color: #fff; }

/* 클릭 가능한 점검 테이블 행 (Task 7에서 사용) */
.atable tr.click-row { cursor: pointer; }
.atable tr.click-row:hover { background: var(--bg-sunken); }
```

- [ ] **Step 3: 타입체크 + 빌드**

Run: `cd frontend && npx tsc --noEmit && pnpm build`
Expected: 오류 없음, `dist/` 생성

- [ ] **Step 4: 커밋**

```bash
git add frontend/src/components/PiiNoteViewer.tsx frontend/src/styles/pii.css
git commit -m "feat(pii): PiiNoteViewer — raw 가상 윈도잉 뷰어 + 라인 포커스/네비"
```

---

### Task 7: Pii.tsx 배선 — 행 클릭 → 뷰어 + 액션 통합

**Files:**
- Modify: `frontend/src/admin/screens/Pii.tsx`

**Interfaces:**
- Consumes: `AdminApi.piiNoteContent`/`ApiPiiContent` (Task 5), `PiiNoteViewer` (Task 6), 기존 `approve`/`doReject`/`setReject`/`notify`.
- Produces: 3개 섹션 행 클릭 → 본문 fetch → 모달. 행 우측 기존 버튼은 `stopPropagation`. 모달 액션은 기존 흐름 재사용.

> React 렌더라 유닛 테스트 없음. 검증 = `tsc --noEmit` + `pnpm build` + Task 8 QA.

- [ ] **Step 1: import 추가**

`Pii.tsx` 상단 import에 추가:

```tsx
import { AdminApi, type ApiPiiNote, type ApiPiiRequest, type ApiPiiContent } from "../api";
import { PiiNoteViewer } from "../../components/PiiNoteViewer";
```
(기존 `import { AdminApi, type ApiPiiNote, type ApiPiiRequest } from "../api";` 줄을 위 첫 줄로 교체.)

- [ ] **Step 2: 뷰어 상태 + 오픈 핸들러 추가**

`const [busy, setBusy] = useState<string | null>(null);` 줄 아래에 추가:

```tsx
  const [viewing, setViewing] = useState<{ data: ApiPiiContent; source: "request" | "note" | "exempted" } | null>(null);

  const openViewer = async (nodeId: string, source: "request" | "note" | "exempted") => {
    try {
      const data = await AdminApi.piiNoteContent(nodeId);
      setViewing({ data, source });
    } catch (e) { toast(e instanceof ApiError ? e.message : "본문을 불러올 수 없습니다"); }
  };
```

- [ ] **Step 3: 행 클릭 배선(3개 섹션) + 버튼 stopPropagation**

세 테이블의 `h("tr", { key: ... }`에 `onClick`을 추가하고, 행 내부 액션 버튼은 `onClick`에서 `e.stopPropagation()`을 호출하도록 수정한다.

(a) 예외 요청 대기 테이블 — `reqs.map((r) => h("tr", { key: r.nodeId },` 를:
```tsx
          h("tbody", null, reqs.map((r) => h("tr", { key: r.nodeId, className: "click-row", onClick: () => void openViewer(r.nodeId, "request") },
```
그리고 그 행의 허용/반려 버튼 `onClick`을 각각:
```tsx
              h("button", { className: "btn sm primary", disabled: busy === r.nodeId, onClick: (e: React.MouseEvent) => { e.stopPropagation(); void approve(r.nodeId); } }, "허용"),
              h("button", { className: "btn sm danger", disabled: busy === r.nodeId, onClick: (e: React.MouseEvent) => { e.stopPropagation(); setReject({ nodeId: r.nodeId, title: r.title }); } }, "반려")))))))),
```

(b) 전체 개인정보 노트 테이블 — `shown.map((n) => h("tr", { key: n.nodeId },` 를:
```tsx
          h("tbody", null, shown.map((n) => h("tr", { key: n.nodeId, className: "click-row", onClick: () => void openViewer(n.nodeId, "note") },
```
그 행의 알림 버튼 `onClick`을:
```tsx
              h("button", { className: "btn sm", disabled: busy === n.nodeId || !n.updatedBy, onClick: (e: React.MouseEvent) => { e.stopPropagation(); void notify(n.nodeId); } }, "알림 보내기"))))))),
```

(c) 예외 처리된 노트 테이블 — `exempted.map((n) => h("tr", { key: n.nodeId },` 를:
```tsx
          h("tbody", null, exempted.map((n) => h("tr", { key: n.nodeId, className: "click-row", onClick: () => void openViewer(n.nodeId, "exempted") },
```

- [ ] **Step 4: 뷰어 모달 렌더 추가**

`reject && h(Modal, { ... })` 블록 바로 위(같은 반환 배열 안)에 추가:

```tsx
    viewing && h(PiiNoteViewer, {
      data: viewing.data,
      source: viewing.source,
      busy: busy === viewing.data.nodeId,
      onApprove: () => { const id = viewing.data.nodeId; setViewing(null); void approve(id); },
      onReject: () => { const { nodeId, title } = { nodeId: viewing.data.nodeId, title: viewing.data.title }; setViewing(null); setReject({ nodeId, title }); },
      onNotice: () => { const id = viewing.data.nodeId; setViewing(null); void notify(id); },
      onClose: () => setViewing(null),
    }),
```

- [ ] **Step 5: (생략) 클릭 행 스타일은 Task 6의 `pii.css`에 포함됨**

`.click-row` 스타일은 Task 6에서 `frontend/src/styles/pii.css`에 추가했다(admin.css 충돌 회피). 별도 작업 없음 — `PiiNoteViewer`가 `pii.css`를 import하므로 admin 번들에 포함된다.

- [ ] **Step 6: 타입체크 + 빌드**

Run: `cd frontend && npx tsc --noEmit && pnpm build`
Expected: 오류 없음

- [ ] **Step 7: 커밋**

```bash
git add frontend/src/admin/screens/Pii.tsx
git commit -m "feat(pii): 점검 화면 행 클릭→본문 열람 모달, 액션 통합"
```

---

### Task 8: 통합 검증 + 실화면 QA

**Files:** (없음 — 검증 전용)

- [ ] **Step 1: 백엔드 전체 테스트**

Run: `cd backend && ./gradlew test`
Expected: PASS

- [ ] **Step 2: 프런트 전체 테스트 + 빌드**

Run: `cd frontend && pnpm test && pnpm build`
Expected: PASS, `dist/` 생성

- [ ] **Step 3: 단일 jar 재빌드 + 로컬 모드 기동**

Run:
```bash
cd backend && ./gradlew bootJar
java -jar build/libs/worknote-0.1.0.jar    # local 모드(무인증)
```
Expected: `Started WorknoteApplication`, 8080 LISTEN

- [ ] **Step 4: 실화면 QA** — `gstack:qa` 또는 수동으로 `http://localhost:8080/admin.html` → 개인정보 점검:
  - 3개 섹션(예외 요청 대기 / 전체 개인정보 노트 / 예외 처리됨) 각 행 클릭 → 본문 모달이 뜨는가
  - PII 라인이 하이라이트(`<mark>`)되고 첫 매치로 스크롤되는가
  - 매치 2개 이상 노트에서 "이전/다음"이 wrap-around로 라인 점프 + "K / N" 갱신되는가
  - request 행 모달 = 허용/반려, note 행 = 알림 보내기, exempted 행 = 액션 없음(닫기만)인가
  - 행 우측 기존 버튼 클릭 시 모달이 안 뜨고(stopPropagation) 기존 동작만 하는가
  - 긴 노트(수백 줄)에서 스크롤이 매끄럽고 라인 점프가 정확한가

- [ ] **Step 5: 감사 확인** — 모달 연 뒤 감사 로그 화면에서 `개인정보 열람`(pii.view) 행이 기록됐는지 확인.

---

## 자체 리뷰 (작성자 체크 완료)

- **스펙 커버리지**: 모달 열람+액션(T6/T7) · raw 가상 윈도잉(T6) · 3섹션 클릭(T7) · 다중매치 네비(T4/T6) · 서버 위치 반환(T1/T2/T3) · pii.view 감사(T3) · 평문(전 task, 마스킹 없음) · 엣지(404=T2/T3, 매치0=T4/T6) — 모두 task 존재.
- **타입 정합**: `ApiPiiMatch`/`ApiPiiContent`(T5) = 백엔드 `MatchLine`/`PiiContentResponse`(T2) 필드 동형(type/line/col/value, nodeId/title/content/matches). 순수함수 `PiiMatch`(T4) = `ApiPiiMatch` 동형. `ROW=22`는 컴포넌트·CSS 양쪽 일치(T6).
- **후속(비범위)**: 목록 페이지네이션, 본문 직접 편집, 마스킹 토글 — 계획에 미포함(스펙 §9와 일치).
