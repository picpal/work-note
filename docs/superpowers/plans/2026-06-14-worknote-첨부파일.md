# 파일 첨부 · 이미지 미리보기 · 업로드 정책 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 노트에 drag&drop/paste/📎로 이미지·문서를 첨부하고, 이미지를 마크다운·`<img>`로 인라인 미리보기하며, 관리자가 허용 확장자·파일당 용량을 통제한다.

**Architecture:** 첨부 바이너리는 서버 디스크(`worknote.upload.dir`)에, 메타는 `attachment` 테이블에 저장(파일시스템 결정). 첨부는 노트 종속 리소스로 노트 read/write 권한을 상속하고, 공유 링크 열람 노트의 이미지는 토큰 스코프 엔드포인트로 서빙(deny 우회 read 예외 동일 적용). 마크다운 렌더는 단일 경로(`lib/markdown.ts`)에서 `<img>` src를 내부 첨부로 제한. 업로드 정책은 신설 `app_setting` key/value 테이블에 저장하고 관리자 화면에서 편집.

**Tech Stack:** Java 21 · Spring Boot 3.5 · MyBatis · Flyway · SQLite · JUnit5/MockMvc · React 18 · CodeMirror 6 · DOMPurify · Vitest

**스펙:** `docs/superpowers/specs/2026-06-14-worknote-첨부파일-이미지-업로드-design.md`

---

## 계약 고정 (전 태스크 공유 — 타입/시그니처 일관성)

- **에러 매핑:** 신규 상태코드 도입 없음. 확장자 거부·용량 초과·무효 토큰 모두 기존 `VaultException`으로 → `invalid()`=422, `notFound()`=404. `ApiExceptionHandler`가 `{"error": msg}`로 직렬화.
- **업로드 루트:** `application.yml`에 `worknote.upload.dir: ${WORKNOTE_UPLOAD_DIR:./attachments}` 신설(기본은 기본 DB `./worknote.db`와 같은 작업 디렉토리에 생성 → 백업 한 폴더).
- **첨부 id/경로:** `uuid = UUID.randomUUID().toString().replace("-","")`; `id = "att-" + uuid`; `relPath = uuid[0:2] + "/" + uuid[2:4] + "/" + uuid` (샤딩, 클라 파일명 미사용 → traversal 차단).
- **AttachmentRow** (record): `(String id, String nodeId, String filename, String ext, String mime, long size, String relPath, String createdBy, String createdAt)`
- **app_setting 키:** `upload.allowed_ext`(콤마 구분 소문자), `upload.max_bytes`(정수 문자열).
- **첨부 URL:** 본문 삽입·다운로드 = `/api/attachments/{id}`; 공유뷰 = `/api/share/{token}/attachments/{id}`.
- **이미지 판정:** `ext ∈ {png,jpg,jpeg,gif,webp}` → inline + 미리보기. 그 외 → 다운로드 칩.

---

## Task 1: 마이그레이션 V4 (attachment + app_setting + seed)

**Files:**
- Create: `backend/src/main/resources/db/migration/sqlite/V4__attachment_settings.sql`
- Test: `backend/src/test/java/com/worknote/SchemaMigrationTest.java` (기존 — 신규 테이블 검증 추가)

- [ ] **Step 1: 마이그레이션 검증 테스트 추가 (failing)**

`SchemaMigrationTest.java`를 열어 기존 테이블 존재 검증 패턴을 그대로 따라 추가:

```java
@Test
void v4_createsAttachmentAndSettingTables() {
    // sqlite_master 조회로 테이블 존재 확인 (기존 테스트와 동일 JdbcTemplate 패턴)
    Integer att = jdbc.queryForObject(
        "SELECT count(*) FROM sqlite_master WHERE type='table' AND name='attachment'", Integer.class);
    Integer setting = jdbc.queryForObject(
        "SELECT count(*) FROM sqlite_master WHERE type='table' AND name='app_setting'", Integer.class);
    assertThat(att).isEqualTo(1);
    assertThat(setting).isEqualTo(1);
    // seed 2건
    String exts = jdbc.queryForObject("SELECT value FROM app_setting WHERE key='upload.allowed_ext'", String.class);
    String max  = jdbc.queryForObject("SELECT value FROM app_setting WHERE key='upload.max_bytes'", String.class);
    assertThat(exts).contains("png").contains("pdf");
    assertThat(max).isEqualTo("26214400");
}
```

(기존 테스트의 `jdbc` autowire/`@SpringBootTest` 설정을 재사용. 없으면 `AuthControllerTest` 헤더의 `@SpringBootTest(properties = "spring.datasource.url=jdbc:sqlite:file::memory:?cache=shared")` + `@Autowired JdbcTemplate jdbc` 패턴 적용.)

- [ ] **Step 2: 테스트 실행 → 실패 확인**

Run: `cd backend && ./gradlew test --tests '*SchemaMigrationTest'`
Expected: FAIL (attachment/app_setting 테이블 없음)

- [ ] **Step 3: 마이그레이션 작성**

`V4__attachment_settings.sql`:

```sql
-- V4__attachment_settings.sql  (ANSI 지향 — Oracle 전환 시 TEXT→VARCHAR2/CLOB)
CREATE TABLE attachment (
  id         TEXT PRIMARY KEY,
  node_id    TEXT NOT NULL REFERENCES node(id),
  filename   TEXT NOT NULL,
  ext        TEXT NOT NULL,
  mime       TEXT NOT NULL,
  size       INTEGER NOT NULL,
  rel_path   TEXT NOT NULL,
  created_by TEXT,
  created_at TEXT NOT NULL
);
CREATE INDEX idx_attachment_node ON attachment(node_id);

CREATE TABLE app_setting (
  key   TEXT PRIMARY KEY,
  value TEXT NOT NULL
);
INSERT INTO app_setting (key, value) VALUES
  ('upload.allowed_ext', 'png,jpg,jpeg,gif,webp,pdf,docx,xlsx,pptx,txt,md,csv,zip'),
  ('upload.max_bytes', '26214400');
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd backend && ./gradlew test --tests '*SchemaMigrationTest'`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add backend/src/main/resources/db/migration/sqlite/V4__attachment_settings.sql backend/src/test/java/com/worknote/SchemaMigrationTest.java
git commit -m "feat(backend): V4 마이그레이션 — attachment·app_setting 테이블 + 업로드 정책 seed"
```

---

## Task 2: UploadPolicy (순수 정책 로직)

**Files:**
- Create: `backend/src/main/java/com/worknote/attachment/UploadPolicy.java`
- Test: `backend/src/test/java/com/worknote/attachment/UploadPolicyTest.java`

- [ ] **Step 1: 실패 테스트 작성**

```java
package com.worknote.attachment;

import com.worknote.vault.VaultException;
import org.junit.jupiter.api.Test;
import java.util.List;
import static org.assertj.core.api.Assertions.*;

class UploadPolicyTest {
    private UploadPolicy policy() { return UploadPolicy.of(List.of("png", "jpg", "pdf"), 1000); }

    @Test void extExtraction_lowercases_and_takesLast() {
        assertThat(UploadPolicy.ext("Photo.PNG")).isEqualTo("png");
        assertThat(UploadPolicy.ext("a.tar.gz")).isEqualTo("gz");
    }
    @Test void noExtension_isRejected() {
        assertThatThrownBy(() -> policy().check("README", 10)).isInstanceOf(VaultException.class);
    }
    @Test void allowedExt_passes() {
        assertThatCode(() -> policy().check("a.png", 10)).doesNotThrowAnyException();
        assertThatCode(() -> policy().check("a.JPG", 10)).doesNotThrowAnyException(); // 대소문자 무관
    }
    @Test void disallowedExt_throws() {
        assertThatThrownBy(() -> policy().check("a.exe", 10)).isInstanceOf(VaultException.class);
    }
    @Test void overSize_throws() {
        assertThatThrownBy(() -> policy().check("a.png", 1001)).isInstanceOf(VaultException.class);
    }
    @Test void isImage_onlyKnownImageExts() {
        assertThat(UploadPolicy.isImage("png")).isTrue();
        assertThat(UploadPolicy.isImage("pdf")).isFalse();
    }
}
```

- [ ] **Step 2: 실패 확인**

Run: `cd backend && ./gradlew test --tests '*UploadPolicyTest'`
Expected: FAIL (UploadPolicy 없음)

- [ ] **Step 3: 구현**

```java
package com.worknote.attachment;

import com.worknote.vault.VaultException;
import java.util.LinkedHashSet;
import java.util.List;
import java.util.Set;

/** 업로드 허용 정책 — 확장자 allowlist + 파일당 최대 바이트. 순수 검사 로직(테스트 용이). */
public final class UploadPolicy {
    private static final Set<String> IMAGE_EXTS = Set.of("png", "jpg", "jpeg", "gif", "webp");

    private final Set<String> allowedExt;
    private final long maxBytes;

    private UploadPolicy(Set<String> allowedExt, long maxBytes) {
        this.allowedExt = allowedExt;
        this.maxBytes = maxBytes;
    }

    public static UploadPolicy of(List<String> exts, long maxBytes) {
        Set<String> set = new LinkedHashSet<>();
        for (String e : exts) {
            String n = e.trim().toLowerCase().replaceFirst("^\\.", "");
            if (!n.isEmpty()) set.add(n);
        }
        return new UploadPolicy(set, maxBytes);
    }

    public Set<String> allowedExt() { return allowedExt; }
    public long maxBytes() { return maxBytes; }

    /** 확장자: 마지막 '.' 뒤 소문자. '.' 없으면 빈 문자열. */
    public static String ext(String filename) {
        String name = filename == null ? "" : filename;
        int dot = name.lastIndexOf('.');
        return dot < 0 ? "" : name.substring(dot + 1).toLowerCase();
    }

    public static boolean isImage(String ext) { return IMAGE_EXTS.contains(ext.toLowerCase()); }

    /** 위반 시 VaultException.invalid(422). */
    public void check(String filename, long size) {
        String ext = ext(filename);
        if (ext.isEmpty() || !allowedExt.contains(ext)) {
            throw VaultException.invalid("허용하지 않는 파일 형식입니다: " + (ext.isEmpty() ? filename : "." + ext));
        }
        if (size > maxBytes) {
            throw VaultException.invalid("파일이 너무 큽니다 (최대 " + (maxBytes / 1024 / 1024) + "MB)");
        }
    }
}
```

- [ ] **Step 4: 통과 확인**

Run: `cd backend && ./gradlew test --tests '*UploadPolicyTest'`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add backend/src/main/java/com/worknote/attachment/UploadPolicy.java backend/src/test/java/com/worknote/attachment/UploadPolicyTest.java
git commit -m "feat(backend): UploadPolicy — 확장자·용량 검사 순수 로직"
```

---

## Task 3: SettingService + SettingMapper (app_setting 접근)

**Files:**
- Create: `backend/src/main/java/com/worknote/setting/SettingMapper.java`
- Create: `backend/src/main/resources/mappers/SettingMapper.xml`
- Create: `backend/src/main/java/com/worknote/setting/SettingService.java`
- Test: `backend/src/test/java/com/worknote/setting/SettingServiceTest.java`

- [ ] **Step 1: 실패 테스트 작성**

```java
package com.worknote.setting;

import com.worknote.attachment.UploadPolicy;
import org.junit.jupiter.api.*;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import java.util.List;
import static org.assertj.core.api.Assertions.assertThat;

@SpringBootTest(properties = "spring.datasource.url=jdbc:sqlite:file::memory:?cache=shared")
class SettingServiceTest {
    @Autowired SettingService svc;

    @Test void seededUploadPolicy_isReadable() {
        UploadPolicy p = svc.uploadPolicy();
        assertThat(p.allowedExt()).contains("png", "pdf");
        assertThat(p.maxBytes()).isEqualTo(26214400L);
    }
    @Test void setUploadPolicy_persists() {
        svc.setUploadPolicy(List.of("png", "svg"), 5000);
        UploadPolicy p = svc.uploadPolicy();
        assertThat(p.allowedExt()).containsExactlyInAnyOrder("png", "svg");
        assertThat(p.maxBytes()).isEqualTo(5000L);
    }
}
```

- [ ] **Step 2: 실패 확인**

Run: `cd backend && ./gradlew test --tests '*SettingServiceTest'`
Expected: FAIL

- [ ] **Step 3: 구현 — Mapper + XML + Service**

`SettingMapper.java`:
```java
package com.worknote.setting;
import org.apache.ibatis.annotations.Mapper;
import org.apache.ibatis.annotations.Param;

@Mapper
public interface SettingMapper {
    String get(@Param("key") String key);
    void put(@Param("key") String key, @Param("value") String value);
}
```

`SettingMapper.xml` (`backend/src/main/resources/mappers/`):
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.worknote.setting.SettingMapper">
  <select id="get" resultType="string">SELECT value FROM app_setting WHERE key = #{key}</select>
  <!-- SQLite upsert. Oracle 전환 시 MERGE 로 대체 -->
  <insert id="put">
    INSERT INTO app_setting (key, value) VALUES (#{key}, #{value})
    ON CONFLICT(key) DO UPDATE SET value = excluded.value
  </insert>
</mapper>
```

`SettingService.java`:
```java
package com.worknote.setting;

import com.worknote.attachment.UploadPolicy;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;
import java.util.Arrays;
import java.util.List;

/** app_setting 기반 런타임 설정. 현재는 업로드 정책만. */
@Service
public class SettingService {
    static final String KEY_EXT = "upload.allowed_ext";
    static final String KEY_MAX = "upload.max_bytes";
    private static final long DEFAULT_MAX = 26214400L; // seed 누락 시 안전망

    private final SettingMapper mapper;
    public SettingService(SettingMapper mapper) { this.mapper = mapper; }

    @Transactional(readOnly = true)
    public UploadPolicy uploadPolicy() {
        String exts = mapper.get(KEY_EXT);
        String max = mapper.get(KEY_MAX);
        List<String> list = (exts == null || exts.isBlank()) ? List.of() : Arrays.asList(exts.split(","));
        long maxBytes = (max == null || max.isBlank()) ? DEFAULT_MAX : Long.parseLong(max.trim());
        return UploadPolicy.of(list, maxBytes);
    }

    @Transactional
    public void setUploadPolicy(List<String> exts, long maxBytes) {
        if (maxBytes < 1) throw com.worknote.vault.VaultException.invalid("최대 용량은 1 이상이어야 합니다");
        // UploadPolicy.of로 정규화(소문자·점 제거·중복 제거) 후 저장
        String joined = String.join(",", UploadPolicy.of(exts, maxBytes).allowedExt());
        mapper.put(KEY_EXT, joined);
        mapper.put(KEY_MAX, String.valueOf(maxBytes));
    }
}
```

- [ ] **Step 4: 통과 확인**

Run: `cd backend && ./gradlew test --tests '*SettingServiceTest'`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add backend/src/main/java/com/worknote/setting/ backend/src/main/resources/mappers/SettingMapper.xml backend/src/test/java/com/worknote/setting/SettingServiceTest.java
git commit -m "feat(backend): SettingService — app_setting 기반 업로드 정책 읽기/쓰기"
```

---

## Task 4: AttachmentMapper + AttachmentService (디스크 IO + 정책 강제)

**Files:**
- Create: `backend/src/main/java/com/worknote/attachment/AttachmentRow.java`
- Create: `backend/src/main/java/com/worknote/attachment/AttachmentMapper.java`
- Create: `backend/src/main/resources/mappers/AttachmentMapper.xml`
- Create: `backend/src/main/java/com/worknote/attachment/AttachmentService.java`
- Test: `backend/src/test/java/com/worknote/attachment/AttachmentServiceTest.java`

- [ ] **Step 1: 실패 테스트 작성**

```java
package com.worknote.attachment;

import com.worknote.vault.VaultException;
import org.junit.jupiter.api.*;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.jdbc.core.JdbcTemplate;
import org.springframework.test.context.TestPropertySource;
import java.nio.file.*;
import static org.assertj.core.api.Assertions.*;

@SpringBootTest(properties = {
    "spring.datasource.url=jdbc:sqlite:file::memory:?cache=shared",
    "worknote.upload.dir=build/test-attachments"
})
class AttachmentServiceTest {
    @Autowired AttachmentService svc;
    @Autowired JdbcTemplate jdbc;

    @BeforeEach void seedNode() {
        jdbc.update("DELETE FROM attachment");
        jdbc.update("DELETE FROM node");
        jdbc.update("INSERT INTO node(id,parent_id,type,name,position) VALUES('n1',NULL,'note','노트',1)");
    }

    @Test void store_writesFileAndRow() throws Exception {
        AttachmentRow r = svc.store("n1", "a.png", new byte[]{1,2,3}, "local");
        assertThat(r.id()).startsWith("att-");
        assertThat(r.nodeId()).isEqualTo("n1");
        assertThat(r.size()).isEqualTo(3);
        assertThat(Files.exists(svc.pathOf(r))).isTrue();
        assertThat(svc.findById(r.id())).isNotNull();
    }
    @Test void store_rejectsDisallowedExt() {
        assertThatThrownBy(() -> svc.store("n1", "a.exe", new byte[]{1}, "local"))
            .isInstanceOf(VaultException.class);
    }
    @Test void delete_removesFileAndRow() {
        AttachmentRow r = svc.store("n1", "a.png", new byte[]{1}, "local");
        svc.delete(r.id());
        assertThat(svc.findById(r.id())).isNull();
        assertThat(Files.exists(svc.pathOf(r))).isFalse();
    }
    @Test void deleteForNodes_removesAll() {
        svc.store("n1", "a.png", new byte[]{1}, "local");
        svc.store("n1", "b.png", new byte[]{2}, "local");
        svc.deleteForNodes(java.util.List.of("n1"));
        assertThat(svc.findByNode("n1")).isEmpty();
    }
}
```

- [ ] **Step 2: 실패 확인**

Run: `cd backend && ./gradlew test --tests '*AttachmentServiceTest'`
Expected: FAIL

- [ ] **Step 3: 구현**

`AttachmentRow.java`:
```java
package com.worknote.attachment;
public record AttachmentRow(String id, String nodeId, String filename, String ext,
                            String mime, long size, String relPath, String createdBy, String createdAt) {}
```

`AttachmentMapper.java`:
```java
package com.worknote.attachment;
import org.apache.ibatis.annotations.*;
import java.util.List;

@Mapper
public interface AttachmentMapper {
    void insert(AttachmentRow row);
    AttachmentRow findById(@Param("id") String id);
    List<AttachmentRow> findByNode(@Param("nodeId") String nodeId);
    void delete(@Param("id") String id);
    List<AttachmentRow> findByNodeIds(@Param("ids") List<String> ids);
    void deleteByNodeIds(@Param("ids") List<String> ids);
}
```

`AttachmentMapper.xml`:
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.worknote.attachment.AttachmentMapper">
  <insert id="insert">
    INSERT INTO attachment (id, node_id, filename, ext, mime, size, rel_path, created_by, created_at)
    VALUES (#{id}, #{nodeId}, #{filename}, #{ext}, #{mime}, #{size}, #{relPath}, #{createdBy}, #{createdAt})
  </insert>
  <select id="findById" resultType="com.worknote.attachment.AttachmentRow">
    SELECT * FROM attachment WHERE id = #{id}
  </select>
  <select id="findByNode" resultType="com.worknote.attachment.AttachmentRow">
    SELECT * FROM attachment WHERE node_id = #{nodeId} ORDER BY created_at
  </select>
  <delete id="delete">DELETE FROM attachment WHERE id = #{id}</delete>
  <select id="findByNodeIds" resultType="com.worknote.attachment.AttachmentRow">
    SELECT * FROM attachment WHERE node_id IN
    <foreach item="i" collection="ids" open="(" separator="," close=")">#{i}</foreach>
  </select>
  <delete id="deleteByNodeIds">
    DELETE FROM attachment WHERE node_id IN
    <foreach item="i" collection="ids" open="(" separator="," close=")">#{i}</foreach>
  </delete>
</mapper>
```

`AttachmentService.java`:
```java
package com.worknote.attachment;

import com.worknote.setting.SettingService;
import com.worknote.vault.VaultException;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.io.IOException;
import java.nio.file.*;
import java.time.Clock;
import java.time.LocalDateTime;
import java.time.format.DateTimeFormatter;
import java.util.List;
import java.util.UUID;

/** 첨부 디스크 저장/삭제 + 정책 강제. 메타는 DB, 바이너리는 worknote.upload.dir 아래. */
@Service
public class AttachmentService {
    private final AttachmentMapper mapper;
    private final SettingService settings;
    private final Clock clock;
    private final Path root;

    public AttachmentService(AttachmentMapper mapper, SettingService settings, Clock clock,
                             @Value("${worknote.upload.dir:./attachments}") String uploadDir) {
        this.mapper = mapper;
        this.settings = settings;
        this.clock = clock;
        this.root = Paths.get(uploadDir).toAbsolutePath().normalize();
    }

    @Transactional
    public AttachmentRow store(String nodeId, String filename, byte[] bytes, String createdBy) {
        settings.uploadPolicy().check(filename, bytes.length);
        String uuid = UUID.randomUUID().toString().replace("-", "");
        String relPath = uuid.substring(0, 2) + "/" + uuid.substring(2, 4) + "/" + uuid;
        Path target = root.resolve(relPath).normalize();
        if (!target.startsWith(root)) throw VaultException.invalid("잘못된 저장 경로"); // 방어
        try {
            Files.createDirectories(target.getParent());
            Files.write(target, bytes, StandardOpenOption.CREATE_NEW);
        } catch (IOException e) {
            throw VaultException.invalid("파일 저장 실패: " + e.getMessage());
        }
        String ext = UploadPolicy.ext(filename);
        String mime = guessMime(ext);
        AttachmentRow row = new AttachmentRow("att-" + uuid, nodeId, filename, ext, mime,
            bytes.length, relPath, createdBy, LocalDateTime.now(clock).format(DateTimeFormatter.ISO_LOCAL_DATE_TIME));
        mapper.insert(row);
        return row;
    }

    public AttachmentRow findById(String id) { return mapper.findById(id); }
    public List<AttachmentRow> findByNode(String nodeId) { return mapper.findByNode(nodeId); }
    public Path pathOf(AttachmentRow row) { return root.resolve(row.relPath()).normalize(); }
    public byte[] read(AttachmentRow row) {
        try { return Files.readAllBytes(pathOf(row)); }
        catch (IOException e) { throw VaultException.notFound("첨부 파일을 찾을 수 없습니다"); }
    }

    @Transactional
    public void delete(String id) {
        AttachmentRow row = mapper.findById(id);
        if (row == null) return;
        mapper.delete(id);
        try { Files.deleteIfExists(pathOf(row)); } catch (IOException ignored) {}
    }

    /** 노트 purge 연계 — 해당 노드들의 첨부 파일+메타 일괄 삭제. */
    @Transactional
    public void deleteForNodes(List<String> nodeIds) {
        if (nodeIds == null || nodeIds.isEmpty()) return;
        for (AttachmentRow row : mapper.findByNodeIds(nodeIds)) {
            try { Files.deleteIfExists(pathOf(row)); } catch (IOException ignored) {}
        }
        mapper.deleteByNodeIds(nodeIds);
    }

    private static String guessMime(String ext) {
        return switch (ext) {
            case "png" -> "image/png";
            case "jpg", "jpeg" -> "image/jpeg";
            case "gif" -> "image/gif";
            case "webp" -> "image/webp";
            case "pdf" -> "application/pdf";
            case "txt", "md", "csv" -> "text/plain";
            default -> "application/octet-stream";
        };
    }
}
```

- [ ] **Step 4: 통과 확인**

Run: `cd backend && ./gradlew test --tests '*AttachmentServiceTest'`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add backend/src/main/java/com/worknote/attachment/ backend/src/main/resources/mappers/AttachmentMapper.xml backend/src/test/java/com/worknote/attachment/AttachmentServiceTest.java
git commit -m "feat(backend): AttachmentService — 디스크 저장/삭제 + 정책 강제 + purge 정리"
```

---

## Task 5: ShareLinkService 비증가 검증 메서드 (공유 첨부용)

**Files:**
- Modify: `backend/src/main/java/com/worknote/share/ShareLinkService.java`
- Test: `backend/src/test/java/com/worknote/share/ShareLinkServiceTest.java` (기존 — 케이스 추가)

- [ ] **Step 1: 실패 테스트 추가**

`ShareLinkServiceTest.java`에 추가 (기존 헬퍼·셋업 재사용):

```java
@Test void nodeIdForAttachment_validToken_returnsNodeId_withoutIncrement() {
    ShareLinkRow link = svc.create("n1", "local", 7, null, null);
    int before = svc.listForNode("n1", null).get(0).viewCount();
    String nodeId = svc.nodeIdForAttachment(link.token(), null);
    assertThat(nodeId).isEqualTo("n1");
    int after = svc.listForNode("n1", null).get(0).viewCount();
    assertThat(after).isEqualTo(before); // 이미지 로드는 열람수 미증가
}
@Test void nodeIdForAttachment_invalidToken_throws() {
    assertThatThrownBy(() -> svc.nodeIdForAttachment("bogus", null))
        .isInstanceOf(com.worknote.vault.VaultException.class);
}
```

(기존 테스트의 노드 셋업 `n1`·`svc` autowire를 따른다. 없으면 `@BeforeEach`에서 `jdbc.update("INSERT INTO node...")`로 'n1' note 시드.)

- [ ] **Step 2: 실패 확인**

Run: `cd backend && ./gradlew test --tests '*ShareLinkServiceTest'`
Expected: FAIL (nodeIdForAttachment 없음)

- [ ] **Step 3: 구현 — resolve 검증부 추출 + 비증가 메서드**

`ShareLinkService`의 `resolve`에서 검증 로직을 `validate(token, viewerEmp)`로 추출하고, 비증가 메서드 추가:

```java
/** 공통 검증 — 무효 사유 전부 404 단일화. 활성·미만료·열람수·pin·노드 존재 확인. */
private NodeRow validate(String token, String viewerEmp) {
    ShareLinkRow row = mapper.findByToken(token);
    if (row == null || row.revokedAt() != null
        || row.expiresAt().compareTo(iso(LocalDateTime.now(clock))) <= 0
        || (row.maxViews() != null && row.viewCount() >= row.maxViews())
        || (row.pinEmps() != null && viewerEmp != null && !fromJson(row.pinEmps()).contains(viewerEmp))) {
        throw invalidLink();
    }
    NodeRow node = nodes.findById(row.nodeId());
    if (node == null || node.deletedAt() != null) throw invalidLink();
    return node;
}

/** 첨부 이미지 서빙용 — 검증만, 열람수 미증가. 노드 id 반환. */
@Transactional(readOnly = true)
public String nodeIdForAttachment(String token, String viewerEmp) {
    return validate(token, viewerEmp).id();
}
```

그리고 기존 `resolve`를 `validate` 사용하도록 정리 (열람수 증가는 resolve에서만):

```java
@Transactional
public ShareView resolve(String token, String viewerEmp) {
    ShareLinkRow row = mapper.findByToken(token);
    NodeRow node = validate(token, viewerEmp);
    mapper.incrementViewCount(row.id());
    return new ShareView(row.id(), row.nodeId(), node.name(), node.content(),
        node.updatedAt() == null ? null : node.updatedAt().substring(0, 10));
}
```

(주의: `validate`가 `findByToken`을 다시 호출하므로 `resolve`에서 `row`는 incrementViewCount용 id 확보 목적. 중복 조회가 거슬리면 `validate`가 `ShareLinkRow`를 반환하도록 바꾸고 node는 별도 조회 — 어느 쪽이든 동작 동일. 기존 테스트 그린 유지가 우선.)

- [ ] **Step 4: 통과 확인 (기존 공유 테스트 포함 회귀)**

Run: `cd backend && ./gradlew test --tests '*ShareLinkServiceTest' --tests '*ShareApiTest'`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add backend/src/main/java/com/worknote/share/ShareLinkService.java backend/src/test/java/com/worknote/share/ShareLinkServiceTest.java
git commit -m "feat(backend): 공유 토큰 비증가 검증(nodeIdForAttachment) — 첨부 이미지 서빙용"
```

---

## Task 6: AttachmentController (업로드·다운로드·삭제·공유 서빙)

**Files:**
- Create: `backend/src/main/java/com/worknote/attachment/AttachmentController.java`
- Modify: `backend/src/main/resources/application.yml` (multipart 한도)
- Modify: `backend/src/main/java/com/worknote/ApiExceptionHandler.java` (MaxUploadSizeExceeded)
- Test: `backend/src/test/java/com/worknote/attachment/AttachmentApiTest.java`

- [ ] **Step 1: 실패 테스트 작성 (MockMvc, local 모드 = 무인증)**

```java
package com.worknote.attachment;

import org.junit.jupiter.api.*;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.jdbc.core.JdbcTemplate;
import org.springframework.mock.web.MockMultipartFile;
import org.springframework.test.web.servlet.MockMvc;

import static org.hamcrest.Matchers.*;
import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.*;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.*;

@SpringBootTest(properties = {
    "spring.datasource.url=jdbc:sqlite:file::memory:?cache=shared",
    "worknote.upload.dir=build/test-attachments-api"
})
@AutoConfigureMockMvc
class AttachmentApiTest {
    @Autowired MockMvc mvc;
    @Autowired JdbcTemplate jdbc;

    @BeforeEach void seed() {
        jdbc.update("DELETE FROM attachment");
        jdbc.update("DELETE FROM node");
        jdbc.update("INSERT INTO node(id,parent_id,type,name,position) VALUES('n1',NULL,'note','노트',1)");
    }

    private String upload(String name, byte[] body) throws Exception {
        return mvc.perform(multipart("/api/nodes/n1/attachments")
                .file(new MockMultipartFile("file", name, null, body)))
            .andExpect(status().isCreated())
            .andExpect(jsonPath("$.url").value(startsWith("/api/attachments/att-")))
            .andReturn().getResponse().getContentAsString();
    }

    @Test void upload_then_download_returnsBytesWithNosniff() throws Exception {
        String json = upload("a.png", new byte[]{1,2,3});
        String id = com.jayway.jsonpath.JsonPath.read(json, "$.id");
        mvc.perform(get("/api/attachments/" + id))
            .andExpect(status().isOk())
            .andExpect(header().string("X-Content-Type-Options", "nosniff"))
            .andExpect(header().string("Content-Type", "image/png"));
    }
    @Test void upload_disallowedExt_is422() throws Exception {
        mvc.perform(multipart("/api/nodes/n1/attachments")
                .file(new MockMultipartFile("file", "a.exe", null, new byte[]{1})))
            .andExpect(status().isUnprocessableEntity())
            .andExpect(jsonPath("$.error").exists());
    }
    @Test void delete_then_download_is404() throws Exception {
        String json = upload("a.png", new byte[]{1});
        String id = com.jayway.jsonpath.JsonPath.read(json, "$.id");
        mvc.perform(delete("/api/attachments/" + id)).andExpect(status().isNoContent());
        mvc.perform(get("/api/attachments/" + id)).andExpect(status().isNotFound());
    }
    @Test void shareScoped_validToken_serves_and_otherNodeAttachment_404() throws Exception {
        String json = upload("a.png", new byte[]{9});
        String id = com.jayway.jsonpath.JsonPath.read(json, "$.id");
        // 공유 링크 생성 (local 모드 — 가드 통과)
        String shareJson = mvc.perform(post("/api/nodes/n1/share").contentType("application/json").content("{}"))
            .andExpect(status().isCreated()).andReturn().getResponse().getContentAsString();
        String token = com.jayway.jsonpath.JsonPath.read(shareJson, "$.token");
        mvc.perform(get("/api/share/" + token + "/attachments/" + id)).andExpect(status().isOk());
        // 다른 노드 + 다른 첨부는 404
        mvc.perform(get("/api/share/" + token + "/attachments/att-nonexistent")).andExpect(status().isNotFound());
    }
}
```

(`com.jayway.jsonpath.JsonPath`는 spring-boot-starter-test에 포함. 미존재 시 응답 문자열 정규식으로 id 추출.)

- [ ] **Step 2: 실패 확인**

Run: `cd backend && ./gradlew test --tests '*AttachmentApiTest'`
Expected: FAIL (컨트롤러 없음)

- [ ] **Step 3: 컨트롤러 구현**

```java
package com.worknote.attachment;

import com.worknote.audit.AuditService;
import com.worknote.auth.AuthFilter;
import com.worknote.auth.UserRow;
import com.worknote.share.ShareLinkService;
import com.worknote.vault.VaultException;
import com.worknote.vault.VaultGuard;
import jakarta.servlet.http.HttpServletRequest;
import org.springframework.http.*;
import org.springframework.web.bind.annotation.*;
import org.springframework.web.multipart.MultipartFile;

import java.io.IOException;
import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.Map;

/** 첨부 REST API. 업로드/삭제=노트 write, 열람=노트 read. 공유 서빙은 토큰이 통제(VaultGuard 없음). */
@RestController
@RequestMapping("/api")
public class AttachmentController {

    private final AttachmentService svc;
    private final VaultGuard guard;
    private final ShareLinkService share;
    private final AuditService audit;

    public AttachmentController(AttachmentService svc, VaultGuard guard, ShareLinkService share, AuditService audit) {
        this.svc = svc; this.guard = guard; this.share = share; this.audit = audit;
    }

    private static UserRow user(HttpServletRequest req) {
        return (UserRow) req.getAttribute(AuthFilter.CURRENT_USER);
    }

    @PostMapping("/nodes/{id}/attachments")
    public ResponseEntity<Map<String, Object>> upload(@PathVariable String id,
            @RequestParam("file") MultipartFile file, HttpServletRequest req) throws IOException {
        UserRow user = user(req);
        guard.requireEdit(user, id); // 업로드 = 노트 편집 권한
        String name = file.getOriginalFilename();
        if (name == null || name.isBlank()) throw VaultException.invalid("파일명이 없습니다");
        AttachmentRow row = svc.store(id, name, file.getBytes(), guard.who(user));
        audit.log(user, "attachment.add", row.id() + " -> " + id, req.getRemoteAddr());
        return ResponseEntity.status(HttpStatus.CREATED).body(Map.of(
            "id", row.id(), "filename", row.filename(), "size", row.size(), "url", "/api/attachments/" + row.id()));
    }

    @GetMapping("/attachments/{id}")
    public ResponseEntity<byte[]> download(@PathVariable String id, HttpServletRequest req) {
        UserRow user = user(req);
        AttachmentRow row = svc.findById(id);
        if (row == null) throw VaultException.notFound("첨부를 찾을 수 없습니다");
        guard.requireRead(user, row.nodeId()); // 열람 = 노트 read
        return serve(row);
    }

    @DeleteMapping("/attachments/{id}")
    @ResponseStatus(HttpStatus.NO_CONTENT)
    public void delete(@PathVariable String id, HttpServletRequest req) {
        UserRow user = user(req);
        AttachmentRow row = svc.findById(id);
        if (row == null) throw VaultException.notFound("첨부를 찾을 수 없습니다");
        guard.requireEdit(user, row.nodeId());
        svc.delete(id);
        audit.log(user, "attachment.remove", id + " -> " + row.nodeId(), req.getRemoteAddr());
    }

    /** 공유 서빙 — 토큰 검증(비증가) + 첨부가 그 노드 소속인지. 무효 전부 404. */
    @GetMapping("/share/{token}/attachments/{id}")
    public ResponseEntity<byte[]> shareDownload(@PathVariable String token, @PathVariable String id,
            HttpServletRequest req) {
        UserRow user = user(req);
        String nodeId = share.nodeIdForAttachment(token, user == null ? null : user.emp());
        AttachmentRow row = svc.findById(id);
        if (row == null || !row.nodeId().equals(nodeId)) throw VaultException.notFound("첨부를 찾을 수 없습니다");
        return serve(row);
    }

    private ResponseEntity<byte[]> serve(AttachmentRow row) {
        byte[] bytes = svc.read(row);
        boolean image = UploadPolicy.isImage(row.ext());
        String dispo = (image ? "inline" : "attachment") + "; filename*=UTF-8''"
            + URLEncoder.encode(row.filename(), StandardCharsets.UTF_8).replace("+", "%20");
        return ResponseEntity.ok()
            .header(HttpHeaders.CONTENT_TYPE, row.mime())
            .header(HttpHeaders.CONTENT_DISPOSITION, dispo)
            .header("X-Content-Type-Options", "nosniff")
            .body(bytes);
    }
}
```

`VaultGuard`에 `requireRead`가 없으면 추가 (read 가드 — `requireShare`가 내부적으로 `perm.canRead`를 쓰므로 동일 패턴):

```java
/** read(N) — 첨부 열람 가드. local/관리자 bypass. */
public void requireRead(UserRow user, String id) {
    if (bypass(user)) return;
    requireUser(user);
    if (!perm.canRead(user, id)) {
        throw VaultException.forbidden("열람 권한이 없습니다: " + id);
    }
}
```

`application.yml`에 multipart 한도 추가 (정책 cap보다 충분히 크게 — 실제 제한은 UploadPolicy가 422로):
```yaml
spring:
  servlet:
    multipart:
      max-file-size: 64MB
      max-request-size: 64MB
```

`ApiExceptionHandler`에 초과 핸들러 추가 (64MB 초과 시 Spring 예외 → 422 단일화):
```java
@ExceptionHandler(org.springframework.web.multipart.MaxUploadSizeExceededException.class)
public ResponseEntity<Map<String, String>> tooLarge(org.springframework.web.multipart.MaxUploadSizeExceededException e) {
    return ResponseEntity.unprocessableEntity().body(Map.of("error", "파일이 너무 큽니다"));
}
```

- [ ] **Step 4: 통과 확인**

Run: `cd backend && ./gradlew test --tests '*AttachmentApiTest'`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add backend/src/main/java/com/worknote/attachment/AttachmentController.java backend/src/main/java/com/worknote/vault/VaultGuard.java backend/src/main/resources/application.yml backend/src/main/java/com/worknote/ApiExceptionHandler.java backend/src/test/java/com/worknote/attachment/AttachmentApiTest.java
git commit -m "feat(backend): AttachmentController — 업로드·다운로드·삭제·공유 서빙 + read 가드"
```

---

## Task 7: 노트 purge 시 첨부 정리 연계

**Files:**
- Modify: `backend/src/main/java/com/worknote/vault/TrashPurgeService.java`
- Modify: `backend/src/main/java/com/worknote/vault/VaultService.java` (purge 경로)
- Test: `backend/src/test/java/com/worknote/vault/TrashPurgeServiceTest.java` (기존 — 케이스 추가)

- [ ] **Step 1: 기존 purge 경로 확인**

`TrashPurgeService.java`와 `VaultService.purge(id)`를 읽어 hard-delete 직전 지점을 찾는다. purge는 노드 subtree를 DELETE하므로, **삭제 대상 node id 집합을 먼저 수집**해 `AttachmentService.deleteForNodes(ids)`를 호출해야 한다(노드 삭제 후엔 집합을 못 구함).

- [ ] **Step 2: 실패 테스트 추가**

`TrashPurgeServiceTest.java`에 추가:
```java
@Test void purge_alsoRemovesAttachments() {
    // n1 노트 + 첨부 1건 시드 → trash → purge → 첨부 메타 0
    jdbc.update("INSERT INTO node(id,parent_id,type,name,position,deleted_at,deleted_by) " +
                "VALUES('np',NULL,'note','x',1,'2000-01-01T00:00:00','local')");
    attachmentService.store("np", "a.png", new byte[]{1}, "local");
    purgeService.purgeExpired(); // 또는 직접 purge 경로 호출 (기존 테스트 방식 따름)
    assertThat(attachmentService.findByNode("np")).isEmpty();
}
```
(`@Autowired AttachmentService attachmentService;` 추가. 만료 cutoff·메서드명은 기존 테스트 시그니처를 따른다.)

- [ ] **Step 3: 실패 확인**

Run: `cd backend && ./gradlew test --tests '*TrashPurgeServiceTest'`
Expected: FAIL

- [ ] **Step 4: 구현 — purge 직전 첨부 정리 주입**

`TrashPurgeService`(또는 `VaultService.purge`)에 `AttachmentService` 의존성을 주입하고, subtree id 수집 직후·node DELETE 직전에 `attachments.deleteForNodes(subtreeIds)` 호출. 예 (TrashPurgeService가 NodeMapper.subtreeIds + purgeSubtree를 쓰는 경우):
```java
List<String> ids = nodes.subtreeIds(root.id());
attachments.deleteForNodes(ids);   // 파일+메타 먼저
nodes.deleteTagsIn(ids);
nodes.purgeSubtree(root.id());
```
(실제 기존 코드 흐름에 맞춰 삽입 — 핵심은 "node 삭제 전에 deleteForNodes".)

- [ ] **Step 5: 통과 + 회귀 확인**

Run: `cd backend && ./gradlew test --tests '*TrashPurge*'`
Expected: PASS

- [ ] **Step 6: 커밋**

```bash
git add backend/src/main/java/com/worknote/vault/ backend/src/test/java/com/worknote/vault/TrashPurgeServiceTest.java
git commit -m "feat(backend): 노트 purge 시 첨부 파일·메타 함께 정리"
```

---

## Task 8: 관리자 업로드 정책 API (AdminSettingController)

**Files:**
- Create: `backend/src/main/java/com/worknote/admin/AdminSettingController.java`
- Create: `backend/src/main/java/com/worknote/admin/dto/UploadPolicyRequest.java`
- Test: `backend/src/test/java/com/worknote/admin/AdminSettingApiTest.java`

- [ ] **Step 1: 실패 테스트 작성**

서버 모드 admin 테스트는 기존 `AdminRoleApiTest` 패턴(세션 + admin 사용자 시드)을 따른다. 핵심 케이스:
```java
@Test void getUploadPolicy_returnsSeed() throws Exception {
    mvc.perform(get("/api/admin/settings/upload").session(adminSession))
        .andExpect(status().isOk())
        .andExpect(jsonPath("$.allowedExt", hasItem("png")))
        .andExpect(jsonPath("$.maxBytes").value(26214400));
}
@Test void putUploadPolicy_persists() throws Exception {
    mvc.perform(put("/api/admin/settings/upload").session(adminSession).contentType(APPLICATION_JSON)
            .content("{\"allowedExt\":[\"png\",\"svg\"],\"maxBytes\":5000}"))
        .andExpect(status().isNoContent());
    mvc.perform(get("/api/admin/settings/upload").session(adminSession))
        .andExpect(jsonPath("$.allowedExt", hasItem("svg")))
        .andExpect(jsonPath("$.maxBytes").value(5000));
}
@Test void nonAdmin_is403() throws Exception {
    mvc.perform(get("/api/admin/settings/upload").session(userSession)).andExpect(status().isForbidden());
}
```
(`adminSession`/`userSession` 셋업은 기존 admin 테스트 베이스 패턴을 그대로 차용.)

- [ ] **Step 2: 실패 확인**

Run: `cd backend && ./gradlew test --tests '*AdminSettingApiTest'`
Expected: FAIL

- [ ] **Step 3: 구현**

`UploadPolicyRequest.java`:
```java
package com.worknote.admin.dto;
import jakarta.validation.constraints.*;
import java.util.List;
public record UploadPolicyRequest(@NotEmpty List<String> allowedExt, @Min(1) long maxBytes) {}
```

`AdminSettingController.java` (기존 AdminXController의 `@RequestMapping("/api/admin")` + AdminGuard 패턴):
```java
package com.worknote.admin;

import com.worknote.admin.dto.UploadPolicyRequest;
import com.worknote.attachment.UploadPolicy;
import com.worknote.audit.AuditService;
import com.worknote.auth.AuthFilter;
import com.worknote.auth.UserRow;
import com.worknote.setting.SettingService;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.validation.Valid;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.*;
import java.util.List;
import java.util.Map;

@RestController
@RequestMapping("/api/admin")
public class AdminSettingController {
    private final SettingService settings;
    private final AdminGuard guard;
    private final AuditService audit;

    public AdminSettingController(SettingService settings, AdminGuard guard, AuditService audit) {
        this.settings = settings; this.guard = guard; this.audit = audit;
    }
    private static UserRow user(HttpServletRequest req) { return (UserRow) req.getAttribute(AuthFilter.CURRENT_USER); }

    @GetMapping("/settings/upload")
    public Map<String, Object> get(HttpServletRequest req) {
        guard.requireAdmin(user(req));
        UploadPolicy p = settings.uploadPolicy();
        return Map.of("allowedExt", List.copyOf(p.allowedExt()), "maxBytes", p.maxBytes());
    }

    @PutMapping("/settings/upload")
    @ResponseStatus(HttpStatus.NO_CONTENT)
    public void set(@Valid @RequestBody UploadPolicyRequest body, HttpServletRequest req) {
        UserRow u = user(req);
        guard.requireAdmin(u);
        settings.setUploadPolicy(body.allowedExt(), body.maxBytes());
        audit.log(u, "settings.upload", "ext=" + body.allowedExt().size() + " max=" + body.maxBytes(), req.getRemoteAddr());
    }
}
```
(`AdminGuard`의 실제 메서드명을 확인 — `requireAdmin` 또는 동등 메서드. 다른 AdminXController가 쓰는 가드 호출을 그대로 모방.)

- [ ] **Step 4: 통과 확인**

Run: `cd backend && ./gradlew test --tests '*AdminSettingApiTest'`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add backend/src/main/java/com/worknote/admin/AdminSettingController.java backend/src/main/java/com/worknote/admin/dto/UploadPolicyRequest.java backend/src/test/java/com/worknote/admin/AdminSettingApiTest.java
git commit -m "feat(backend): 관리자 업로드 정책 API (GET/PUT /admin/settings/upload)"
```

---

## Task 9: 프런트 AttachmentApi + reqForm

**Files:**
- Modify: `frontend/src/api/http.ts` (reqForm 추가)
- Create: `frontend/src/storage/AttachmentApi.ts`
- Test: `frontend/src/storage/AttachmentApi.test.ts`

- [ ] **Step 1: 실패 테스트 작성**

```ts
import { describe, it, expect, vi, beforeEach } from "vitest";
import { AttachmentApi } from "./AttachmentApi";

function mockFetch(status: number, body: unknown) {
  return vi.fn().mockResolvedValue({
    ok: status >= 200 && status < 300, status,
    json: () => Promise.resolve(body),
  });
}

describe("AttachmentApi", () => {
  beforeEach(() => { vi.restoreAllMocks(); });

  it("upload는 FormData를 multipart로 POST한다", async () => {
    const f = mockFetch(201, { id: "att-1", filename: "a.png", size: 3, url: "/api/attachments/att-1" });
    vi.stubGlobal("fetch", f);
    const file = new File([new Uint8Array([1, 2, 3])], "a.png", { type: "image/png" });
    const res = await AttachmentApi.upload("n1", file);
    expect(res.url).toBe("/api/attachments/att-1");
    const [path, init] = f.mock.calls[0];
    expect(path).toBe("/api/nodes/n1/attachments");
    expect(init.method).toBe("POST");
    expect(init.body).toBeInstanceOf(FormData);
    // Content-Type을 직접 설정하지 않는다 (브라우저가 boundary 부여)
    expect(init.headers?.["Content-Type"]).toBeUndefined();
  });

  it("url 헬퍼는 인코딩된 경로를 만든다", () => {
    expect(AttachmentApi.url("att-1")).toBe("/api/attachments/att-1");
  });

  it("remove는 DELETE를 보낸다", async () => {
    const f = mockFetch(204, {});
    vi.stubGlobal("fetch", f);
    await AttachmentApi.remove("att-1");
    expect(f.mock.calls[0][0]).toBe("/api/attachments/att-1");
    expect(f.mock.calls[0][1].method).toBe("DELETE");
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `cd frontend && pnpm exec vitest run src/storage/AttachmentApi.test.ts`
Expected: FAIL

- [ ] **Step 3: 구현 — reqForm + AttachmentApi**

`http.ts`에 추가 (req와 동일 오류·401 처리, Content-Type은 미설정):
```ts
/** multipart 업로드 — Content-Type을 설정하지 않아 브라우저가 boundary를 채운다. */
export async function reqForm<T>(path: string, form: FormData): Promise<T> {
  const res = await fetch(BASE + path, { method: "POST", body: form });
  if (!res.ok) {
    if (res.status === 401 && on401) on401();
    const body = (await res.json().catch(() => ({}))) as { error?: string };
    throw new ApiError(body.error ?? `HTTP ${res.status}`, res.status);
  }
  return res.status === 204 ? (undefined as T) : ((await res.json()) as T);
}
```

`AttachmentApi.ts`:
```ts
/* AttachmentApi — 노트 첨부 업로드/삭제. 업로드는 multipart(reqForm), 삭제는 JSON 코어(req). */
import { req, reqForm } from "../api/http";

export interface UploadedAttachment { id: string; filename: string; size: number; url: string; }

export const AttachmentApi = {
  upload: (nodeId: string, file: File) => {
    const form = new FormData();
    form.append("file", file);
    return reqForm<UploadedAttachment>(`/nodes/${encodeURIComponent(nodeId)}/attachments`, form);
  },
  url: (id: string) => `/api/attachments/${encodeURIComponent(id)}`,
  remove: (id: string) => req<void>(`/attachments/${encodeURIComponent(id)}`, { method: "DELETE" }),
};
```

- [ ] **Step 4: 통과 확인**

Run: `cd frontend && pnpm exec vitest run src/storage/AttachmentApi.test.ts`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add frontend/src/api/http.ts frontend/src/storage/AttachmentApi.ts frontend/src/storage/AttachmentApi.test.ts
git commit -m "feat(frontend): AttachmentApi + reqForm(multipart 업로드 코어)"
```

---

## Task 10: 마크다운 img src 제한 + 첨부 칩

**Files:**
- Modify: `frontend/src/lib/markdown.ts`
- Test: `frontend/src/lib/markdown.test.ts`

- [ ] **Step 1: 실패 테스트 작성**

```ts
import { describe, it, expect } from "vitest";
import { renderMarkdown } from "./markdown";

describe("renderMarkdown 이미지 src 제한", () => {
  it("내부 첨부 src는 보존한다", () => {
    const html = renderMarkdown("![cap](/api/attachments/att-1)");
    expect(html).toContain('src="/api/attachments/att-1"');
  });
  it("상대경로 src는 보존한다", () => {
    expect(renderMarkdown("![x](images/a.png)")).toContain('src="images/a.png"');
  });
  it("외부 http(s) src는 제거한다", () => {
    const html = renderMarkdown('<img src="https://evil.example/p.gif">');
    expect(html).not.toContain("evil.example");
  });
  it("data: src는 제거한다", () => {
    const html = renderMarkdown('<img src="data:image/png;base64,AAAA">');
    expect(html).not.toContain("data:image");
  });
  it("<img>의 width 속성은 보존한다", () => {
    const html = renderMarkdown('<img src="/api/attachments/att-1" width="100%">');
    expect(html).toContain('width="100%"');
  });
});

describe("renderMarkdown 첨부 칩", () => {
  it("/api/attachments 링크에 attach-chip 클래스를 부여한다", () => {
    const html = renderMarkdown("[보고서.pdf](/api/attachments/att-2)");
    expect(html).toContain("attach-chip");
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `cd frontend && pnpm exec vitest run src/lib/markdown.test.ts`
Expected: FAIL

- [ ] **Step 3: 구현 — DOMPurify 훅(1회 등록) + 칩 후처리**

`markdown.ts` 상단(모듈 로드 시 1회) DOMPurify 훅 등록:
```ts
// 이미지 src를 내부 첨부·상대경로로만 제한 (설계 결정 ②: 외부 콜백/추적 픽셀 차단).
// 허용: "/api/attachments/...", "/api/share/.../attachments/...", 스킴/"//" 없는 상대경로.
let imgHookAdded = false;
function ensureImgHook(): void {
  if (imgHookAdded) return;
  imgHookAdded = true;
  DOMPurify.addHook("afterSanitizeAttributes", (node) => {
    if (node.tagName !== "IMG") return;
    const src = node.getAttribute("src") || "";
    const internal = src.startsWith("/api/attachments/") || src.startsWith("/api/share/");
    const relative = !/^[a-z][a-z0-9+.-]*:/i.test(src) && !src.startsWith("//"); // 스킴·protocol-relative 아님
    if (!(internal || relative)) node.removeAttribute("src");
  });
}
```
`renderMarkdown` 진입부에서 `ensureImgHook()` 호출 후 기존 로직 진행. 칩은 frag 처리에 추가(task-list 처리 근처):
```ts
// 첨부 링크 → 다운로드 칩 (비이미지). href가 내부 첨부 경로인 a에 클래스 부여.
frag.querySelectorAll('a[href^="/api/attachments/"]').forEach((a) => {
  (a as HTMLElement).classList.add("attach-chip");
});
```
(클래스는 DOMPurify 기본이 보존. CSS `.attach-chip`는 styles에 종이클립 스타일로 추가 — 별도 토큰 불필요.)

- [ ] **Step 4: 통과 확인**

Run: `cd frontend && pnpm exec vitest run src/lib/markdown.test.ts`
Expected: PASS

- [ ] **Step 5: 칩 스타일 + 커밋**

`frontend/src/styles`(기존 CSS 위치)에 `.attach-chip` 규칙 추가 (인라인 칩: 배경 var(--surface-2), 패딩, 종이클립 ::before). 그 후:
```bash
git add frontend/src/lib/markdown.ts frontend/src/lib/markdown.test.ts frontend/src/styles
git commit -m "feat(frontend): 이미지 src 내부 제한 훅 + 첨부 다운로드 칩"
```

---

## Task 11: 에디터 drag&drop · paste · 📎 업로드

**Files:**
- Modify: `frontend/src/editor/cm.ts` (insertAtCursor 헬퍼)
- Modify: `frontend/src/components/Editor.tsx` (drop/paste/picker + 업로드)
- Modify: `frontend/src/App.tsx` (toast·canUpload prop 전달)
- Test: `frontend/src/editor/cm.test.ts` (insertAtCursor 단위) — 없으면 생성

- [ ] **Step 1: insertAtCursor 실패 테스트**

`cm.test.ts`(기존 있으면 추가, 없으면 생성):
```ts
import { describe, it, expect } from "vitest";
import * as cm from "./cm";

describe("cm.insertAtCursor", () => {
  it("커서 위치에 텍스트를 삽입한다", () => {
    const host = document.createElement("div");
    const view = cm.create(host, { doc: "ab" });
    view.dispatch({ selection: { anchor: 1 } });
    cm.insertAtCursor(view, "X");
    expect(view.state.doc.toString()).toBe("aXb");
    view.destroy();
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `cd frontend && pnpm exec vitest run src/editor/cm.test.ts`
Expected: FAIL

- [ ] **Step 3: cm.insertAtCursor 구현**

`cm.ts`에 export 추가:
```ts
export function insertAtCursor(view: EditorView, text: string): void {
  const { from, to } = view.state.selection.main;
  view.dispatch({ changes: { from, to, insert: text }, selection: { anchor: from + text.length } });
  view.focus();
}
```

- [ ] **Step 4: insertAtCursor 통과 확인**

Run: `cd frontend && pnpm exec vitest run src/editor/cm.test.ts`
Expected: PASS

- [ ] **Step 5: Editor.tsx 업로드 배선 (수동 검증 — 브라우저 동작)**

`Editor.tsx` 변경:
1. props에 `toast: (msg: string, icon?: string) => void` 와 `canUpload: boolean` 추가.
2. 숨은 파일 input ref 추가: `const fileRef = useRef<HTMLInputElement>(null);`
3. 업로드+삽입 헬퍼 (placeholder 없이 await 후 삽입 — 견고성 우선):
```ts
const uploadFiles = async (files: FileList | File[]) => {
  if (!props.canUpload) { props.toast("서버 모드에서만 첨부할 수 있습니다"); return; }
  const v = viewRef.current; if (!v) return;
  for (const file of Array.from(files)) {
    props.toast("업로드 중…");
    try {
      const res = await AttachmentApi.upload(note.id, file);
      const isImg = /\.(png|jpe?g|gif|webp)$/i.test(res.filename);
      const md = isImg ? `![${res.filename}](${res.url})` : `[📎 ${res.filename}](${res.url})`;
      cm.insertAtCursor(v, md + "\n");
      props.toast("첨부했습니다", "check");
    } catch (e) {
      props.toast(e instanceof ApiError ? e.message : "업로드 실패");
    }
  }
};
```
4. CM 마운트 useEffect에서 DOM 이벤트 등록 (`viewRef.current.dom`):
```ts
const dom = viewRef.current.dom;
const onDrop = (e: DragEvent) => {
  if (!e.dataTransfer?.files?.length) return;
  e.preventDefault();
  void uploadFiles(e.dataTransfer.files);
};
const onPaste = (e: ClipboardEvent) => {
  const files = e.clipboardData?.files;
  if (files && files.length) { e.preventDefault(); void uploadFiles(files); }
};
dom.addEventListener("drop", onDrop);
dom.addEventListener("paste", onPaste);
// cleanup에서 removeEventListener
```
(`uploadFiles`는 ref로 최신 note/props를 닫도록 `useRef` 패턴 사용 — 기존 `onChangeRef` 방식 모방하여 stale closure 방지.)
5. 툴바 `image` 핸들러 분기: `image: () => { if (props.canUpload) fileRef.current?.click(); else cm.block(viewRef.current!, TEMPLATES.image); }`
6. 숨은 input 렌더: `createElement("input", { type: "file", ref: fileRef, multiple: true, style: { display: "none" }, onChange: (e) => { if (e.target.files?.length) void uploadFiles(e.target.files); e.target.value = ""; } })`
7. import: `import { AttachmentApi } from "../storage/AttachmentApi"; import { ApiError } from "../api/http"; import * as cm from "../editor/cm";`(cm 이미 import됨).

`App.tsx` Editor 생성부에 prop 전달:
```ts
createElement(Editor, {
  key: activeNote.id, note: activeNote, theme: settings.dark ? "dark" : "light",
  onChange: (patch) => actions.updateNote(activeNote.id, patch),
  registerToolbar: (h) => { toolbarRef.current = h; },
  onView: (v) => { editorViewRef.current = v; },
  toast, canUpload: storageMode === "http",
})
```

- [ ] **Step 6: 빌드 + 수동 브라우저 검증**

Run: `cd frontend && pnpm build`
Expected: 빌드 성공(타입 에러 0). 브라우저 수동: http 모드 노트에 이미지 드롭 → `![..](/api/attachments/..)` 삽입 + 인라인 미리보기. local 모드 드롭 → toast.

- [ ] **Step 7: 커밋**

```bash
git add frontend/src/editor/cm.ts frontend/src/editor/cm.test.ts frontend/src/components/Editor.tsx frontend/src/App.tsx
git commit -m "feat(frontend): 에디터 drag&drop·paste·📎 파일 업로드 + 커서 삽입"
```

---

## Task 12: 공유뷰 첨부 src 재작성

**Files:**
- Modify: `frontend/src/share/SharePage.tsx`

- [ ] **Step 1: 현재 SharePage 렌더 흐름 확인**

`SharePage.tsx`에서 token을 알고 있고(`view` 로드 키), `bodyRef`에 `renderMarkdown` 결과를 주입하며 `enhanceMermaid(bodyRef.current)`를 호출하는 useEffect가 있다. 같은 useEffect에서 img src 재작성을 추가.

- [ ] **Step 2: src 재작성 추가**

`enhanceMermaid(bodyRef.current)` 직후:
```ts
// 공유 열람자는 노트 read 권한이 없으므로 첨부 이미지를 토큰 스코프 엔드포인트로 재지정.
bodyRef.current.querySelectorAll<HTMLImageElement>('img[src^="/api/attachments/"]').forEach((img) => {
  const id = img.getAttribute("src")!.slice("/api/attachments/".length);
  img.setAttribute("src", `/api/share/${encodeURIComponent(token)}/attachments/${encodeURIComponent(id)}`);
});
```
(`token`은 SharePage가 URL에서 파싱해 보유한 값 — 실제 변수명 확인 후 사용. 없으면 `new URLSearchParams(location.search).get("t")` 등 기존 추출 방식 재사용.)

- [ ] **Step 3: 빌드 + 수동 검증**

Run: `cd frontend && pnpm build`
Expected: 성공. 브라우저: 이미지 포함 노트를 공유 링크로 열면 이미지가 `/api/share/{token}/attachments/..`로 로드됨.

- [ ] **Step 4: 커밋**

```bash
git add frontend/src/share/SharePage.tsx
git commit -m "feat(frontend): 공유뷰 첨부 이미지 src를 토큰 스코프로 재작성"
```

---

## Task 13: 관리자 업로드 정책 화면 (Uploads 스크린)

**Files:**
- Modify: `frontend/src/admin/api.ts` (getUploadPolicy/setUploadPolicy)
- Create: `frontend/src/admin/screens/Uploads.tsx`
- Modify: `frontend/src/admin/AdminApp.tsx` (NAV/TITLES/screenMap)
- Test: `frontend/src/admin/api.test.ts` (기존 — 케이스 추가)

- [ ] **Step 1: api.ts 테스트 추가 (failing)**

`api.test.ts`에 추가 (기존 fetch mock 패턴):
```ts
it("getUploadPolicy는 정책을 GET한다", async () => {
  const f = mockFetch(200, { allowedExt: ["png"], maxBytes: 26214400 });
  vi.stubGlobal("fetch", f);
  const p = await AdminApi.getUploadPolicy();
  expect(p.allowedExt).toContain("png");
  expect(f.mock.calls[0][0]).toBe("/api/admin/settings/upload");
});
it("setUploadPolicy는 PUT한다", async () => {
  const f = mockFetch(204, {});
  vi.stubGlobal("fetch", f);
  await AdminApi.setUploadPolicy(["png", "svg"], 5000);
  expect(f.mock.calls[0][1].method).toBe("PUT");
});
```

- [ ] **Step 2: 실패 확인**

Run: `cd frontend && pnpm exec vitest run src/admin/api.test.ts`
Expected: FAIL

- [ ] **Step 3: api.ts 구현**

`AdminApi`에 추가:
```ts
export interface UploadPolicy { allowedExt: string[]; maxBytes: number; }
// ... AdminApi 객체 안:
getUploadPolicy: () => req<UploadPolicy>("/admin/settings/upload"),
setUploadPolicy: (allowedExt: string[], maxBytes: number) =>
  req<void>("/admin/settings/upload", { method: "PUT", body: JSON.stringify({ allowedExt, maxBytes }) }),
```

- [ ] **Step 4: api 테스트 통과**

Run: `cd frontend && pnpm exec vitest run src/admin/api.test.ts`
Expected: PASS

- [ ] **Step 5: Uploads 스크린 구현 (Roles.tsx 패턴)**

`Uploads.tsx` — chip add/remove 확장자 + MB 입력 + 저장. 로드는 자체 useEffect(`AdminApi.getUploadPolicy`), 저장은 `run()` 헬퍼(Roles 패턴). SVG 포함 시 경고 문구. `SecHead`·`panel` 클래스 재사용. 핵심 골격:
```tsx
export function Uploads({ toast }: { toast: (msg: string, icon?: string) => void }) {
  const [exts, setExts] = useState<string[]>([]);
  const [maxMb, setMaxMb] = useState(25);
  const [draft, setDraft] = useState("");
  const [busy, setBusy] = useState(false);
  useEffect(() => { void AdminApi.getUploadPolicy().then((p) => { setExts(p.allowedExt); setMaxMb(Math.round(p.maxBytes / 1024 / 1024)); }).catch(() => toast("정책을 불러오지 못했습니다")); }, []);
  const addExt = (v: string) => { const e = v.trim().toLowerCase().replace(/^\./, ""); if (e && !exts.includes(e)) setExts([...exts, e]); setDraft(""); };
  const save = async () => {
    if (busy) return; setBusy(true);
    try { await AdminApi.setUploadPolicy(exts, maxMb * 1024 * 1024); toast("저장했습니다", "check"); }
    catch (e) { toast(e instanceof ApiError ? e.message : "저장 실패"); }
    finally { setBusy(false); }
  };
  // ... chip 목록(× 제거), draft input(Enter/comma=addExt), MB number input, SVG 경고, 저장 버튼
}
```

- [ ] **Step 6: AdminApp 등록**

`AdminApp.tsx`: import `Uploads`, NAV에 `{ id: "uploads", label: "업로드 정책", icon: "image" }`(security 앞), TITLES에 `uploads: ["업로드 정책", "첨부 허용 확장자·용량"]`, screenMap에 `uploads: Uploads`.

- [ ] **Step 7: 빌드 + 수동 검증**

Run: `cd frontend && pnpm build`
Expected: 성공. 관리자 화면에서 확장자 추가/삭제·용량 변경·저장 → 재로드 시 유지.

- [ ] **Step 8: 커밋**

```bash
git add frontend/src/admin/
git commit -m "feat(frontend): 관리자 업로드 정책 화면(Uploads) + AdminApi 배선"
```

---

## Task 14: 통합 — 전체 테스트 + 단일 jar + e2e 시나리오 문서

**Files:**
- Create: `docs/qa-e2e-첨부파일.md`
- Modify: `CLAUDE.md` (첨부 기능 한 줄 추가)

- [ ] **Step 1: 백엔드 전체 테스트**

Run: `cd backend && ./gradlew test`
Expected: 전체 PASS (회귀 없음)

- [ ] **Step 2: 프런트 전체 테스트 + 빌드**

Run: `cd frontend && pnpm test && pnpm build`
Expected: 전체 PASS, dist 빌드 성공

- [ ] **Step 3: e2e 시나리오 문서 작성**

`docs/qa-e2e-첨부파일.md`에 화면 e2e 시나리오 작성 (기존 `docs/qa-e2e-scenarios.md` 배치 형식 계승). 최소 배치:
- B1 이미지 드롭 업로드 → 인라인 미리보기 (에디터)
- B2 `<img src="/api/attachments/.." width=100%>` 수동 작성 → 미리보기 + width 적용
- B3 비이미지(pdf) 드롭 → 다운로드 칩 → 클릭 다운로드
- B4 외부 `<img src="https://..">` → 미표시(차단) 확인
- B5 paste 업로드(클립보드 이미지)
- B6 확장자 미허용 파일 드롭 → 거부 toast(422)
- B7 용량 초과 드롭 → 거부 toast
- B8 관리자 업로드 정책: 확장자 추가/삭제·용량 변경·저장·재로드 유지
- B9 관리자가 차단한 확장자 → 업로드 거부 연동 확인
- B10 공유 링크 열람 시 첨부 이미지 토큰 스코프 로드
- B11 노트 휴지통→purge 후 첨부 파일 정리(서버 디스크/메타)
- B12 local 모드 드롭 → "서버 모드에서만" toast
각 배치에 사전조건·단계·기대결과·자동화 가능성(자동/반자동/수동) 표기.

- [ ] **Step 4: CLAUDE.md 갱신**

`backend/` 설명에 "+ 첨부 파일(디스크 저장·노트 종속·공유 토큰 서빙)" 한 줄, `frontend/` 설명에 "drag&drop 첨부·이미지 미리보기" 한 줄 추가.

- [ ] **Step 5: 커밋**

```bash
git add docs/qa-e2e-첨부파일.md CLAUDE.md
git commit -m "docs: 첨부 기능 e2e 시나리오 + CLAUDE.md 갱신"
```

---

## Self-Review 체크리스트 (실행 전 controller 확인)

1. **스펙 커버리지:** drop/paste/📎(T11) · 마크다운+`<img>` 미리보기(T10) · 비이미지 칩(T10) · 관리자 확장자·용량(T8/T13) · 파일시스템 저장(T4) · 내부 src 제한(T10) · 공유 서빙(T6/T12) · purge 정리(T7). 전 항목 태스크 매핑됨.
2. **플레이스홀더:** 모든 코드 스텝에 실제 코드/시그니처. 기존 파일 의존부(AdminGuard 메서드명·SharePage token 변수·TrashPurge 흐름)는 "확인 후 모방" 주석으로 명시 — 구현자가 실파일을 읽고 맞춘다.
3. **타입 일관성:** `AttachmentRow`(9필드)·`UploadPolicy.of/check/ext/isImage`·`SettingService.uploadPolicy/setUploadPolicy`·`AttachmentService.store/findById/delete/deleteForNodes/read/pathOf`·`AttachmentApi.upload/url/remove`·`UploadedAttachment{id,filename,size,url}` — 전 태스크 일치.
4. **순서 의존:** T1→T2→T3→T4(SettingService 의존)→T5→T6(서비스·가드·공유 의존)→T7→T8 / 프런트 T9→T10→T11→T12→T13 / T14 통합. 백엔드 먼저(API 계약 확정) 후 프런트.
