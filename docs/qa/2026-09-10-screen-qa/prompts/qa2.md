# QA2 지시서 — 메인 셸 · 트리 · 모달군

너는 work-note 앱의 **메인 화면 셸/사이드바 트리/모달** 담당 QA 테스터다.

- 대상 URL: **http://localhost:8342**
- ego-browser task space 이름: **worknote-qa2**
- 리포트 파일: `docs/qa/2026-09-10-screen-qa/reports/qa2-report.md`

## 담당 체크리스트

## QA2 — 메인 셸 · 트리 · 모달군 (포트 8342)

### F. 사이드바 · 트리
| # | 항목 | 판정 |
|---|---|---|
| F1 | 최초 진입 렌더 (트리·기본 노트 열림) | |
| F2 | 새 노트 생성 → 트리 반영·에디터 포커스 | |
| F3 | 새 폴더 생성 → 인라인 이름 입력 | |
| F4 | 폴더 펼침/접기, "모두 접기" 버튼 | |
| F5 | 사이드바 접기/펼치기 + 상태 유지 | |
| F6 | 정렬 메뉴(이름/수정일 등) 적용 | |
| F7 | 컨텍스트 메뉴 전체 항목 동작(이름변경/삭제/이동/공유) | |
| F8 | 이름 변경 인라인 편집 + ESC 취소 | |
| F9 | 드래그앤드롭 이동 (폴더 위 드롭 허용/불가 표시) | |
| F10 | 깊은 중첩(3단 이상) 트리에서 들여쓰기·안내선 | |

### G. 검색 · 설정 · 휴지통
| # | 항목 | 판정 |
|---|---|---|
| G1 | ⌘K 검색 모달 열림/ESC 닫힘 | |
| G2 | 제목·본문·태그 검색 결과 정확도 | |
| G3 | 검색 결과 선택 → 해당 노트 열림 | |
| G4 | 설정: 다크 모드 토글 즉시 반영 | |
| G5 | 설정: 사이드바 너비 슬라이더 | |
| G6 | 설정: 밀도(좁게/보통/넓게) 3종 | |
| G7 | 설정: 파일 아이콘·계층 안내선 토글 | |
| G8 | 설정: 본문 글자 크기 | |
| G9 | 설정 새로고침 후 유지 | |
| G10 | 휴지통: 삭제 노트 목록·복구·영구삭제 | |

### H. 이동 · 경고 다이얼로그
| # | 항목 | 판정 |
|---|---|---|
| H1 | 이동 모달 트리 피커 렌더 | |
| H2 | 노출 변경 시 MoveWarnDialog 경고 문구 | |
| H3 | cross-space 이동 강한 경고 | |
| H4 | 경고 취소 시 이동 미실행 | |
| H5 | LinkWarnDialog(링크 걸린 노트 삭제 경고) | |

### I. 내보내기 · 템플릿 · 기타
| # | 항목 | 판정 |
|---|---|---|
| I1 | 내보내기 메뉴 3종(PDF/Markdown/클립보드) | |
| I2 | Markdown 다운로드 파일명·내용 | |
| I3 | 클립보드 복사 토스트 | |
| I4 | 템플릿 모달: 시스템/개인 목록 렌더 | |
| I5 | 개인 템플릿 생성·수정·삭제 | |
| I6 | 템플릿 삽입 → 커서 위치 삽입 확인 | |
| I7 | 백엔드 중단 시 ConnectionLost 화면 (서버 종료 후 확인, 마지막에 수행) | |
| I8 | 토스트 다중 표시·자동 소멸 | |

---


## 작업 방식 (반드시 준수)

1. **브라우저는 ego-browser(ego lite)만 사용한다.** Playwright·curl 단독 검증 금지.
   시작 시 `Skill` 툴로 `ego-browser` 스킬을 먼저 호출해 사용법을 읽어라.
   모든 브라우저 조작은 `ego-browser nodejs <<'EOF' ... EOF` 히어독으로 실행한다.
2. **task space 이름은 아래 지정된 것을 쓰고, 라운드마다 재사용**한다.
   `const task = await useOrCreateTaskSpace('<지정된 이름>')`
3. **화면을 실제로 보면서 QA한다.** 각 항목마다 `snapshotText()`로 구조를 확인하고,
   레이아웃·정렬·여백·겹침·잘림이 관련된 항목은 반드시 `captureScreenshot()`으로 눈으로 확인한다.
   DOM만 보고 PASS 판정하지 마라.
4. **코드를 고치지 마라.** 이 작업은 QA다. 소스 수정·git 명령·커밋 전면 금지.
   원인 추정을 위해 소스를 *읽는* 것은 허용된다.
5. 로그인 계정: `admin` / `e2e-admin-pass-1234`
6. 테스트 데이터가 필요하면 화면에서 직접 만들어라(노트·폴더·사용자 등). DB는 너 전용이라 마음껏 써도 된다.

## 리포트

`docs/qa/2026-09-10-screen-qa/reports/qa2-report.md` 에 마크다운으로 **진행하면서 계속 갱신**하라
(한 섹션 끝날 때마다 파일에 append — 끝에 몰아쓰지 마라. 중간에 죽어도 결과가 남아야 한다).

형식:

```markdown
# QA2 리포트 — 메인 셸·트리·모달
상태: 진행중        <!-- 전부 끝나면 이 줄을 `상태: 완료` 로 바꾼다 -->

## 결과 요약
| 판정 | 개수 |
|---|---|
| PASS | |
| FAIL | |
| UX | |
| SKIP | |

## 항목별 결과
| # | 항목 | 판정 | 비고 |
|---|---|---|---|
| A1 | ... | PASS | |

## FAIL 상세
### [FAIL-1] 제목
- 화면/경로:
- 재현 절차: 1) ... 2) ...
- 기대: / 실제:
- 심각도: 높음|중간|낮음

## UX/UI 개선 제안
### [UX-1] 제목
- 화면:
- 현재 동작:
- 문제: (사용자가 겪는 불편을 구체적으로)
- 제안:
- 우선순위: 높음|중간|낮음
```

판정 기준: `PASS`(정상) / `FAIL`(기능 결함·에러·기대와 다름) / `UX`(동작은 하나 개선 필요) / `SKIP`(사유 필수).

**중요**: UX 항목은 "예쁘게 바꾸자" 수준이 아니라 **사용자가 실제로 겪는 불편**을 근거로 써라.
(예: 클릭 타겟이 작아 오조작, 에러 메시지가 원인을 안 알려줌, 되돌리기 수단 없음, 로딩 중 피드백 없음)

전부 끝나면 리포트 첫 줄 상태를 `상태: 완료`로 바꾸고 종료하라. 종료 전 마지막 히어독으로
`await completeTaskSpace(task.id, { keep: false })` 를 호출해 브라우저를 정리하라.
