# QA4 지시서 — 관리자 화면

너는 work-note 앱의 **관리자 페이지(admin.html) 13개 스크린** 담당 QA 테스터다.

- 대상 URL: **http://localhost:8344**
- ego-browser task space 이름: **worknote-qa4**
- 리포트 파일: `docs/qa/2026-09-10-screen-qa/reports/qa4-report.md`

## 담당 체크리스트

## QA4 — 관리자 화면 (포트 8344)

### P. 셸 · 대시보드 · 승인
| # | 항목 | 판정 |
|---|---|---|
| P1 | admin.html 진입·좌측 nav 13항목 렌더 | |
| P2 | nav 접기/펼치기 + 상태 유지 | |
| P3 | 해시 라우팅(새로고침 시 화면 유지) | |
| P4 | "노트로 돌아가기" 링크 | |
| P5 | 대시보드 통계 카드·최근 활동 | |
| P6 | 가입 승인 대기 목록 → 승인/거부 | |
| P7 | 승인 후 해당 계정 로그인 가능 확인 | |

### Q. 사용자 · 권한 · 역할
| # | 항목 | 판정 |
|---|---|---|
| Q1 | 사용자 목록 렌더·정렬·검색 | |
| Q2 | 사용자 생성/수정/비활성화 | |
| Q3 | 마지막 관리자 락아웃 방지 경고 | |
| Q4 | 권한 관리: 폴더 트리 + ACL grant/deny 부여 | |
| Q5 | deny 우선·deny-sticky 동작 표기 | |
| Q6 | public 노출 토글 | |
| Q7 | 역할 목록: 시스템 역할 → 읽기전용 '보기' 모달 | |
| Q8 | 커스텀 역할 생성·편집·삭제 | |

### R. 팀 · 스페이스 · PII
| # | 항목 | 판정 |
|---|---|---|
| R1 | 팀 생성·구성원 추가/제거 | |
| R2 | 팀 스페이스 생성 + 소유 팀 자동 grant | |
| R3 | 스페이스 삭제 가드 | |
| R4 | 개인정보 점검 목록·필터 | |
| R5 | PII 노트 본문 모달 + 라인 포커스 이동 | |
| R6 | PII 대량 결과 가상 스크롤 동작 | |

### S. 업로드 정책 · Redmine · 템플릿
| # | 항목 | 판정 |
|---|---|---|
| S1 | 업로드 정책: 허용 확장자 편집·저장 | |
| S2 | 업로드 정책: 최대 용량 변경 반영 | |
| S3 | Redmine 연동 설정 저장·연결 테스트 | |
| S4 | Redmine 비활성 시 사용자 화면 노출 안 됨 | |
| S5 | 시스템 템플릿 생성·수정·삭제 | |
| S6 | 템플릿 50개 상한/검증 메시지 | |
| S7 | 관리자 화면 전반 반응형(좁은 폭 테이블 가로 스크롤) | |
| S8 | 빈 상태(Empty state) 문구 일관성 | |

## 작업 방식 (반드시 준수)

1. **브라우저는 ego-browser(ego lite)만 사용한다.** Playwright·curl 단독 검증 금지.
   시작 시 `Skill` 툴로 `ego-browser` 스킬을 먼저 호출해 사용법을 읽어라.
   모든 브라우저 조작은 `ego-browser nodejs <<'EOF' ... EOF` 히어독으로 실행한다.
2. **task space 이름은 아래 지정된 것을 쓰고, 라운드마다 재사용**한다.
   `const task = await useOrCreateTaskSpace('<지정된 이름>')`
3. **화면을 실제로 보면서 QA한다.** 각 항목마다 `snapshotText()`로 구조를 확인하고,
   레이아웃·정렬·여백·겹침·잘림이 관련된 항목은 반드시 `captureScreenshot()`으로 눈으로 확인한다.
   DOM만 보고 PASS 판정하지 마라.
4. **코드를 고치지 마라.** 이 작업은 QA다. 소스 수정·git 명령·커밋 전면 금지.
   원인 추정을 위해 소스를 *읽는* 것은 허용된다.
5. 로그인 계정: `admin` / `e2e-admin-pass-1234`
6. 테스트 데이터가 필요하면 화면에서 직접 만들어라(노트·폴더·사용자 등). DB는 너 전용이라 마음껏 써도 된다.

## 리포트

`docs/qa/2026-09-10-screen-qa/reports/qa4-report.md` 에 마크다운으로 **진행하면서 계속 갱신**하라
(한 섹션 끝날 때마다 파일에 append — 끝에 몰아쓰지 마라. 중간에 죽어도 결과가 남아야 한다).

형식:

```markdown
# QA4 리포트 — 관리자 화면
상태: 진행중        <!-- 전부 끝나면 이 줄을 `상태: 완료` 로 바꾼다 -->

## 결과 요약
| 판정 | 개수 |
|---|---|
| PASS | |
| FAIL | |
| UX | |
| SKIP | |

## 항목별 결과
| # | 항목 | 판정 | 비고 |
|---|---|---|---|
| A1 | ... | PASS | |

## FAIL 상세
### [FAIL-1] 제목
- 화면/경로:
- 재현 절차: 1) ... 2) ...
- 기대: / 실제:
- 심각도: 높음|중간|낮음

## UX/UI 개선 제안
### [UX-1] 제목
- 화면:
- 현재 동작:
- 문제: (사용자가 겪는 불편을 구체적으로)
- 제안:
- 우선순위: 높음|중간|낮음
```

판정 기준: `PASS`(정상) / `FAIL`(기능 결함·에러·기대와 다름) / `UX`(동작은 하나 개선 필요) / `SKIP`(사유 필수).

**중요**: UX 항목은 "예쁘게 바꾸자" 수준이 아니라 **사용자가 실제로 겪는 불편**을 근거로 써라.
(예: 클릭 타겟이 작아 오조작, 에러 메시지가 원인을 안 알려줌, 되돌리기 수단 없음, 로딩 중 피드백 없음)

전부 끝나면 리포트 첫 줄 상태를 `상태: 완료`로 바꾸고 종료하라. 종료 전 마지막 히어독으로
`await completeTaskSpace(task.id, { keep: false })` 를 호출해 브라우저를 정리하라.
